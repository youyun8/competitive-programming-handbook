---
id: hdu-2594
volume: lower
source_file: lower-volume
title: HDU 2594 Simpsons’ Hidden Talents
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [kmp, border, concatenation]
prerequisites: [kmp, string]
statement: '求第一個字串的最長前綴，使它同時是第二個字串的後綴；輸出該字串及長度。'
constraints:
  - '每個字串長度不超過 50000'
  - '輸入多組直到 EOF'
input_format: '每組兩個字串 s1 s2。'
output_format: '若長度正，輸出前綴與長度；否則輸出 0。'
samples:
  - input: "abc zzabc\nab cd\n"
    output: "abc 3\n0"
    explanation: '第一組 abc 是 s1 前綴與 s2 後綴；第二組無非空答案。'
core_knowledge:
  - 'kmp'
  - 'border'
  - 'concatenation'
judgment: '若長度正，輸出前綴與長度；否則輸出 0。'
hints:
  - '只需讓模式 s1 掃過文本 s2。'
  - '掃完 s2 時的 matched 就是 s2 後綴與 s1 前綴的最長匹配。'
  - '途中達到完整 s1 後若還有字元，沿 border 繼續；最後依 matched 輸出。'
solution_outline: '以 s1 的前綴函數 KMP 掃 s2，不特別計數匹配，保留掃描結束狀態。'
proof_or_invariant: 'KMP 狀態始終是已掃文本的最長模式前綴後綴，掃完整個 s2 後正是題目定義的最長值。'
common_errors:
  - '找到完整 s1 就提前結束'
  - '輸出 s2 的前綴'
  - '零長度仍輸出空格'
complexity:
  time: 'O(|s1|+|s2|)'
  space: 'O(|s1|)'
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成核心演算法。*/return 0;}
cpp_solution: |
  #include <iostream>
  #include <string>
  #include <vector>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);string p,s;while(cin>>p>>s){vector<int>pi(p.size());for(size_t i=1;i<p.size();++i){int j=pi[i-1];while(j>0&&p[i]!=p[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j-1)];if(p[i]==p[static_cast<size_t>(j)])++j;pi[i]=j;}int j=0;for(char c:s){while(j>0&&(j==static_cast<int>(p.size())||c!=p[static_cast<size_t>(j)]))j=pi[static_cast<size_t>(j-1)];if(c==p[static_cast<size_t>(j)])++j;}if(j==0)cout<<"0\n";else cout<<p.substr(0,static_cast<size_t>(j))<<' '<<j<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2594
external_platform: HDU
external_problem_id: '2594'
external_title: 'Simpsons’ Hidden Talents'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

以線性字串結構重用已知前後綴資訊，避免重新比較。
