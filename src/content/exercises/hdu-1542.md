---
id: hdu-1542
volume: upper
source_file: upper-volume
title: HDU 1542 Atlantis
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - sweep-line
  - coordinate-compression
  - covered-length
prerequisites:
  - segment-tree
statement: 給定多個軸平行矩形，求聯集面積。
constraints:
  - n <= 100
  - 座標為浮點數
  - n=0 結束
input_format: 多組 n 與 n 個 x1 y1 x2 y2。
output_format: 依題面案例格式輸出面積至兩位小數。
samples:
  - input: |
      2
      0 0 2 2
      1 1 3 3
      0
    output: |+
      Test case #1
      Total explored area: 7.00

    explanation: 兩個面積四的正方形重疊一，聯集為七。
core_knowledge: *id001
judgment: 掃描線加覆蓋次數線段樹。
hints:
  - 沿 y 掃描矩形上下邊事件。
  - x 離散化後，線段樹維護目前被至少一個矩形覆蓋的總長。
  - 相鄰 y 事件間覆蓋不變，面積增量為長度乘高度差。
solution_outline: 掃描線加覆蓋次數線段樹。
proof_or_invariant: 每個 y 帶狀區間內活躍矩形集合固定；線段樹長度是該帶聯集橫寬，乘高度後各帶互斥且完整。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O(n log n)
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;struct Event{double y,x1,x2;int delta;};struct Node{int cover=0;double length=0;};static vector<Node>tr;static vector<double>xs;static void pull(int p,int l,int r){if(tr[static_cast<size_t>(p)].cover>0)tr[static_cast<size_t>(p)].length=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)];else if(l==r)tr[static_cast<size_t>(p)].length=0;else tr[static_cast<size_t>(p)].length=tr[static_cast<size_t>(p*2)].length+tr[static_cast<size_t>(p*2+1)].length;}static void update(int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].cover+=d;else{int m=(l+r)/2;update(p*2,l,m,ql,qr,d);update(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,case_no=0;while(cin>>n&&n!=0){vector<Event>events;xs.clear();for(int i=0;i<n;++i){double x1,y1,x2,y2;cin>>x1>>y1>>x2>>y2;events.push_back({y1,x1,x2,1});events.push_back({y2,x1,x2,-1});xs.push_back(x1);xs.push_back(x2);}sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(events.begin(),events.end(),[](const Event&a,const Event&b){return a.y<b.y;});tr.assign(xs.size()*4+4,Node{});double area=0;for(size_t i=0;i<events.size();++i){int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),events[i].x1)-xs.begin());int r=static_cast<int>(lower_bound(xs.begin(),xs.end(),events[i].x2)-xs.begin())-1;if(l<=r)update(1,0,static_cast<int>(xs.size())-2,l,r,events[i].delta);if(i+1<events.size())area+=tr[1].length*(events[i+1].y-events[i].y);}cout<<"Test case #"<<++case_no<<"\nTotal explored area: "<<fixed<<setprecision(2)<<area<<"\n\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct Event{double y,x1,x2;int delta;};struct Node{int cover=0;double length=0;};static vector<Node>tr;static vector<double>xs;static void pull(int p,int l,int r){if(tr[static_cast<size_t>(p)].cover>0)tr[static_cast<size_t>(p)].length=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)];else if(l==r)tr[static_cast<size_t>(p)].length=0;else tr[static_cast<size_t>(p)].length=tr[static_cast<size_t>(p*2)].length+tr[static_cast<size_t>(p*2+1)].length;}static void update(int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].cover+=d;else{int m=(l+r)/2;update(p*2,l,m,ql,qr,d);update(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,case_no=0;while(cin>>n&&n!=0){vector<Event>events;xs.clear();for(int i=0;i<n;++i){double x1,y1,x2,y2;cin>>x1>>y1>>x2>>y2;events.push_back({y1,x1,x2,1});events.push_back({y2,x1,x2,-1});xs.push_back(x1);xs.push_back(x2);}sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(events.begin(),events.end(),[](const Event&a,const Event&b){return a.y<b.y;});tr.assign(xs.size()*4+4,Node{});double area=0;for(size_t i=0;i<events.size();++i){int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),events[i].x1)-xs.begin());int r=static_cast<int>(lower_bound(xs.begin(),xs.end(),events[i].x2)-xs.begin())-1;if(l<=r)update(1,0,static_cast<int>(xs.size())-2,l,r,events[i].delta);if(i+1<events.size())area+=tr[1].length*(events[i+1].y-events[i].y);}cout<<"Test case #"<<++case_no<<"\nTotal explored area: "<<fixed<<setprecision(2)<<area<<"\n\n";}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1542
external_platform: HDU
external_problem_id: '1542'
external_title: HDU 1542 Atlantis
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
