---
id: hdu-2460
volume: lower
source_file: lower-volume
original_label: 'HDU 2460'
title: 'HDU 2460 Network'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 4
topics: [bridge, incremental-edges, disjoint-set]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  初始連通無向網路逐次新增連線；每次加入後輸出目前橋的數量。
constraints: [1 <= n <= 100000, n-1 <= m <= 200000, 1 <= q <= 1000]
input_format: >-
  多組 n m 與 m 行邊，再給 q 與 q 行新增邊；0 0 結束。
output_format: >-
  每案輸出 Case k:，接著每次操作後的橋數，案末空行。
samples:
  - input: |
      3 2
      1 2
      2 3
      2
      1 2
      1 3
      0 0
    output: |
      Case 1:
      1
      0
    explanation: >-
      重加 1-2 不影響另一座橋；加入 1-3 後整條鏈成環。
core_knowledge: [橋, DFS 樹路徑, DSU 跳躍]
judgment: >-
  新增 u-v 後，DFS 樹路徑上尚為橋的邊全部且僅有這些失效。
hints:
  - 先 Tarjan 標記初始橋。
  - 樹邊以較深端點代表。
  - 失效邊永不恢復，可用並查集跳過。
solution_outline: >-
  保存初始 DFS parent/depth 與橋標記；每次將兩端沿父鏈向 LCA 拉近，遇橋則減答案，並壓縮已處理路徑。
proof_or_invariant: >-
  新增邊與任一既有 u-v 路徑形成環，路徑上的橋因此失效；路徑外邊不新落入環。壓縮只跳過已證實非橋且永不復原的樹邊，故不漏計。
complexity: { time: 'O((n+m+q)*alpha(n)) amortized', space: 'O(n+m)' }
common_errors: [新增邊後重算所有橋, 同一失效橋重複扣除, 用父頂點而非邊編號處理重邊]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m,cs=0;while(cin>>n>>m&&(n||m)){vector<vector<pair<int,int>>>g(n);for(int i=0;i<m;++i){int u,v;cin>>u>>v;--u;--v;g[u].push_back({v,i});g[v].push_back({u,i});}vector<int>d(n),low(n),parent(n,-1),depth(n),jump(n);vector<char>bridge(n);int timer=0,count_bridges=0;auto dfs=[&](auto&&self,int u,int pe)->void{d[u]=low[u]=++timer;for(auto [v,id]:g[u])if(id!=pe){if(!d[v]){parent[v]=u;depth[v]=depth[u]+1;self(self,v,id);low[u]=min(low[u],low[v]);if(low[v]>d[u])bridge[v]=1,++count_bridges;}else low[u]=min(low[u],d[v]);}};dfs(dfs,0,-1);iota(jump.begin(),jump.end(),0);auto find=[&](auto&&self,int x)->int{return jump[x]==x?x:jump[x]=self(self,jump[x]);};int queries;cin>>queries;cout<<"Case "<<++cs<<":\n";while(queries--){int u,v;cin>>u>>v;--u;--v;while((u=find(find,u))!=(v=find(find,v))){if(depth[u]<depth[v])swap(u,v);if(bridge[u])bridge[u]=0,--count_bridges;jump[u]=find(find,parent[u]);}cout<<count_bridges<<'\n';}cout<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2460
external_platform: HDU
external_problem_id: '2460'
external_title: 'Network'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
