---
id: hdu-1686
volume: lower
source_file: lower-volume
title: HDU 1686 Oulipo
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [kmp, overlapping-match, prefix-function]
prerequisites: [kmp, string]
statement: '對每組模式與文本，計算模式在文本中的出現次數，重疊亦計。'
constraints:
  - '測資組數不超過 20'
  - '模式長度不超過 10^4'
  - '文本長度不超過 10^6'
input_format: '第一行 T；每組先模式、後文本。'
output_format: '每組輸出出現次數。'
samples:
  - input: "2\nAZA\nAZAZAZA\nAA\nAAAA\n"
    output: "3\n3"
    explanation: 'AZA 出現三次；AA 在 AAAA 的三個起點出現。'
core_knowledge:
  - 'kmp'
  - 'overlapping-match'
  - 'prefix-function'
judgment: '每組輸出出現次數。'
hints:
  - '建立模式前綴函數。'
  - '掃文本失配時沿 border 回退，文本指標不退。'
  - '完整匹配後回到 pi[m-1]，保留可重疊後綴。'
solution_outline: '標準 KMP 計數所有匹配。'
proof_or_invariant: 'matched 不變量為目前文本前綴最長的模式前綴後綴；border 回退枚舉所有可能，完整後保留最長 border，故重疊不漏。'
common_errors:
  - '完整匹配後清零漏重疊'
  - '輸入順序顛倒'
  - '逐起點比較超時'
complexity:
  time: 'O(n+m)'
  space: 'O(m)'
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成核心演算法。*/return 0;}
cpp_solution: |
  #include <iostream>
  #include <string>
  #include <vector>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int t=0;cin>>t;while(t--){string p,s;cin>>p>>s;vector<int> pi(p.size());for(size_t i=1;i<p.size();++i){int j=pi[i-1];while(j>0&&p[i]!=p[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j-1)];if(p[i]==p[static_cast<size_t>(j)])++j;pi[i]=j;}int j=0,ans=0;for(char c:s){while(j>0&&c!=p[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j-1)];if(c==p[static_cast<size_t>(j)])++j;if(j==static_cast<int>(p.size())){++ans;j=pi.back();}}cout<<ans<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1686
external_platform: HDU
external_problem_id: '1686'
external_title: 'Oulipo'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

以線性字串結構重用已知前後綴資訊，避免重新比較。
