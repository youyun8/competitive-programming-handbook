---
id: 洛谷-p2568
volume: lower
source_file: lower-volume
source_book_pages:
  - 450
source_pdf_pages:
  - 80
title: Luogu P2568 GCD
chapter: 6
section: '6.7'
kind: external-oj
difficulty: 4
topics:
  - 歐拉函數
  - 質數篩
prerequisites:
  - 最大公因數與整除
statement: 給定 n，求 1<=x,y<=n 中 gcd(x,y) 為質數的有序數對數量。
constraints:
  - 1 <= n <= 10^7
input_format: 一個整數 n。
output_format: 輸出符合條件的有序數對數。
samples:
  - input: |
      4
    output: |
      4
    explanation: 符合者為 (2,2)、(2,4)、(4,2)、(3,3)。
core_knowledge:
  - 線性篩
  - 互質數對
judgment: 固定質數 p，令 x=pa、y=pb，需 gcd(a,b)=1 且 a,b<=n/p；該方形內有序互質對數為 2·sum(phi(1..m))-1。
hints:
  - 依 gcd 的質數值分類計數。
  - 同除該質數後，問題變成計算有序互質對。
  - 用線性篩同時計算質數與 phi 前綴和。
solution_outline: 固定質數 p，令 x=pa、y=pb，需 gcd(a,b)=1 且 a,b<=n/p；該方形內有序互質對數為 2·sum(phi(1..m))-1。
proof_or_invariant: 每對數的 gcd 唯一，分類不重不漏；方形 [1,m]^2 的互質有序對由對角兩側對稱，計數為 1+2·sum_{i=2}^m phi(i)。
complexity:
  time: O(n)
  space: O(n)
common_errors:
  - 漏算兩種順序
  - 結果使用 32 位元
cpp_skeleton: |
  // TODO：理解證明後，可嘗試自行重寫核心步驟。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;cin>>n;vector<int> phi(n+1),pr;vector<bool> comp(n+1);phi[1]=1;for(int i=2;i<=n;++i){if(!comp[i]){pr.push_back(i);phi[i]=i-1;}for(int p:pr){if((long long)i*p>n)break;comp[i*p]=true;if(i%p==0){phi[i*p]=phi[i]*p;break;}phi[i*p]=phi[i]*(p-1);}}vector<long long>s(n+1);for(int i=1;i<=n;++i)s[i]=s[i-1]+phi[i];long long ans=0;for(int p:pr)ans+=2*s[n/p]-1;cout<<ans<<"\n";}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;cin>>n;vector<int> phi(n+1),pr;vector<bool> comp(n+1);phi[1]=1;for(int i=2;i<=n;++i){if(!comp[i]){pr.push_back(i);phi[i]=i-1;}for(int p:pr){if((long long)i*p>n)break;comp[i*p]=true;if(i%p==0){phi[i*p]=phi[i]*p;break;}phi[i*p]=phi[i]*(p-1);}}vector<long long>s(n+1);for(int i=1;i<=n;++i)s[i]=s[i-1]+phi[i];long long ans=0;for(int p:pr)ans+=2*s[n/p]-1;cout<<ans<<"\n";}
external_url: https://www.luogu.com.cn/problem/P2568
external_platform: Luogu
external_problem_id: P2568
external_title: GCD
external_relation: original
review_status: verified
---

本題以可驗證的數論性質化簡後實作。
