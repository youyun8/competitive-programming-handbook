---
id: hdu-1599
volume: lower
source_file: lower-volume
original_label: HDU 1599
title: HDU 1599 find the mincost route：Floyd 求最小環
chapter: 10
section: '10.8'
kind: external-oj
difficulty: 3
topics: [Floyd-Warshall, 最小環, 無向圖]
prerequisites: [dijkstra]
core_knowledge: [環上最大編號點, 更新前距離, 原始邊矩陣]
judgment: 在用 k 更新 Floyd 前，dist[i][j] 只經過編號小於 k 的點，可與 i-k、k-j 組成簡單環。
statement: 多組正權無向圖，求至少含三個不同頂點的最小權重環；不存在則輸出指定字串。
constraints: ['n <= 100', 'm <= 1000', '邊權 <= 100']
input_format: 多組直到 EOF；每組 n、m，接著 m 條 a、b、c。
output_format: 輸出最小環權，或 It's impossible.。
samples:
  - input: |-
      3 3
      1 2 1
      2 3 2
      3 1 3
    output: '6'
    explanation: 唯一簡單環經過三點，總權重 1+2+3=6。
hints:
  - 保留 original 矩陣，不能讓 i-k、k-j 使用後來形成的路徑。
  - 在第 k 輪 Floyd 更新前枚舉 i<j<k，候選是 dist[i][j]+original[i][k]+original[k][j]。
  - 對每個環取編號最大的點 k，便會恰在第 k 輪被考慮。
solution_outline: 初始化原始邊與距離矩陣；每輪先枚舉以 k 為最大編號點的環，再執行標準 Floyd 更新。
proof_or_invariant: 任一簡單環有唯一最大編號點 k，移除 k 後剩下 i 到 j 且中繼編號皆小於 k 的路徑；更新前 dist 給出不更差的此類最短路，故會找到該環或更優環。所有候選本身都是合法環。
complexity: { time: 'O(n^3)', space: 'O(n^2)' }
common_errors: [先更新 Floyd 才計算第 k 輪環, 原始邊與最短距離共用後被覆寫, 沒有取重邊最小值]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){int n,m;while(cin>>n>>m){/* TODO：更新前枚舉最小環。 */}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){
      ios::sync_with_stdio(false);cin.tie(nullptr);
      int n,m;while(cin>>n>>m){
          const long long inf=LLONG_MAX/4;
          vector<vector<long long>> edge(static_cast<size_t>(n),vector<long long>(static_cast<size_t>(n),inf));
          for(int i=0;i<n;++i)edge[static_cast<size_t>(i)][static_cast<size_t>(i)]=0;
          for(int z=0;z<m;++z){int a,b;long long c;cin>>a>>b>>c;--a;--b;edge[static_cast<size_t>(a)][static_cast<size_t>(b)]=edge[static_cast<size_t>(b)][static_cast<size_t>(a)]=min(edge[static_cast<size_t>(a)][static_cast<size_t>(b)],c);}
          auto dist=edge;long long answer=inf;
          for(int k=0;k<n;++k){
              for(int i=0;i<k;++i)for(int j=i+1;j<k;++j)if(dist[static_cast<size_t>(i)][static_cast<size_t>(j)]<inf&&edge[static_cast<size_t>(i)][static_cast<size_t>(k)]<inf&&edge[static_cast<size_t>(k)][static_cast<size_t>(j)]<inf)
                  answer=min(answer,dist[static_cast<size_t>(i)][static_cast<size_t>(j)]+edge[static_cast<size_t>(i)][static_cast<size_t>(k)]+edge[static_cast<size_t>(k)][static_cast<size_t>(j)]);
              for(int i=0;i<n;++i)for(int j=0;j<n;++j)if(dist[static_cast<size_t>(i)][static_cast<size_t>(k)]<inf&&dist[static_cast<size_t>(k)][static_cast<size_t>(j)]<inf)
                  dist[static_cast<size_t>(i)][static_cast<size_t>(j)]=min(dist[static_cast<size_t>(i)][static_cast<size_t>(j)],dist[static_cast<size_t>(i)][static_cast<size_t>(k)]+dist[static_cast<size_t>(k)][static_cast<size_t>(j)]);
          }
          if(answer==inf)cout<<"It's impossible.\n";else cout<<answer<<'\n';
      }
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1599
external_platform: HDU
external_problem_id: '1599'
external_title: find the mincost route
external_relation: original
source_book_pages: [656]
source_pdf_pages: [286]
review_status: verified
---

Floyd 的「更新前」狀態提供了限制中繼點編號的精確語意。
