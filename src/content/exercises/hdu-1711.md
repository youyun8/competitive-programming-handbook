---
id: hdu-1711
volume: lower
source_file: lower-volume
title: HDU 1711 Number Sequence
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [kmp, integer-sequence, first-match]
prerequisites: [kmp, string]
statement: '在整數序列 text 中尋找 pattern 第一次連續出現的位置。'
constraints:
  - '1 <= N <= 10^6'
  - '1 <= M <= 10^4'
  - '元素為 32 位整數'
input_format: '第一行 T；每組 N M、N 個文本元素、M 個模式元素。'
output_format: '首次位置以 1-based 輸出；不存在為 -1。'
samples:
  - input: "2\n5 2\n1 2 3 2 3\n2 3\n3 2\n1 1 1\n1 2\n"
    output: "2\n-1"
    explanation: '第一組模式首次從位置 2 開始，第二組不存在。'
core_knowledge:
  - 'kmp'
  - 'integer-sequence'
  - 'first-match'
judgment: '首次位置以 1-based 輸出；不存在為 -1。'
hints:
  - 'KMP 不依賴字元型別，相等比較可直接套在整數。'
  - '先對模式序列建立前綴函數。'
  - '掃到完整模式立即輸出 i-m+2；掃完未命中輸出 -1。'
solution_outline: '在 vector<int> 上執行 KMP。'
proof_or_invariant: '與字串 KMP 同一不變量；首次達完整長度時文本掃描位置最小，因此起點也是最早。'
common_errors:
  - '輸出 0-based'
  - '把 N、M 順序讀反'
  - '找完仍繼續覆蓋成最後位置'
complexity:
  time: 'O(N+M)'
  space: 'O(M)'
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成核心演算法。*/return 0;}
cpp_solution: |
  #include <iostream>
  #include <vector>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int t=0;cin>>t;while(t--){int n=0,m=0;cin>>n>>m;vector<int>a(static_cast<size_t>(n)),p(static_cast<size_t>(m)),pi(static_cast<size_t>(m));for(int&x:a)cin>>x;for(int&x:p)cin>>x;for(int i=1;i<m;++i){int j=pi[static_cast<size_t>(i-1)];while(j>0&&p[static_cast<size_t>(i)]!=p[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j-1)];if(p[static_cast<size_t>(i)]==p[static_cast<size_t>(j)])++j;pi[static_cast<size_t>(i)]=j;}int j=0,ans=-1;for(int i=0;i<n;++i){while(j>0&&a[static_cast<size_t>(i)]!=p[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j-1)];if(a[static_cast<size_t>(i)]==p[static_cast<size_t>(j)])++j;if(j==m){ans=i-m+2;break;}}cout<<ans<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1711
external_platform: HDU
external_problem_id: '1711'
external_title: 'Number Sequence'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

以線性字串結構重用已知前後綴資訊，避免重新比較。
