---
id: hdu-1541
volume: upper
source_file: upper-volume
title: HDU 1541 Stars：星星層級統計
chapter: 4
section: '4.2'
kind: external-oj
difficulty: 4
topics: &id001
  - fenwick-tree
  - dominance-counting
prerequisites:
  - fenwick-tree
statement: 點依 y 再 x 非遞減輸入；一點的層級是其左下含邊界的先前點數。統計各層級點數。
constraints:
  - 1 <= N <= 15000
  - 0 <= x,y <= 32000
  - 輸入已依 y、x 排序
input_format: 多組資料；每組 N 後接 N 個 x y。
output_format: 輸出 level 0 到 N-1 的星星數。
samples:
  - input: |
      5
      1 1
      5 1
      7 1
      3 3
      5 5
    output: |
      1
      2
      1
      1
      0
    explanation: 依輸入順序用 x 前綴計數可得到各點層級。
core_knowledge: *id001
judgment: 掃描點並用 BIT 維護 x 座標頻率。
hints:
  - y 已排序，所以先前點自動滿足 y 不大於目前。
  - 只需統計先前 x<=目前 x 的點數。
  - BIT 查 x 前綴後再加入目前點。
solution_outline: 掃描點並用 BIT 維護 x 座標頻率。
proof_or_invariant: 排序條件與 BIT 前綴分別保證兩個座標偏序，故查得值正是層級。
common_errors:
  - 索引基準或閉區間端點處理錯誤
  - 更新資料結構後忘記同步原始狀態
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(N log C)
  space: O(N+C)
cpp_skeleton: |
  // TODO：先依三階段提示自行完成核心；以下框架可嚴格編譯。
  #include <bits/stdc++.h>
  using namespace std;
  class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}long long sum(int x)const{long long s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<long long>t;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){Fenwick bit(32002);vector<int>level(static_cast<size_t>(n));for(int i=0;i<n;++i){int x,y;cin>>x>>y;(void)y;int k=static_cast<int>(bit.sum(x+1));++level[static_cast<size_t>(k)];bit.add(x+1,1);}for(int x:level)cout<<x<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}long long sum(int x)const{long long s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<long long>t;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){Fenwick bit(32002);vector<int>level(static_cast<size_t>(n));for(int i=0;i<n;++i){int x,y;cin>>x>>y;(void)y;int k=static_cast<int>(bit.sum(x+1));++level[static_cast<size_t>(k)];bit.add(x+1,1);}for(int x:level)cout<<x<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1541
external_platform: HDU
external_problem_id: '1541'
external_title: HDU 1541 Stars
external_relation: original
source_book_pages:
  - 170
source_pdf_pages:
  - 188
review_status: verified
---

本卡片依外部題面與限制獨立整理。
