---
id: hdu-4553
volume: upper
source_file: upper-volume
title: HDU 4553 約會安排
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - dual-availability
  - lazy-segment-tree
  - priority-allocation
prerequisites:
  - segment-tree
statement: 時間軸支援 DS 低優先預約、NS 高優先預約（必要時覆蓋 DS）與 STUDY!! 區間清空，皆取最早連續時段。
constraints:
  - T 組
  - 時間長 T、操作數 Q <= 100000
input_format: DS x、NS x、STUDY!! l r。
output_format: 依題面固定字串輸出結果。
samples:
  - input: |
      1
      5 4
      DS 2
      NS 2
      NS 2
      STUDY!! 1 3
    output: |
      Case 1:
      1,let's fly
      3,don't put my gezi
      1,don't put my gezi
      I am the hope of chinese chengxuyuan!!
    explanation: 第三次 NS 可覆蓋起點一的 DS；最後清空前三格。
core_knowledge: *id001
judgment: 一棵線段樹維護兩組連續空段摘要與三態賦值。
hints:
  - 同時維護「完全空」與「沒有 NS」兩種可用連續段。
  - DS 只讓完全空摘要失效；NS 讓兩種都失效；STUDY 讓兩種恢復。
  - NS 先找完全空，沒有才在忽略 DS 的摘要找最早段並覆蓋。
solution_outline: 一棵線段樹維護兩組連續空段摘要與三態賦值。
proof_or_invariant: 兩摘要分別精確表示 DS、NS 的可選集合；每次以左優先定位並按狀態更新，等價於題目優先規則。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O(Q log T)
  space: O(T)
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;struct Runs{int pref=0,suff=0,best=0;};struct Node{Runs d,n;int lazy=-1;};static vector<Node>tr;static Runs merge_runs(const Runs&a,const Runs&b,int left_len,int right_len){Runs c;c.pref=a.pref+(a.pref==left_len?b.pref:0);c.suff=b.suff+(b.suff==right_len?a.suff:0);c.best=max({a.best,b.best,a.suff+b.pref});return c;}static void apply(int p,int l,int r,int state){int len=r-l+1;tr[static_cast<size_t>(p)].d=state==0?Runs{len,len,len}:Runs{};tr[static_cast<size_t>(p)].n=state==2?Runs{}:Runs{len,len,len};tr[static_cast<size_t>(p)].lazy=state;}static void push(int p,int l,int r){if(tr[static_cast<size_t>(p)].lazy<0||l==r)return;int m=(l+r)/2;apply(p*2,l,m,tr[static_cast<size_t>(p)].lazy);apply(p*2+1,m+1,r,tr[static_cast<size_t>(p)].lazy);tr[static_cast<size_t>(p)].lazy=-1;}static void pull(int p,int l,int r){int m=(l+r)/2;tr[static_cast<size_t>(p)].d=merge_runs(tr[static_cast<size_t>(p*2)].d,tr[static_cast<size_t>(p*2+1)].d,m-l+1,r-m);tr[static_cast<size_t>(p)].n=merge_runs(tr[static_cast<size_t>(p*2)].n,tr[static_cast<size_t>(p*2+1)].n,m-l+1,r-m);}static void update(int p,int l,int r,int ql,int qr,int state){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,l,r,state);return;}push(p,l,r);int m=(l+r)/2;update(p*2,l,m,ql,qr,state);update(p*2+1,m+1,r,ql,qr,state);pull(p,l,r);}static int locate(int p,int l,int r,int need,bool ns){if(l==r)return l;push(p,l,r);int m=(l+r)/2;const Runs&left=ns?tr[static_cast<size_t>(p*2)].n:tr[static_cast<size_t>(p*2)].d;const Runs&right=ns?tr[static_cast<size_t>(p*2+1)].n:tr[static_cast<size_t>(p*2+1)].d;if(left.best>=need)return locate(p*2,l,m,need,ns);if(left.suff+right.pref>=need)return m-left.suff+1;return locate(p*2+1,m+1,r,need,ns);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n,q;cin>>n>>q;tr.assign(static_cast<size_t>(4*n+4),Node{});apply(1,1,n,0);cout<<"Case "<<cs<<":\n";while(q--){string op;cin>>op;if(op=="DS"){int x;cin>>x;if(tr[1].d.best<x)cout<<"fly with yourself\n";else{int l=locate(1,1,n,x,false);update(1,1,n,l,l+x-1,1);cout<<l<<",let's fly\n";}}else if(op=="NS"){int x;cin>>x;bool use_n=tr[1].d.best<x;if((use_n?tr[1].n.best:tr[1].d.best)<x)cout<<"wait for me\n";else{int l=locate(1,1,n,x,use_n);update(1,1,n,l,l+x-1,2);cout<<l<<",don't put my gezi\n";}}else{int l,r;cin>>l>>r;update(1,1,n,l,r,0);cout<<"I am the hope of chinese chengxuyuan!!\n";}}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct Runs{int pref=0,suff=0,best=0;};struct Node{Runs d,n;int lazy=-1;};static vector<Node>tr;static Runs merge_runs(const Runs&a,const Runs&b,int left_len,int right_len){Runs c;c.pref=a.pref+(a.pref==left_len?b.pref:0);c.suff=b.suff+(b.suff==right_len?a.suff:0);c.best=max({a.best,b.best,a.suff+b.pref});return c;}static void apply(int p,int l,int r,int state){int len=r-l+1;tr[static_cast<size_t>(p)].d=state==0?Runs{len,len,len}:Runs{};tr[static_cast<size_t>(p)].n=state==2?Runs{}:Runs{len,len,len};tr[static_cast<size_t>(p)].lazy=state;}static void push(int p,int l,int r){if(tr[static_cast<size_t>(p)].lazy<0||l==r)return;int m=(l+r)/2;apply(p*2,l,m,tr[static_cast<size_t>(p)].lazy);apply(p*2+1,m+1,r,tr[static_cast<size_t>(p)].lazy);tr[static_cast<size_t>(p)].lazy=-1;}static void pull(int p,int l,int r){int m=(l+r)/2;tr[static_cast<size_t>(p)].d=merge_runs(tr[static_cast<size_t>(p*2)].d,tr[static_cast<size_t>(p*2+1)].d,m-l+1,r-m);tr[static_cast<size_t>(p)].n=merge_runs(tr[static_cast<size_t>(p*2)].n,tr[static_cast<size_t>(p*2+1)].n,m-l+1,r-m);}static void update(int p,int l,int r,int ql,int qr,int state){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,l,r,state);return;}push(p,l,r);int m=(l+r)/2;update(p*2,l,m,ql,qr,state);update(p*2+1,m+1,r,ql,qr,state);pull(p,l,r);}static int locate(int p,int l,int r,int need,bool ns){if(l==r)return l;push(p,l,r);int m=(l+r)/2;const Runs&left=ns?tr[static_cast<size_t>(p*2)].n:tr[static_cast<size_t>(p*2)].d;const Runs&right=ns?tr[static_cast<size_t>(p*2+1)].n:tr[static_cast<size_t>(p*2+1)].d;if(left.best>=need)return locate(p*2,l,m,need,ns);if(left.suff+right.pref>=need)return m-left.suff+1;return locate(p*2+1,m+1,r,need,ns);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n,q;cin>>n>>q;tr.assign(static_cast<size_t>(4*n+4),Node{});apply(1,1,n,0);cout<<"Case "<<cs<<":\n";while(q--){string op;cin>>op;if(op=="DS"){int x;cin>>x;if(tr[1].d.best<x)cout<<"fly with yourself\n";else{int l=locate(1,1,n,x,false);update(1,1,n,l,l+x-1,1);cout<<l<<",let's fly\n";}}else if(op=="NS"){int x;cin>>x;bool use_n=tr[1].d.best<x;if((use_n?tr[1].n.best:tr[1].d.best)<x)cout<<"wait for me\n";else{int l=locate(1,1,n,x,use_n);update(1,1,n,l,l+x-1,2);cout<<l<<",don't put my gezi\n";}}else{int l,r;cin>>l>>r;update(1,1,n,l,r,0);cout<<"I am the hope of chinese chengxuyuan!!\n";}}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4553
external_platform: HDU
external_problem_id: '4553'
external_title: HDU 4553 約會安排
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
