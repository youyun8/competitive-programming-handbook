---
id: hdu-2087
volume: lower
source_file: lower-volume
title: HDU 2087 剪花布條
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [kmp, non-overlapping-match, greedy]
prerequisites: [kmp, string]
statement: '給定花布 text 與飾條 pattern，求最多能剪出多少個互不重疊的 pattern。'
constraints:
  - '兩字串長度皆不超過 1000'
  - '字元為可見 ASCII'
  - 'text 單獨為 # 時結束'
input_format: '每組兩行 text、pattern；讀到 # 結束。'
output_format: '每組輸出最多不重疊出現數。'
samples:
  - input: "abcde\na3\naaaaaa\naa\n#\n"
    output: "0\n3"
    explanation: '第二組可依次剪出三段 aa，不能重用字元。'
core_knowledge:
  - 'kmp'
  - 'non-overlapping-match'
  - 'greedy'
judgment: '每組輸出最多不重疊出現數。'
hints:
  - '先用 KMP 找出下一個出現。'
  - '從左到右選最早結束的可用出現，為後續保留最多空間。'
  - '完整匹配後把 matched 清零，而非回到 border，便不會重疊。'
solution_outline: 'KMP 掃描文本；每次完整匹配答案加一並清空匹配長度。'
proof_or_invariant: '最早結束貪心對等長區間最優；KMP 不漏任何最早出現，清零恰禁止重用已剪區間。'
common_errors:
  - '匹配後沿 border 回退而計入重疊'
  - '把 # 當一般文本'
  - 'pattern 比 text 長時越界'
complexity:
  time: 'O(|text|+|pattern|)'
  space: 'O(|pattern|)'
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成核心演算法。*/return 0;}
cpp_solution: |
  #include <iostream>
  #include <string>
  #include <vector>
  using namespace std;
  static vector<int> prefix_of(const string& p){vector<int> pi(p.size());for(size_t i=1;i<p.size();++i){int j=pi[i-1];while(j>0&&p[i]!=p[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j-1)];if(p[i]==p[static_cast<size_t>(j)])++j;pi[i]=j;}return pi;}
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);string text,pattern;while(cin>>text&&text!="#"){cin>>pattern;auto pi=prefix_of(pattern);int j=0,answer=0;for(char c:text){while(j>0&&c!=pattern[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j-1)];if(c==pattern[static_cast<size_t>(j)])++j;if(j==static_cast<int>(pattern.size())){++answer;j=0;}}cout<<answer<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2087
external_platform: HDU
external_problem_id: '2087'
external_title: '剪花布條'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

以線性字串結構重用已知前後綴資訊，避免重新比較。
