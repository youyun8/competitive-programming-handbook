---
id: hdu-3062
volume: lower
source_file: lower-volume
original_label: HDU 3062
title: HDU 3062 Party：夫妻二選一
chapter: 10
section: '10.7'
kind: external-oj
difficulty: 3
topics: ['2-SAT', '強連通分量', '衝突限制']
prerequisites: ['directed-connectivity']
core_knowledge: ['二選一變數', '互斥子句', 'SCC 判矛盾']
judgment: 每對夫妻恰派一人；每組衝突中的兩人不能同時出席。
statement: |-
  有 n 對夫妻，每對恰好一人參加聚會。另給 m 組人員衝突，衝突的兩人不能同時參加。判斷是否存在選法。
  本站依可信存檔重新敘述；完整原文與限制以外部題目頁為準。
constraints:
  - 'n <= 1000'
  - '夫妻編號為 0..n-1'
  - '性別以 0、1 表示'
input_format: 多組資料直到 EOF。每組先給 n、m；接著 m 行為 a b c d，表示第 a 對的 c 與第 b 對的 d 互相衝突。
output_format: 每組有合法選法輸出 YES，否則輸出 NO。
samples:
  - input: |-
      2 1
      0 1 0 0
    output: 'YES'
    explanation: 例如讓第 0 對的 0 號與第 1 對的 1 號參加，唯一一組衝突不會同時出現。
hints:
  - 每對夫妻是一個布林變數，兩種取值就是派 0 號或 1 號出席。
  - 若文字 u 與 v 衝突，子句是「非 u 或非 v」，加入 `u -> 非 v`、`v -> 非 u`。
  - 建完蘊含圖後，只須檢查每個變數的兩種取值是否落在同一 SCC。
solution_outline: 把第 i 對派 c 號出席編成節點 2i+c。每組衝突加入兩條蘊含邊，Tarjan 求 SCC 後檢查 2i 與 2i+1。
proof_or_invariant: 衝突 u、v 不可同選等價於子句 (¬u∨¬v)，又等價於 u→¬v 與 v→¬u。所有限制都被等價放入蘊含圖；2-SAT 定理保證文字與其否定同 SCC 當且僅當公式矛盾。
complexity:
  time: 'O(n + m)'
  space: 'O(n + m)'
common_errors:
  - 把輸入的夫妻編號誤當成從 1 開始
  - 衝突邊連成 u 與 v 互相蘊含
  - 只從一個節點啟動 SCC 搜尋
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      int n, m;
      while (cin >> n >> m) {
          vector<vector<int>> graph(static_cast<size_t>(2 * n));
          for (int i = 0; i < m; ++i) {
              int a, b, c, d;
              cin >> a >> b >> c >> d;
              // TODO：u 與 v 不能同選，加入 u -> 非 v、v -> 非 u。
          }
          // TODO：以 SCC 判斷每對夫妻的兩個選項是否矛盾。
      }
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;

  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      int n, m;
      while (cin >> n >> m) {
          vector<vector<int>> graph(static_cast<size_t>(2 * n));
          for (int i = 0; i < m; ++i) {
              int a, b, c, d;
              cin >> a >> b >> c >> d;
              const int left = 2 * a + c;
              const int right = 2 * b + d;
              graph[static_cast<size_t>(left)].push_back(right ^ 1);
              graph[static_cast<size_t>(right)].push_back(left ^ 1);
          }
          vector<int> dfn(static_cast<size_t>(2 * n)), low(static_cast<size_t>(2 * n));
          vector<int> component(static_cast<size_t>(2 * n), -1), stack_nodes;
          vector<char> on_stack(static_cast<size_t>(2 * n));
          int timer = 0, component_count = 0;
          auto tarjan = [&](auto&& self, int node) -> void {
              dfn[static_cast<size_t>(node)] = low[static_cast<size_t>(node)] = ++timer;
              stack_nodes.push_back(node);
              on_stack[static_cast<size_t>(node)] = 1;
              for (const int next : graph[static_cast<size_t>(node)]) {
                  if (dfn[static_cast<size_t>(next)] == 0) {
                      self(self, next);
                      low[static_cast<size_t>(node)] =
                          min(low[static_cast<size_t>(node)], low[static_cast<size_t>(next)]);
                  } else if (on_stack[static_cast<size_t>(next)]) {
                      low[static_cast<size_t>(node)] =
                          min(low[static_cast<size_t>(node)], dfn[static_cast<size_t>(next)]);
                  }
              }
              if (low[static_cast<size_t>(node)] != dfn[static_cast<size_t>(node)]) { return; }
              while (true) {
                  const int top = stack_nodes.back();
                  stack_nodes.pop_back();
                  on_stack[static_cast<size_t>(top)] = 0;
                  component[static_cast<size_t>(top)] = component_count;
                  if (top == node) { break; }
              }
              ++component_count;
          };
          for (int node = 0; node < 2 * n; ++node) {
              if (dfn[static_cast<size_t>(node)] == 0) { tarjan(tarjan, node); }
          }
          bool possible = true;
          for (int i = 0; i < n; ++i) {
              if (component[static_cast<size_t>(2 * i)] ==
                  component[static_cast<size_t>(2 * i + 1)]) {
                  possible = false;
              }
          }
          cout << (possible ? "YES" : "NO") << '\n';
      }
      return 0;
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3062
external_platform: HDU
external_problem_id: '3062'
external_title: Party
external_relation: original
source_book_pages: [633]
source_pdf_pages: [263]
review_status: verified
---

「不能同時選」先寫成析取子句，邊的方向就不容易接反。
