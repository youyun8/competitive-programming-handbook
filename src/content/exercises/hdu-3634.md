---
id: hdu-3634
volume: upper
source_file: upper-volume
title: HDU 3634 City Planning
chapter: 2
section: '2.7'
kind: external-oj
difficulty: 4
topics: ['座標離散化', '矩形覆蓋', '貪心']
prerequisites: []
statement: |-
  給定最多 20 個帶單位價值的矩形，可任意安排覆蓋順序；求可見區域總價值最大值。
constraints:
  - 'T<3000；n<=20；座標<=10000；價值<=22。'
input_format: '依上列敘述順序輸入所有參數與操作。'
output_format: '依題意輸出每組答案。'
samples:
  - input: |
      1
      1
      0 0 2 3 4
    output: |
      Case 1: 24
    explanation: '依操作或定義直接計算可得此結果。'
core_knowledge: ['座標離散化', '矩形覆蓋', '貪心']
judgment: '座標或狀態範圍大，但實際事件有限，應用本節離散化、差分或狀態壓縮。'
hints:
  [
    '先找出真正會改變答案的事件或邊界。',
    '將大座標／大量區間轉成有限狀態後依序處理。',
    '維持狀態不變量，最後掃描所有候選並取最優。'
  ]
solution_outline: '依核心知識壓縮資料，再按事件順序更新並輸出答案。'
proof_or_invariant: '壓縮後每個原始事件仍有唯一對應，且事件間狀態不變；逐段計算涵蓋所有候選，因此所得值與原問題相同。'
common_errors: ['閉區間端點處理錯誤', '多組資料未清空', '計數或面積使用 int 溢位']
complexity:
  time: 'O(n log n) 或與壓縮後網格大小同階'
  space: 'O(n) 或與壓縮後網格大小同階'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：依提示完成。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct R{int a,b,c,d,v;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;for(int cs=1;cs<=T;++cs){int n;cin>>n;vector<R>r(n);vector<int>x,y;for(auto&z:r){cin>>z.a>>z.b>>z.c>>z.d>>z.v;x.push_back(z.a);x.push_back(z.c);y.push_back(z.b);y.push_back(z.d);}sort(x.begin(),x.end());x.erase(unique(x.begin(),x.end()),x.end());sort(y.begin(),y.end());y.erase(unique(y.begin(),y.end()),y.end());long long ans=0;for(size_t i=0;i+1<x.size();++i)for(size_t j=0;j+1<y.size();++j){int best=0;for(auto z:r)if(z.a<=x[i]&&x[i]<z.c&&z.b<=y[j]&&y[j]<z.d)best=max(best,z.v);ans+=1LL*best*(x[i+1]-x[i])*(y[j+1]-y[j]);}cout<<"Case "<<cs<<": "<<ans<<'\n';}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3634
external_platform: HDU
external_problem_id: '3634'
external_title: 'City Planning'
external_relation: original
source_book_pages: [78]
source_pdf_pages: [96]
review_status: verified
---

本題採獨立敘述與 C++17 解法。
