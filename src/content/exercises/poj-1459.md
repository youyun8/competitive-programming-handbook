---
id: poj-1459
volume: lower
source_file: lower-volume
original_label: OpenJ_Bailian 1459
title: OpenJudge 1459 Power Network：多源多匯最大流
chapter: 10
section: '10.10'
kind: external-oj
difficulty: 3
topics: ['最大流', '超級源匯', '特殊格式解析']
prerequisites: ['max-flow']
statement: '電網含發電站、消費者與轉送節點，線路及供需皆有容量；求全網最大可消耗電力。'
constraints: ['0<=n<=100', '線路數<=n^2', '線路容量<=1000', '供需容量<=10000', '多組至 EOF']
input_format: '每組 n、np、nc、m；m 個 `(u,v)z`，np 個 `(u)z`，nc 個 `(u)z`。'
output_format: '每組輸出最大總消耗量。'
samples:
  - input: |
      2 1 1 2 (0,1)20 (1,0)10 (0)15 (1)20
    output: |
      15
    explanation: '發電站最多供 15，線路與消費容量均不成更小瓶頸。 此例已以窮舉可行路徑、割或配置的獨立小資料程式對拍。'
core_knowledge: ['super source/sink', 'Dinic', 'punctuation parsing']
judgment: '多發電站與消費者可用超級源匯化成標準單源單匯最大流。'
hints:
  - '源向發電站連 pmax。'
  - '消費者向匯連 cmax，原輸電線照方向連容量。'
  - '用 char 逐一吃掉括號與逗號，不要假設資料分行。'
solution_outline: '逐 EOF 資料集建超級源匯殘量網路，Dinic 後輸出流量。'
proof_or_invariant: '任一可行電力配置滿足節點守恆與各容量，正是網路中的可行流；超源流量等於總發電，匯入量等於總消費，故最大流即最大消耗。'
common_errors: ['把輸電線當無向', 'scanf/cin 未處理標點', '把 np、nc、m 次序讀錯', '資料跨行時按行解析']
complexity: { time: 'O(n^2m)', space: 'O(n+m)' }
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;

  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依三階段提示完成建模與演算法。 */}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Dinic {
      struct Edge { int to; long long capacity; };
      vector<Edge> edges; vector<vector<int>> graph; vector<int> level; vector<size_t> current;
      explicit Dinic(int n):graph(static_cast<size_t>(n)),level(static_cast<size_t>(n)),current(static_cast<size_t>(n)){}
      int add_edge(int u,int v,long long c){int id=static_cast<int>(edges.size());graph[static_cast<size_t>(u)].push_back(id);edges.push_back({v,c});graph[static_cast<size_t>(v)].push_back(id+1);edges.push_back({u,0});return id;}
      bool bfs(int s,int t){fill(level.begin(),level.end(),-1);queue<int>q;q.push(s);level[static_cast<size_t>(s)]=0;while(!q.empty()){int u=q.front();q.pop();for(int id:graph[static_cast<size_t>(u)]){const auto&e=edges[static_cast<size_t>(id)];if(e.capacity>0&&level[static_cast<size_t>(e.to)]<0){level[static_cast<size_t>(e.to)]=level[static_cast<size_t>(u)]+1;q.push(e.to);}}}return level[static_cast<size_t>(t)]>=0;}
      long long dfs(int u,int t,long long limit){if(u==t)return limit;for(size_t&i=current[static_cast<size_t>(u)];i<graph[static_cast<size_t>(u)].size();++i){int id=graph[static_cast<size_t>(u)][i];auto&e=edges[static_cast<size_t>(id)];if(e.capacity<=0||level[static_cast<size_t>(e.to)]!=level[static_cast<size_t>(u)]+1)continue;long long sent=dfs(e.to,t,min(limit,e.capacity));if(sent){e.capacity-=sent;edges[static_cast<size_t>(id^1)].capacity+=sent;return sent;}}return 0;}
      long long max_flow(int s,int t){long long result=0;while(bfs(s,t)){fill(current.begin(),current.end(),0);while(long long sent=dfs(s,t,LLONG_MAX/4))result+=sent;}return result;}
  };
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,producer_count,consumer_count,line_count;while(cin>>n>>producer_count>>consumer_count>>line_count){int source=n,sink=n+1;Dinic flow(n+2);char left,comma,right;for(int i=0,u,v,c;i<line_count;++i){cin>>left>>u>>comma>>v>>right>>c;flow.add_edge(u,v,c);}for(int i=0,u,c;i<producer_count;++i){cin>>left>>u>>right>>c;flow.add_edge(source,u,c);}for(int i=0,u,c;i<consumer_count;++i){cin>>left>>u>>right>>c;flow.add_edge(u,sink,c);}cout<<flow.max_flow(source,sink)<<'\n';}}
external_url: http://bailian.openjudge.cn/practice/1459/
external_platform: OpenJ_Bailian
external_problem_id: '1459'
external_title: 'Power Network'
external_relation: original
source_book_pages: [675]
source_pdf_pages: [305]
review_status: verified
---

本卡片依官方題面或可信原賽事存檔獨立整理，未以 OCR 猜測題意。
