---
id: hdu-5124
volume: upper
source_file: upper-volume
title: HDU 5124 lines
chapter: 2
section: '2.7'
kind: external-oj
difficulty: 4
topics: ['掃描線', '事件排序']
prerequisites: []
statement: |-
  給定 n 個閉區間，求某一點最多可同時被多少區間覆蓋。
constraints:
  - 'T<=100；n<=100000；端點<=10^9。'
input_format: '依上列敘述順序輸入所有參數與操作。'
output_format: '依題意輸出每組答案。'
samples:
  - input: |
      1
      3
      1 2
      2 4
      3 5
    output: |
      2
    explanation: '依操作或定義直接計算可得此結果。'
core_knowledge: ['掃描線', '事件排序']
judgment: '座標或狀態範圍大，但實際事件有限，應用本節離散化、差分或狀態壓縮。'
hints:
  [
    '先找出真正會改變答案的事件或邊界。',
    '將大座標／大量區間轉成有限狀態後依序處理。',
    '維持狀態不變量，最後掃描所有候選並取最優。'
  ]
solution_outline: '依核心知識壓縮資料，再按事件順序更新並輸出答案。'
proof_or_invariant: '壓縮後每個原始事件仍有唯一對應，且事件間狀態不變；逐段計算涵蓋所有候選，因此所得值與原問題相同。'
common_errors: ['閉區間端點處理錯誤', '多組資料未清空', '計數或面積使用 int 溢位']
complexity:
  time: 'O(n log n) 或與壓縮後網格大小同階'
  space: 'O(n) 或與壓縮後網格大小同階'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：依提示完成。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int n;cin>>n;vector<pair<long long,int>>e;e.reserve(2*n);while(n--){long long l,r;cin>>l>>r;e.push_back({l,1});e.push_back({r+1,-1});}sort(e.begin(),e.end());int cur=0,ans=0;for(size_t i=0;i<e.size();){size_t j=i;int d=0;while(j<e.size()&&e[j].first==e[i].first)d+=e[j++].second;cur+=d;ans=max(ans,cur);i=j;}cout<<ans<<'\n';}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5124
external_platform: HDU
external_problem_id: '5124'
external_title: 'lines'
external_relation: original
source_book_pages: [78]
source_pdf_pages: [96]
review_status: verified
---

本題採獨立敘述與 C++17 解法。
