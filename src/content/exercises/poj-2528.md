---
id: poj-2528
volume: upper
source_file: upper-volume
title: OpenJudge 百練 2528 Mayor’s posters
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - coordinate-compression
  - lazy-segment-tree
  - range-assignment
prerequisites:
  - segment-tree
statement: 海報依序覆蓋整數牆段，求最後仍至少部分可見的海報數。
constraints:
  - 測資組數 c
  - n <= 10000
  - 座標 <= 10000000
input_format: 每組 n 與依張貼順序的 n 個 l r。
output_format: 每組輸出可見海報數。
samples:
  - input: |
      1
      5
      1 4
      2 6
      8 10
      3 4
      7 10
    output: |
      4
    explanation: 最終有四張海報仍保留至少一段。
core_knowledge: *id001
judgment: 保留空隙的離散化加區間覆蓋線段樹。
hints:
  - 只需保留端點與端點間是否存在整數段。
  - 相鄰原座標有空隙時插入一個代表點，避免壓縮後錯接。
  - 依序整段賦海報編號，最後收集葉面顏色。
solution_outline: 保留空隙的離散化加區間覆蓋線段樹。
proof_or_invariant: 代表點保持每段覆蓋關係；後貼賦值精確模擬遮蓋，終態出現的編號恰為可見海報。
common_errors:
  - 懶標記合成順序錯誤
  - 閉區間端點或 0/1 起始索引混淆
  - 合併節點摘要時漏掉跨左右子樹的候選
complexity:
  time: O(n log n)
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成資料結構。
  #include <bits/stdc++.h>
  using namespace std;static vector<int>tr,lazy_tag;static void push(int p){if(lazy_tag[static_cast<size_t>(p)]==0)return;for(int c:{p*2,p*2+1}){tr[static_cast<size_t>(c)]=lazy_tag[static_cast<size_t>(p)];lazy_tag[static_cast<size_t>(c)]=lazy_tag[static_cast<size_t>(p)];}lazy_tag[static_cast<size_t>(p)]=0;}static void paint(int p,int l,int r,int ql,int qr,int color){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){tr[static_cast<size_t>(p)]=color;lazy_tag[static_cast<size_t>(p)]=color;return;}push(p);int m=(l+r)/2;paint(p*2,l,m,ql,qr,color);paint(p*2+1,m+1,r,ql,qr,color);tr[static_cast<size_t>(p)]=tr[static_cast<size_t>(p*2)]==tr[static_cast<size_t>(p*2+1)]?tr[static_cast<size_t>(p*2)]:-1;}static void collect(int p,int l,int r,vector<char>&seen){if(tr[static_cast<size_t>(p)]>0){seen[static_cast<size_t>(tr[static_cast<size_t>(p)])]=1;return;}if(l==r)return;push(p);int m=(l+r)/2;collect(p*2,l,m,seen);collect(p*2+1,m+1,r,seen);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n;cin>>n;vector<pair<int,int>>seg(static_cast<size_t>(n));vector<int>endpoints;for(auto&[l,r]:seg){cin>>l>>r;endpoints.push_back(l);endpoints.push_back(r);}sort(endpoints.begin(),endpoints.end());endpoints.erase(unique(endpoints.begin(),endpoints.end()),endpoints.end());vector<int>coord;for(int x:endpoints){if(!coord.empty()&&x>coord.back()+1)coord.push_back(coord.back()+1);coord.push_back(x);}int size=static_cast<int>(coord.size());tr.assign(static_cast<size_t>(4*size+4),0);lazy_tag=tr;for(int i=0;i<n;++i){int l=static_cast<int>(lower_bound(coord.begin(),coord.end(),seg[static_cast<size_t>(i)].first)-coord.begin());int r=static_cast<int>(lower_bound(coord.begin(),coord.end(),seg[static_cast<size_t>(i)].second)-coord.begin());paint(1,0,size-1,l,r,i+1);}vector<char>seen(static_cast<size_t>(n+1));collect(1,0,size-1,seen);cout<<count(seen.begin(),seen.end(),static_cast<char>(1))<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;static vector<int>tr,lazy_tag;static void push(int p){if(lazy_tag[static_cast<size_t>(p)]==0)return;for(int c:{p*2,p*2+1}){tr[static_cast<size_t>(c)]=lazy_tag[static_cast<size_t>(p)];lazy_tag[static_cast<size_t>(c)]=lazy_tag[static_cast<size_t>(p)];}lazy_tag[static_cast<size_t>(p)]=0;}static void paint(int p,int l,int r,int ql,int qr,int color){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){tr[static_cast<size_t>(p)]=color;lazy_tag[static_cast<size_t>(p)]=color;return;}push(p);int m=(l+r)/2;paint(p*2,l,m,ql,qr,color);paint(p*2+1,m+1,r,ql,qr,color);tr[static_cast<size_t>(p)]=tr[static_cast<size_t>(p*2)]==tr[static_cast<size_t>(p*2+1)]?tr[static_cast<size_t>(p*2)]:-1;}static void collect(int p,int l,int r,vector<char>&seen){if(tr[static_cast<size_t>(p)]>0){seen[static_cast<size_t>(tr[static_cast<size_t>(p)])]=1;return;}if(l==r)return;push(p);int m=(l+r)/2;collect(p*2,l,m,seen);collect(p*2+1,m+1,r,seen);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n;cin>>n;vector<pair<int,int>>seg(static_cast<size_t>(n));vector<int>endpoints;for(auto&[l,r]:seg){cin>>l>>r;endpoints.push_back(l);endpoints.push_back(r);}sort(endpoints.begin(),endpoints.end());endpoints.erase(unique(endpoints.begin(),endpoints.end()),endpoints.end());vector<int>coord;for(int x:endpoints){if(!coord.empty()&&x>coord.back()+1)coord.push_back(coord.back()+1);coord.push_back(x);}int size=static_cast<int>(coord.size());tr.assign(static_cast<size_t>(4*size+4),0);lazy_tag=tr;for(int i=0;i<n;++i){int l=static_cast<int>(lower_bound(coord.begin(),coord.end(),seg[static_cast<size_t>(i)].first)-coord.begin());int r=static_cast<int>(lower_bound(coord.begin(),coord.end(),seg[static_cast<size_t>(i)].second)-coord.begin());paint(1,0,size-1,l,r,i+1);}vector<char>seen(static_cast<size_t>(n+1));collect(1,0,size-1,seen);cout<<count(seen.begin(),seen.end(),static_cast<char>(1))<<'\n';}}
external_url: http://bailian.openjudge.cn/practice/2528/
external_platform: OpenJ_Bailian
external_problem_id: '2528'
external_title: OpenJudge 百練 2528 Mayor’s posters
external_relation: original
source_book_pages:
  - 182
  - 192
source_pdf_pages:
  - 200
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
