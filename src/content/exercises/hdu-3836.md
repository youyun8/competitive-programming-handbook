---
id: hdu-3836
volume: lower
source_file: lower-volume
original_label: 'HDU 3836'
title: 'HDU 3836 Equivalent Sets'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 3
topics: [strongly-connected-components, condensation]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  已知若干集合的子集關係，每一步可再證明一條 X⊆Y；求最少新增幾步使所有集合兩兩等價。
constraints: [n <= 20000, m <= 50000, 多組資料直到 EOF]
input_format: >-
  每組 n m，後接 m 行 X Y。
output_format: >-
  每組輸出最少新增關係數。
samples:
  - input: |
      4 0
      3 2
      1 2
      1 3
    output: |
      4
      2
    explanation: >-
      空關係的四點需形成一個有向環；第二組需補兩條關係。
core_knowledge: [SCC 縮點, 強連通化]
judgment: >-
  若已只有一個 SCC 輸出 0；否則答案為源分量數與匯分量數較大者。
hints:
  - 等價代表雙向可達。
  - 先縮掉已等價的集合。
  - 將 DAG 匯點循環接到源點。
solution_outline: >-
  Tarjan 縮點並統計 DAG 入出度零分量。
proof_or_invariant: >-
  每個源缺至少一條入邊、每個匯缺至少一條出邊，故下界為兩者最大值；循環配對匯與源可達成此界。
complexity: { time: 'O(n + m)', space: 'O(n + m)' }
common_errors: [已強連通時輸出 1, 混淆原圖與縮點度數, 只取兩者較小值]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Scc{int node_count,t=0,c=0;vector<vector<int>>g;vector<int>d,l,st,id;vector<char>on;explicit Scc(int count):node_count(count),g(static_cast<size_t>(count)),d(static_cast<size_t>(count)),l(static_cast<size_t>(count)),id(static_cast<size_t>(count)),on(static_cast<size_t>(count)){}void dfs(int u){d[u]=l[u]=++t;st.push_back(u);on[u]=1;for(int v:g[u])if(!d[v])dfs(v),l[u]=min(l[u],l[v]);else if(on[v])l[u]=min(l[u],d[v]);if(d[u]==l[u]){while(true){int v=st.back();st.pop_back();on[v]=0;id[v]=c;if(v==u)break;}++c;}}void run(){for(int i=0;i<node_count;++i)if(!d[i])dfs(i);}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){Scc s(n);vector<pair<int,int>>e;while(m--){int u,v;cin>>u>>v;--u;--v;s.g[u].push_back(v);e.push_back({u,v});}s.run();if(s.c==1){cout<<"0\n";continue;}vector<int>in(s.c),out(s.c);for(auto [u,v]:e)if(s.id[u]!=s.id[v])in[s.id[v]]=out[s.id[u]]=1;int a=0,b=0;for(int i=0;i<s.c;++i)a+=!in[i],b+=!out[i];cout<<max(a,b)<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3836
external_platform: HDU
external_problem_id: '3836'
external_title: 'Equivalent Sets'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
