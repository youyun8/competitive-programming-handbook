---
id: hdu-3072
volume: lower
source_file: lower-volume
original_label: 'HDU 3072'
title: 'HDU 3072 Intelligence System'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 3
topics: [strongly-connected-components, minimum-incoming-edge]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  情報由編號 0 出發沿帶費用有向邊傳遞；同一互通分支內傳遞免費。求使所有人收到情報的最低總費用。
constraints: [1 <= n <= 50000, 0 <= m <= 100000, 1 <= cost <= 100000]
input_format: >-
  多組至 EOF；每組 n m，後接 m 行 x y cost。
output_format: >-
  每組輸出最低費用。
samples:
  - input: |
      3 2
      0 1 100
      1 2 50
    output: |
      150
    explanation: >-
      兩個非根分量各選最低的進入邊。
core_knowledge: [SCC 縮點, DAG 最小進邊]
judgment: >-
  保證 0 能通知全圖；根分量不需進邊，其餘每分量選最便宜跨分量入邊。
hints:
  - 先把免費互通關係縮成 SCC。
  - 縮點後沒有環。
  - 每個非根分量只需支付一條最便宜入邊。
solution_outline: >-
  Tarjan 縮點，掃描原邊更新各目標分量的最小跨分量邊費，排除 0 所在分量後加總。
proof_or_invariant: >-
  每個非根 DAG 分量要首次收到情報必用至少一條入邊；選各自最低入邊後，依拓撲序可逐分量收到情報，選擇互不衝突。
complexity: { time: 'O(n + m)', space: 'O(n + m)' }
common_errors: [把分量內邊計費, 漏排除根分量, 同一分量只看第一條入邊]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Scc{int node_count,t=0,c=0;vector<vector<int>>g;vector<int>d,l,st,id;vector<char>on;explicit Scc(int count):node_count(count),g(static_cast<size_t>(count)),d(static_cast<size_t>(count)),l(static_cast<size_t>(count)),id(static_cast<size_t>(count)),on(static_cast<size_t>(count)){}void dfs(int u){d[u]=l[u]=++t;st.push_back(u);on[u]=1;for(int v:g[u])if(!d[v])dfs(v),l[u]=min(l[u],l[v]);else if(on[v])l[u]=min(l[u],d[v]);if(d[u]==l[u]){while(true){int v=st.back();st.pop_back();on[v]=0;id[v]=c;if(v==u)break;}++c;}}void run(){for(int i=0;i<node_count;++i)if(!d[i])dfs(i);}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){Scc s(n);vector<tuple<int,int,int>>e;while(m--){int u,v,w;cin>>u>>v>>w;s.g[u].push_back(v);e.push_back({u,v,w});}s.run();vector<int>mn(s.c,INT_MAX);for(auto [u,v,w]:e)if(s.id[u]!=s.id[v])mn[s.id[v]]=min(mn[s.id[v]],w);long long ans=0;for(int i=0;i<s.c;++i)if(i!=s.id[0])ans+=mn[i];cout<<ans<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3072
external_platform: HDU
external_problem_id: '3072'
external_title: 'Intelligence System'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
