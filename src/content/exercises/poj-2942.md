---
id: poj-2942
volume: lower
source_file: lower-volume
original_label: 'POJ 2942'
title: 'POJ 2942 Knights of the Round Table'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 5
topics: [vertex-biconnected-components, bipartite-graph, complement-graph]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  n 位騎士中某些人互相憎恨。圓桌會議至少三人且相鄰者不得憎恨；求不可能參加任何一次合法會議的人數。
constraints: [1 <= n <= 1000, 憎恨對數 <= 1000000, 多組以 0 0 結束]
input_format: >-
  每組 n m，後接 m 對互相憎恨的騎士。
output_format: >-
  每組輸出無法參與任何合法奇數圓桌的人數。
samples:
  - input: |
      3 0
      0 0
    output: |
      0
    explanation: >-
      三人互不憎恨，三人即可圍成合法圓桌。
core_knowledge: [補圖, 點雙連通分量, 奇環判定]
judgment: >-
  騎士可參會等價於補圖中位於某個奇環；一個點雙分量非二分圖時，其所有頂點都位於奇環。
hints:
  - 以互不憎恨關係建補圖。
  - 用邊堆疊分解點雙分量。
  - 對每個 block 單獨二分染色。
solution_outline: >-
  建補圖後 Tarjan 枚舉點雙分量；若某分量不可二分染色，標記其中全部頂點，答案為未標記數。
proof_or_invariant: >-
  奇數座位圓桌正對應補圖奇環。每個環完全落在某點雙分量；非二分圖含奇環，而點雙分量中任一頂點可藉兩條內點不交路接入該奇環形成包含它的奇環。
complexity: { time: 'O(n^2)', space: 'O(n^2)' }
common_errors: [在憎恨圖找環, 只標記染色衝突端點, 跨點雙分量沿用顏色]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m&&(n||m)){vector<vector<char>>hate(n,vector<char>(n));while(m--){int u,v;cin>>u>>v;--u;--v;hate[u][v]=hate[v][u]=1;}vector<pair<int,int>>edges;vector<vector<pair<int,int>>>g(n);for(int u=0;u<n;++u)for(int v=u+1;v<n;++v)if(!hate[u][v]){int id=static_cast<int>(edges.size());edges.push_back({u,v});g[u].push_back({v,id});g[v].push_back({u,id});}vector<int>d(n),low(n),stack_edges;vector<char>bad(n);int timer=0;auto check=[&](const set<int>&vertices){vector<int>color(n,-1);bool bipartite=true;for(int start:vertices)if(color[start]<0){queue<int>q;q.push(start);color[start]=0;while(!q.empty()){int u=q.front();q.pop();for(auto [v,id]:g[u])if(vertices.count(v)){if(color[v]<0)color[v]=color[u]^1,q.push(v);else if(color[v]==color[u])bipartite=false;}}}if(!bipartite)for(int x:vertices)bad[x]=1;};auto dfs=[&](auto&&self,int u,int pe)->void{d[u]=low[u]=++timer;for(auto [v,id]:g[u])if(id!=pe){if(!d[v]){stack_edges.push_back(id);self(self,v,id);low[u]=min(low[u],low[v]);if(low[v]>=d[u]){set<int>vertices;int last;do{last=stack_edges.back();stack_edges.pop_back();vertices.insert(edges[last].first);vertices.insert(edges[last].second);}while(last!=id);check(vertices);}}else if(d[v]<d[u])stack_edges.push_back(id),low[u]=min(low[u],d[v]);}};for(int i=0;i<n;++i)if(!d[i])dfs(dfs,i,-1);cout<<n-static_cast<int>(count(bad.begin(),bad.end(),1))<<'\n';}}
external_url: http://poj.org/problem?id=2942
external_platform: POJ
external_problem_id: '2942'
external_title: 'Knights of the Round Table'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
