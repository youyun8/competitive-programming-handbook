---
id: hdu-4080
volume: lower
source_file: lower-volume
title: HDU 4080 Stammering Aliens
chapter: 9
section: '9.1'
kind: external-oj
difficulty: 5
topics: [suffix-array, lcp, binary-search]
prerequisites: [suffix-array, longest-common-prefix]
statement: '求字串中至少出現 m 次（可重疊）的最長子串；同長答案選擇最右出現起點。'
constraints:
  - 'm >= 1'
  - 'm <= |s| <= 40000'
  - s 只含小寫英文字母
  - m=0 為終止標記
input_format: '每組先一行 m，再一行字串；讀到 0 結束。'
output_format: '無正長答案輸出 none；否則輸出最大長度與 0-based 最右起點。'
samples:
  - input: "3\nbaaaababababbababbab\n11\nbaaaababababbababbab\n3\ncccccc\n0\n"
    output: "5 12\nnone\n4 2\n"
    explanation: '第一組 babab 長 5 且最右從 12 開始；第二組不存在；第三組 cccc 最右從 2 開始。'
core_knowledge: [後綴陣列相鄰性, LCP 連續群組, 答案長度單調性]
judgment: '起點使用 0-based；出現可重疊，同長時必須選最右位置。'
hints:
  - '長度 L 子串至少出現 m 次，等價於 SA 中存在 m 個連續後綴，其相鄰 LCP 都至少 L。'
  - '固定 L 掃 LCP 分組，記每組後綴數與最大起點；組數達 m 即可行。'
  - '可行性對 L 單調，二分最大 L；最後再次檢查取得所有可行組中的最右起點。'
solution_outline: '倍增排序建 SA、Kasai 求 LCP，二分長度並掃描 LCP 群組。'
proof_or_invariant: '具有共同長 L 前綴的後綴在 SA 中連續，且整組相鄰 LCP 至少 L；反之此條件保證共同前綴。故檢查充要，二分取得最長，群組最大起點處理 tie。'
common_errors:
  - m=1 時沒有輸出整串與位置 0
  - 只保留第一個可行群組而非最右起點
  - 把起點輸出成 1-based
complexity:
  time: O(n log^2 n)
  space: O(n)
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：建 SA/LCP 並二分答案。*/return 0;}
cpp_solution: |
  #include <algorithm>
  #include <iostream>
  #include <numeric>
  #include <string>
  #include <vector>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int need=0;while(cin>>need&&need!=0){string s;cin>>s;int n=static_cast<int>(s.size());if(need==1){cout<<n<<" 0\n";continue;}vector<int>sa(static_cast<size_t>(n)),rank(static_cast<size_t>(n)),next_rank(static_cast<size_t>(n));iota(sa.begin(),sa.end(),0);for(int i=0;i<n;++i)rank[static_cast<size_t>(i)]=static_cast<unsigned char>(s[static_cast<size_t>(i)]);for(int width=1;width<n;width*=2){sort(sa.begin(),sa.end(),[&](int a,int b){if(rank[static_cast<size_t>(a)]!=rank[static_cast<size_t>(b)])return rank[static_cast<size_t>(a)]<rank[static_cast<size_t>(b)];int ar=a+width<n?rank[static_cast<size_t>(a+width)]:-1,br=b+width<n?rank[static_cast<size_t>(b+width)]:-1;return ar<br;});next_rank[static_cast<size_t>(sa[0])]=0;for(int i=1;i<n;++i){int a=sa[static_cast<size_t>(i-1)],b=sa[static_cast<size_t>(i)];bool different=rank[static_cast<size_t>(a)]!=rank[static_cast<size_t>(b)]||(a+width<n?rank[static_cast<size_t>(a+width)]:-1)!=(b+width<n?rank[static_cast<size_t>(b+width)]:-1);next_rank[static_cast<size_t>(b)]=next_rank[static_cast<size_t>(a)]+(different?1:0);}rank=next_rank;if(rank[static_cast<size_t>(sa.back())]==n-1)break;}vector<int>where(static_cast<size_t>(n));for(int i=0;i<n;++i)where[static_cast<size_t>(sa[static_cast<size_t>(i)])]=i;vector<int>lcp(static_cast<size_t>(n));for(int i=0,k=0;i<n;++i){int r=where[static_cast<size_t>(i)];if(r==0)continue;int j=sa[static_cast<size_t>(r-1)];while(i+k<n&&j+k<n&&s[static_cast<size_t>(i+k)]==s[static_cast<size_t>(j+k)])++k;lcp[static_cast<size_t>(r)]=k;if(k>0)--k;}auto check=[&](int length){int count=1,max_position=sa[0],answer=-1;for(int i=1;i<n;++i){if(lcp[static_cast<size_t>(i)]>=length){++count;max_position=max(max_position,sa[static_cast<size_t>(i)]);}else{count=1;max_position=sa[static_cast<size_t>(i)];}if(count>=need)answer=max(answer,max_position);}return answer;};int low=1,high=n,best=0;while(low<=high){int mid=(low+high)/2;if(check(mid)>=0){best=mid;low=mid+1;}else high=mid-1;}if(best==0)cout<<"none\n";else cout<<best<<' '<<check(best)<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4080
external_platform: HDU
external_problem_id: '4080'
external_title: Stammering Aliens
external_relation: original
source_book_pages: [554, 574]
source_pdf_pages: [184, 204]
review_status: verified
---

後綴陣列把所有相同前綴聚成連續群組，LCP 則提供群組可行長度。
