---
id: poj-2886
volume: upper
source_file: upper-volume
title: POJ 2886 Who Gets the Most Candies?
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - fenwick-order-statistics
  - josephus
  - divisor-count
prerequisites:
  - segment-tree
statement: 帶有正負步數卡片的約瑟夫淘汰；第 p 位淘汰者得 p 的約數個數顆糖，求最幸運小孩。
constraints:
  - N <= 500000
  - 卡片非零且絕對值 <= 10^8
  - 多組至 EOF
input_format: 每組 N K，接著姓名與卡片值。
output_format: 輸出獲勝姓名與糖果數。
samples:
  - input: |
      3 1
      A 1
      B 1
      C 1
    output: |
      B 2
    explanation: 第二位淘汰者得到約數數二，為全程最高。
core_knowledge: *id001
judgment: 預處理約數數，只模擬到最佳輪次。
hints:
  - 先找 1..N 中約數數首次達到最大值的淘汰輪次。
  - BIT 的一代表仍在圈內，支援刪除與第 k 人。
  - 正負卡片換算到刪除後圈中的 1 起秩。
solution_outline: 預處理約數數，只模擬到最佳輪次。
proof_or_invariant: 糖果只由淘汰輪次決定，首次最大者唯一決定需找的人；BIT 模擬保持每輪存活秩。
common_errors:
  - 同座標事件順序或開閉邊界處理錯誤
  - 區間為空時仍遞迴更新
  - 合併時忘記保留跨左右區間候選
complexity:
  time: O(M log M+N log N)
  space: O(M)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;struct F{vector<int>t;explicit F(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(;x<static_cast<int>(t.size());x+=x&-x)t[static_cast<size_t>(x)]+=v;}int sum(int x)const{int s=0;for(;x>0;x-=x&-x)s+=t[static_cast<size_t>(x)];return s;}int kth(int k)const{int x=0,step=1;while((step<<1)<static_cast<int>(t.size()))step<<=1;for(;step;step>>=1){int y=x+step;if(y<static_cast<int>(t.size())&&t[static_cast<size_t>(y)]<k){x=y;k-=t[static_cast<size_t>(y)];}}return x+1;}};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);const int M=500000;vector<int>divs(static_cast<size_t>(M+1));for(int i=1;i<=M;++i)for(int j=i;j<=M;j+=i)++divs[static_cast<size_t>(j)];int n,k;while(cin>>n>>k){vector<string>name(static_cast<size_t>(n+1));vector<long long>cardv(static_cast<size_t>(n+1));F bit(n);for(int i=1;i<=n;++i){cin>>name[static_cast<size_t>(i)]>>cardv[static_cast<size_t>(i)];bit.add(i,1);}int step_best=1,candy=divs[1];for(int i=2;i<=n;++i)if(divs[static_cast<size_t>(i)]>candy){candy=divs[static_cast<size_t>(i)];step_best=i;}int rank=k,id=0;for(int turn=1;turn<=step_best;++turn){id=bit.kth(rank);long long move=cardv[static_cast<size_t>(id)];bit.add(id,-1);int rem=n-turn;if(turn==step_best)break;int prefix=bit.sum(id);long long zero=static_cast<long long>(prefix)+move-(move>0?1:0);zero%=rem;if(zero<0)zero+=rem;rank=static_cast<int>(zero)+1;}cout<<name[static_cast<size_t>(id)]<<' '<<candy<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct F{vector<int>t;explicit F(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(;x<static_cast<int>(t.size());x+=x&-x)t[static_cast<size_t>(x)]+=v;}int sum(int x)const{int s=0;for(;x>0;x-=x&-x)s+=t[static_cast<size_t>(x)];return s;}int kth(int k)const{int x=0,step=1;while((step<<1)<static_cast<int>(t.size()))step<<=1;for(;step;step>>=1){int y=x+step;if(y<static_cast<int>(t.size())&&t[static_cast<size_t>(y)]<k){x=y;k-=t[static_cast<size_t>(y)];}}return x+1;}};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);const int M=500000;vector<int>divs(static_cast<size_t>(M+1));for(int i=1;i<=M;++i)for(int j=i;j<=M;j+=i)++divs[static_cast<size_t>(j)];int n,k;while(cin>>n>>k){vector<string>name(static_cast<size_t>(n+1));vector<long long>cardv(static_cast<size_t>(n+1));F bit(n);for(int i=1;i<=n;++i){cin>>name[static_cast<size_t>(i)]>>cardv[static_cast<size_t>(i)];bit.add(i,1);}int step_best=1,candy=divs[1];for(int i=2;i<=n;++i)if(divs[static_cast<size_t>(i)]>candy){candy=divs[static_cast<size_t>(i)];step_best=i;}int rank=k,id=0;for(int turn=1;turn<=step_best;++turn){id=bit.kth(rank);long long move=cardv[static_cast<size_t>(id)];bit.add(id,-1);int rem=n-turn;if(turn==step_best)break;int prefix=bit.sum(id);long long zero=static_cast<long long>(prefix)+move-(move>0?1:0);zero%=rem;if(zero<0)zero+=rem;rank=static_cast<int>(zero)+1;}cout<<name[static_cast<size_t>(id)]<<' '<<candy<<'\n';}}
external_url: http://poj.org/problem?id=2886
external_platform: POJ
external_problem_id: '2886'
external_title: POJ 2886 Who Gets the Most Candies?
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
