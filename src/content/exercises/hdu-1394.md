---
id: hdu-1394
volume: upper
source_file: upper-volume
title: HDU 1394 Minimum Inversion Number
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 3
topics: &id001
  - inversion-count
  - rotation-formula
  - fenwick-tree
prerequisites:
  - segment-tree
statement: 0..n-1 的排列可反覆把首元素移到尾端，求所有循環位移中的最少逆序數。
constraints:
  - n <= 5000
  - 每組為 0..n-1 排列
  - 讀到 EOF
input_format: 每組 n 與排列。
output_format: 每組輸出最小逆序數。
samples:
  - input: |
      4
      1 3 2 0
    output: |
      1
    explanation: 枚舉循環位移後最少可只剩一個逆序。
core_knowledge: *id001
judgment: BIT 求初值，再用位移公式更新逆序數。
hints:
  - 先用 BIT 算原排列逆序數。
  - 把首值 x 移尾，少掉 x 個與較小值的逆序，增加 n-1-x 個與較大值的逆序。
  - 所以 inv += n-1-2x，可 O(n) 枚舉後續位移。
solution_outline: BIT 求初值，再用位移公式更新逆序數。
proof_or_invariant: 排列恰含 0..n-1，因此比 x 小有 x 個、比 x 大有 n-1-x 個；公式精確描述唯一改變的配對。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O(n log n)
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)++t[static_cast<size_t>(i)];}int sum(int x)const{int s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<int>t;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){vector<int>a(static_cast<size_t>(n));Fenwick bit(n);long long inv=0;for(int i=0;i<n;++i){cin>>a[static_cast<size_t>(i)];int rank=a[static_cast<size_t>(i)]+1;inv+=i-bit.sum(rank);bit.add(rank);}long long answer=inv;for(int x:a){inv+=n-1-2LL*x;answer=min(answer,inv);}cout<<answer<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)++t[static_cast<size_t>(i)];}int sum(int x)const{int s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<int>t;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){vector<int>a(static_cast<size_t>(n));Fenwick bit(n);long long inv=0;for(int i=0;i<n;++i){cin>>a[static_cast<size_t>(i)];int rank=a[static_cast<size_t>(i)]+1;inv+=i-bit.sum(rank);bit.add(rank);}long long answer=inv;for(int x:a){inv+=n-1-2LL*x;answer=min(answer,inv);}cout<<answer<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1394
external_platform: HDU
external_problem_id: '1394'
external_title: HDU 1394 Minimum Inversion Number
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
