---
id: hdu-1540
volume: upper
source_file: upper-volume
title: HDU 1540 Tunnel Warfare
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 3
topics: &id001
  - ordered-set
  - rollback-stack
  - predecessor-successor
prerequisites:
  - segment-tree
statement: 線上隧道點可摧毀，R 回復最近一次摧毀；Q x 查包含 x 的最長連續未毀區段長。
constraints:
  - N,M <= 50000
  - 摧毀操作可由棧回復
input_format: 每組 N M；D x、Q x 或 R。
output_format: 每個 Q 輸出長度。
samples:
  - input: |
      7 6
      D 3
      D 6
      Q 4
      Q 3
      R
      Q 4
    output: |
      2
      0
      4
    explanation: 毀 3、6 時位置 4 所在段為 4..5；回復 6 後延伸到 7。
core_knowledge: *id001
judgment: 有序集合維護毀點，棧支援回復。
hints:
  - 毀壞點把完整隧道切成若干開區間。
  - 若 x 未毀，找左右最近毀壞點即可得到段長。
  - R 是後進先出，用棧記 D 並從有序集合移除。
solution_outline: 有序集合維護毀點，棧支援回復。
proof_or_invariant: 左右相鄰毀點之間沒有其他毀點，因此其中所有位置連續可用且不能再向外延伸。
common_errors:
  - 懶標記合成順序錯誤
  - 閉區間端點或 0/1 起始索引混淆
  - 合併節點摘要時漏掉跨左右子樹的候選
complexity:
  time: O(M log N)
  space: O(M)
cpp_skeleton: |
  // TODO：依三階段提示完成資料結構。
  #include <bits/stdc++.h>
  using namespace std;int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){set<int>destroyed{0,n+1};vector<int>history;while(m--){char op;cin>>op;if(op=='D'){int x;cin>>x;destroyed.insert(x);history.push_back(x);}else if(op=='R'){if(!history.empty()){destroyed.erase(history.back());history.pop_back();}}else{int x;cin>>x;if(destroyed.count(x)!=0){cout<<0<<'\n';continue;}auto right=destroyed.upper_bound(x);int r=*right,l=*prev(right);cout<<r-l-1<<'\n';}}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){set<int>destroyed{0,n+1};vector<int>history;while(m--){char op;cin>>op;if(op=='D'){int x;cin>>x;destroyed.insert(x);history.push_back(x);}else if(op=='R'){if(!history.empty()){destroyed.erase(history.back());history.pop_back();}}else{int x;cin>>x;if(destroyed.count(x)!=0){cout<<0<<'\n';continue;}auto right=destroyed.upper_bound(x);int r=*right,l=*prev(right);cout<<r-l-1<<'\n';}}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1540
external_platform: HDU
external_problem_id: '1540'
external_title: HDU 1540 Tunnel Warfare
external_relation: original
source_book_pages:
  - 182
  - 192
source_pdf_pages:
  - 200
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
