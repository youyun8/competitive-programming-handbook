---
id: poj-2750
volume: upper
source_file: upper-volume
title: POJ 2750 Potted Flower
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - maximum-subarray
  - minimum-subarray
  - circular-array
prerequisites:
  - segment-tree
statement: 環狀序列支援單點賦值；每次輸出不能包含全部元素的非空連續環段最大和。
constraints:
  - 值介於 -1000..1000
  - 保證答案為正
input_format: 輸入 N、初值、M 與 M 個位置和值。
output_format: 每次修改後輸出答案。
samples:
  - input: |
      4
      1 2 3 4
      2
      1 -10
      1 5
    output: |
      9
      12
    explanation: 第一次選 2+3+4，第二次環段可選 3+4+5。
core_knowledge: *id001
judgment: 線段樹同時維護最大與最小非空子段摘要。
hints:
  - 不跨切口答案是線性最大子段和。
  - 跨切口答案等於總和減去中間最小子段和。
  - 若線性最大段是全序列，必須排除全選。
solution_outline: 線段樹同時維護最大與最小非空子段摘要。
proof_or_invariant: 環段或不跨切口，或為全體扣除一個非空線性段；兩類取最大並排除整環即完整。
common_errors:
  - 端點、開閉區間或 0/1 起始索引處理錯誤
  - 合併摘要時遺漏跨左右區間的候選
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O((N+M) log N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;struct N{long long sum=0,mp=LLONG_MIN/4,ms=LLONG_MIN/4,mb=LLONG_MIN/4,np=LLONG_MAX/4,ns=LLONG_MAX/4,nb=LLONG_MAX/4;};static N merge_n(const N&a,const N&b){N c;c.sum=a.sum+b.sum;c.mp=max(a.mp,a.sum+b.mp);c.ms=max(b.ms,b.sum+a.ms);c.mb=max({a.mb,b.mb,a.ms+b.mp});c.np=min(a.np,a.sum+b.np);c.ns=min(b.ns,b.sum+a.ns);c.nb=min({a.nb,b.nb,a.ns+b.np});return c;}static vector<N>tr;static vector<int>a;static void build(int p,int l,int r){if(l==r){long long v=a[static_cast<size_t>(l)];tr[static_cast<size_t>(p)]={v,v,v,v,v,v,v};return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);tr[static_cast<size_t>(p)]=merge_n(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static void update(int p,int l,int r,int x,int v){if(l==r){tr[static_cast<size_t>(p)]={v,v,v,v,v,v,v};return;}int m=(l+r)/2;if(x<=m)update(p*2,l,m,x,v);else update(p*2+1,m+1,r,x,v);tr[static_cast<size_t>(p)]=merge_n(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static N query(int p,int l,int r,int ql,int qr){if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)];int m=(l+r)/2;if(qr<=m)return query(p*2,l,m,ql,qr);if(ql>m)return query(p*2+1,m+1,r,ql,qr);return merge_n(query(p*2,l,m,ql,qr),query(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){a.assign(static_cast<size_t>(n+1),0);for(int i=1;i<=n;++i)cin>>a[static_cast<size_t>(i)];tr.assign(static_cast<size_t>(4*n+4),N{});build(1,1,n);int m;cin>>m;while(m--){int x,v;cin>>x>>v;update(1,1,n,x,v);N left=query(1,1,n,1,n-1),right=query(1,1,n,2,n);long long max_proper=max(left.mb,right.mb),min_proper=min(left.nb,right.nb);cout<<max(max_proper,tr[1].sum-min_proper)<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct N{long long sum=0,mp=LLONG_MIN/4,ms=LLONG_MIN/4,mb=LLONG_MIN/4,np=LLONG_MAX/4,ns=LLONG_MAX/4,nb=LLONG_MAX/4;};static N merge_n(const N&a,const N&b){N c;c.sum=a.sum+b.sum;c.mp=max(a.mp,a.sum+b.mp);c.ms=max(b.ms,b.sum+a.ms);c.mb=max({a.mb,b.mb,a.ms+b.mp});c.np=min(a.np,a.sum+b.np);c.ns=min(b.ns,b.sum+a.ns);c.nb=min({a.nb,b.nb,a.ns+b.np});return c;}static vector<N>tr;static vector<int>a;static void build(int p,int l,int r){if(l==r){long long v=a[static_cast<size_t>(l)];tr[static_cast<size_t>(p)]={v,v,v,v,v,v,v};return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);tr[static_cast<size_t>(p)]=merge_n(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static void update(int p,int l,int r,int x,int v){if(l==r){tr[static_cast<size_t>(p)]={v,v,v,v,v,v,v};return;}int m=(l+r)/2;if(x<=m)update(p*2,l,m,x,v);else update(p*2+1,m+1,r,x,v);tr[static_cast<size_t>(p)]=merge_n(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static N query(int p,int l,int r,int ql,int qr){if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)];int m=(l+r)/2;if(qr<=m)return query(p*2,l,m,ql,qr);if(ql>m)return query(p*2+1,m+1,r,ql,qr);return merge_n(query(p*2,l,m,ql,qr),query(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){a.assign(static_cast<size_t>(n+1),0);for(int i=1;i<=n;++i)cin>>a[static_cast<size_t>(i)];tr.assign(static_cast<size_t>(4*n+4),N{});build(1,1,n);int m;cin>>m;while(m--){int x,v;cin>>x>>v;update(1,1,n,x,v);N left=query(1,1,n,1,n-1),right=query(1,1,n,2,n);long long max_proper=max(left.mb,right.mb),min_proper=min(left.nb,right.nb);cout<<max(max_proper,tr[1].sum-min_proper)<<'\n';}}}
external_url: http://poj.org/problem?id=2750
external_platform: POJ
external_problem_id: '2750'
external_title: POJ 2750 Potted Flower
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
