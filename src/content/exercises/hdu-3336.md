---
id: hdu-3336
volume: lower
source_file: lower-volume
title: HDU 3336 Count the string
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [prefix-function, border-dp, counting]
prerequisites: [kmp, string]
statement: '求字串每個非空前綴在整串中的出現次數總和 modulo 10007。'
constraints:
  - '1 <= T'
  - '1 <= n <= 200000'
  - '字串只含小寫字母'
input_format: '第一行 T；每組給 n 與字串。'
output_format: '每組輸出總和 modulo 10007。'
samples:
  - input: "1\n4\nabab\n"
    output: '6'
    explanation: '前綴 a、ab 各出現兩次，aba、abab 各一次，總和 6。'
core_knowledge:
  - 'prefix-function'
  - 'border-dp'
  - 'counting'
judgment: '每組輸出總和 modulo 10007。'
hints:
  - '對每個結束位置，所有同時是整串前綴的後綴沿 pi 鏈排列。'
  - '令 dp[i]=dp[pi[i]]+1，表示長度 i 前綴末端有多少個前綴匹配。'
  - '把所有 i 的 dp 相加並隨時 modulo 10007。'
solution_outline: '計算 1-based failure 長度並依序做 border DP。'
proof_or_invariant: '每個前綴本身貢獻一，其他匹配前綴恰是其最長 border 的所有匹配，故遞推不漏不重；跨所有結尾相加等於所有前綴出現數。'
common_errors:
  - '漏算前綴本身'
  - 'modulus 寫錯'
  - 'pi 索引混用'
complexity:
  time: 'O(n)'
  space: 'O(n)'
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成核心演算法。*/return 0;}
cpp_solution: |
  #include <iostream>
  #include <string>
  #include <vector>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int t=0;cin>>t;while(t--){int n=0;string s;cin>>n>>s;vector<int> pi(static_cast<size_t>(n+1)),dp(static_cast<size_t>(n+1));int ans=0;for(int i=2,j=0;i<=n;++i){while(j>0&&s[static_cast<size_t>(i-1)]!=s[static_cast<size_t>(j)])j=pi[static_cast<size_t>(j)];if(s[static_cast<size_t>(i-1)]==s[static_cast<size_t>(j)])++j;pi[static_cast<size_t>(i)]=j;}for(int i=1;i<=n;++i){dp[static_cast<size_t>(i)]=dp[static_cast<size_t>(pi[static_cast<size_t>(i)])]+1;ans=(ans+dp[static_cast<size_t>(i)])%10007;}cout<<ans<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3336
external_platform: HDU
external_problem_id: '3336'
external_title: 'Count the string'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

以線性字串結構重用已知前後綴資訊，避免重新比較。
