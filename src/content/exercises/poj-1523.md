---
id: poj-1523
volume: lower
source_file: lower-volume
original_label: OpenJ_Bailian 1523
title: OpenJudge 1523 SPF
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 3
topics: [articulation-point, low-link]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  輸入若干網路連線，找出所有單點故障會把原連通網路分成多個子網的 SPF 節點及子網數。
constraints: [節點編號為正整數, 每個網路以 0 結束, 全部輸入再以額外的 0 結束]
input_format: >-
  每行一條無向邊 u v；單獨 0 結束目前網路。
output_format: >-
  依案例與節點遞增輸出 SPF node u leaves k subnets；無 SPF 時輸出 No SPF nodes。
samples:
  - input: |
      1 2
      2 3
      2 4
      0
      0
    output: |
      Network #1
        SPF node 2 leaves 3 subnets
    explanation: >-
      移除 2 後，1、3、4 各成一個子網。
core_knowledge: [割點, 移除後分量數]
judgment: >-
  DFS 根的子網數為子樹數；非根為 1 加上 low[child]>=dfn[u] 的子樹數。
hints:
  - 讀到第一個端點 0 即結束網路。
  - 非根還有包含父方向的那一塊。
  - 只輸出子網數大於一的節點。
solution_outline: >-
  建立每案實際出現節點的圖，以 Tarjan 計每點移除後分量數並格式化輸出。
proof_or_invariant: >-
  每個 low 不小於 u 的子樹在移除 u 後各自孤立；非根其餘祖先與可回祖先的子樹同屬一塊。根則每棵 DFS 子樹各一塊。
complexity: { time: 'O(V + E)', space: 'O(V + E)' }
common_errors: [非根漏加父方向子網, 輸出未依編號排序, 案例間漏空行]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int first,cs=0;while(cin>>first&&first){int second;cin>>second;vector<pair<int,int>>e{{first,second}};int mx=max(first,second);while(cin>>first&&first){cin>>second;e.push_back({first,second});mx=max(mx,max(first,second));}vector<vector<int>>g(mx+1);vector<char>exists(mx+1);for(auto [u,v]:e)g[u].push_back(v),g[v].push_back(u),exists[u]=exists[v]=1;vector<int>d(mx+1),low(mx+1),parts(mx+1,1);int timer=0;auto dfs=[&](auto&&self,int u,int parent)->void{d[u]=low[u]=++timer;int child=0;for(int v:g[u])if(v!=parent){if(!d[v]){++child;self(self,v,u);low[u]=min(low[u],low[v]);if(parent>=0&&low[v]>=d[u])++parts[u];}else low[u]=min(low[u],d[v]);}if(parent<0)parts[u]=child;};for(int i=1;i<=mx;++i)if(exists[i]&&!d[i])dfs(dfs,i,-1);cout<<"Network #"<<++cs<<'\n';bool any=false;for(int i=1;i<=mx;++i)if(parts[i]>1){cout<<"  SPF node "<<i<<" leaves "<<parts[i]<<" subnets\n";any=true;}if(!any)cout<<"  No SPF nodes\n";cout<<'\n';}}
external_url: http://bailian.openjudge.cn/practice/1523/
external_platform: OpenJ_Bailian
external_problem_id: '1523'
external_title: 'SPF'
external_relation: original
source_book_pages: [619]
source_pdf_pages: [249]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
