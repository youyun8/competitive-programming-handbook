---
id: hdu-4027
volume: upper
source_file: upper-volume
title: HDU 4027 Can you answer these queries?
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - segment-tree
  - stabilizing-update
  - range-sum
prerequisites:
  - segment-tree
statement: 維護正整數序列：區間每數改為 floor(sqrt(x))，或查區間和。
constraints:
  - n <= 100000
  - 元素 <= 2^63-1
  - 多組至 EOF
input_format: 每組 n、序列、m；0 l r 開根，1 l r 求和。
output_format: '每組先印 Case #k:，查詢逐行輸出，組間空行。'
samples:
  - input: |
      3
      16 1 10
      3
      1 1 3
      0 1 3
      1 1 3
    output: |+
      Case #1:
      27
      8

    explanation: 開根後成 4、1、3，和為 8。
core_knowledge: *id001
judgment: 線段樹維護 sum、max，以穩定值剪枝區間開根。
hints:
  - 數字反覆開根很快固定在 0 或 1。
  - 節點維護最大值；max<=1 時整段更新可剪枝。
  - 只在未固定葉節點真正計算平方根，再向上合併和與最大值。
solution_outline: 線段樹維護 sum、max，以穩定值剪枝區間開根。
proof_or_invariant: max<=1 保證整段值不變；其餘遞迴至葉精確更新。每元素能嚴格變小僅 O(log log A) 次。
common_errors:
  - 懶標記合成順序錯誤
  - 閉區間端點或 0/1 起始索引混淆
  - 合併節點摘要時漏掉跨左右子樹的候選
complexity:
  time: O((n log log A+m) log n) 攤銷
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成資料結構。
  #include <bits/stdc++.h>
  using namespace std;
  struct Node{long long sum=0,mx=0;};static vector<Node>tree;static vector<long long>a;static void build(int p,int l,int r){if(l==r){tree[static_cast<size_t>(p)]={a[static_cast<size_t>(l)],a[static_cast<size_t>(l)]};return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);tree[static_cast<size_t>(p)]={tree[static_cast<size_t>(p*2)].sum+tree[static_cast<size_t>(p*2+1)].sum,max(tree[static_cast<size_t>(p*2)].mx,tree[static_cast<size_t>(p*2+1)].mx)};}static void root_update(int p,int l,int r,int ql,int qr){if(qr<l||r<ql||tree[static_cast<size_t>(p)].mx<=1)return;if(l==r){long long v=static_cast<long long>(sqrt(static_cast<long double>(tree[static_cast<size_t>(p)].sum)));tree[static_cast<size_t>(p)]={v,v};return;}int m=(l+r)/2;root_update(p*2,l,m,ql,qr);root_update(p*2+1,m+1,r,ql,qr);tree[static_cast<size_t>(p)]={tree[static_cast<size_t>(p*2)].sum+tree[static_cast<size_t>(p*2+1)].sum,max(tree[static_cast<size_t>(p*2)].mx,tree[static_cast<size_t>(p*2+1)].mx)};}static long long query(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tree[static_cast<size_t>(p)].sum;int m=(l+r)/2;return query(p*2,l,m,ql,qr)+query(p*2+1,m+1,r,ql,qr);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,case_no=0;while(cin>>n){a.assign(static_cast<size_t>(n+1),0);for(int i=1;i<=n;++i)cin>>a[static_cast<size_t>(i)];tree.assign(static_cast<size_t>(4*n+4),Node{});build(1,1,n);int m;cin>>m;cout<<"Case #"<<++case_no<<":\n";while(m--){int op,l,r;cin>>op>>l>>r;if(l>r)swap(l,r);if(op==0)root_update(1,1,n,l,r);else cout<<query(1,1,n,l,r)<<'\n';}cout<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Node{long long sum=0,mx=0;};static vector<Node>tree;static vector<long long>a;static void build(int p,int l,int r){if(l==r){tree[static_cast<size_t>(p)]={a[static_cast<size_t>(l)],a[static_cast<size_t>(l)]};return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);tree[static_cast<size_t>(p)]={tree[static_cast<size_t>(p*2)].sum+tree[static_cast<size_t>(p*2+1)].sum,max(tree[static_cast<size_t>(p*2)].mx,tree[static_cast<size_t>(p*2+1)].mx)};}static void root_update(int p,int l,int r,int ql,int qr){if(qr<l||r<ql||tree[static_cast<size_t>(p)].mx<=1)return;if(l==r){long long v=static_cast<long long>(sqrt(static_cast<long double>(tree[static_cast<size_t>(p)].sum)));tree[static_cast<size_t>(p)]={v,v};return;}int m=(l+r)/2;root_update(p*2,l,m,ql,qr);root_update(p*2+1,m+1,r,ql,qr);tree[static_cast<size_t>(p)]={tree[static_cast<size_t>(p*2)].sum+tree[static_cast<size_t>(p*2+1)].sum,max(tree[static_cast<size_t>(p*2)].mx,tree[static_cast<size_t>(p*2+1)].mx)};}static long long query(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tree[static_cast<size_t>(p)].sum;int m=(l+r)/2;return query(p*2,l,m,ql,qr)+query(p*2+1,m+1,r,ql,qr);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,case_no=0;while(cin>>n){a.assign(static_cast<size_t>(n+1),0);for(int i=1;i<=n;++i)cin>>a[static_cast<size_t>(i)];tree.assign(static_cast<size_t>(4*n+4),Node{});build(1,1,n);int m;cin>>m;cout<<"Case #"<<++case_no<<":\n";while(m--){int op,l,r;cin>>op>>l>>r;if(l>r)swap(l,r);if(op==0)root_update(1,1,n,l,r);else cout<<query(1,1,n,l,r)<<'\n';}cout<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4027
external_platform: HDU
external_problem_id: '4027'
external_title: HDU 4027 Can you answer these queries?
external_relation: original
source_book_pages:
  - 182
  - 192
source_pdf_pages:
  - 200
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
