---
id: hdu-3397
volume: upper
source_file: upper-volume
title: HDU 3397 Sequence operation
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - lazy-segment-tree
  - binary-run-summary
  - tag-composition
prerequisites:
  - segment-tree
statement: 二進位序列支援區間設 0、設 1、翻轉，查 1 的數量或最長連續 1。
constraints:
  - T 組
  - n,m <= 100000
  - 索引從 0 起
input_format: 操作 0/1/2 l r 更新；3 查總數；4 查最長連續 1。
output_format: 依序輸出查詢。
samples:
  - input: |
      1
      5 5
      1 0 1 1 0
      3 0 4
      4 0 4
      2 1 4
      3 0 4
      4 0 4
    output: |
      3
      2
      3
      2
    explanation: 翻轉後序列 1,1,0,0,1，共三個 1，最長連續二。
core_knowledge: *id001
judgment: 線段樹維護雙值連續段摘要與賦值／翻轉標記。
hints:
  - 同時維護 0 與 1 的前綴、後綴、最長段。
  - 翻轉只需交換兩組摘要；賦值會清除翻轉標記。
  - 合併最長段要比較左右答案與左後綴+右前綴。
solution_outline: 線段樹維護雙值連續段摘要與賦值／翻轉標記。
proof_or_invariant: 摘要合併枚舉最長段在左、右或跨界三種情況；標記操作與位元語意完全一致。
common_errors:
  - 懶標記合成順序錯誤
  - 閉區間端點或 0/1 起始索引混淆
  - 合併節點摘要時漏掉跨左右子樹的候選
complexity:
  time: O((n+m) log n)
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成資料結構。
  #include <bits/stdc++.h>
  using namespace std;struct Node{int len=0,ones=0,p1=0,s1=0,b1=0,p0=0,s0=0,b0=0,assign=-1;bool flip=false;};static vector<Node>tr;static Node merge_node(const Node&a,const Node&b){Node c;c.len=a.len+b.len;c.ones=a.ones+b.ones;c.p1=a.p1+(a.p1==a.len?b.p1:0);c.s1=b.s1+(b.s1==b.len?a.s1:0);c.b1=max({a.b1,b.b1,a.s1+b.p1});c.p0=a.p0+(a.p0==a.len?b.p0:0);c.s0=b.s0+(b.s0==b.len?a.s0:0);c.b0=max({a.b0,b.b0,a.s0+b.p0});return c;}static void set_value(int p,int v){Node&x=tr[static_cast<size_t>(p)];x.ones=v*x.len;x.p1=x.s1=x.b1=v?x.len:0;x.p0=x.s0=x.b0=v?0:x.len;x.assign=v;x.flip=false;}static void flip_value(int p){Node&x=tr[static_cast<size_t>(p)];x.ones=x.len-x.ones;swap(x.p1,x.p0);swap(x.s1,x.s0);swap(x.b1,x.b0);if(x.assign>=0)x.assign^=1;else x.flip=!x.flip;}static void build(int p,int l,int r,const vector<int>&a){tr[static_cast<size_t>(p)].len=r-l+1;if(l==r){set_value(p,a[static_cast<size_t>(l)]);tr[static_cast<size_t>(p)].assign=-1;return;}int m=(l+r)/2;build(p*2,l,m,a);build(p*2+1,m+1,r,a);tr[static_cast<size_t>(p)]=merge_node(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static void push(int p){if(tr[static_cast<size_t>(p)].assign>=0){set_value(p*2,tr[static_cast<size_t>(p)].assign);set_value(p*2+1,tr[static_cast<size_t>(p)].assign);tr[static_cast<size_t>(p)].assign=-1;}if(tr[static_cast<size_t>(p)].flip){flip_value(p*2);flip_value(p*2+1);tr[static_cast<size_t>(p)].flip=false;}}static void update(int p,int l,int r,int ql,int qr,int op){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){if(op<2)set_value(p,op);else flip_value(p);return;}push(p);int m=(l+r)/2;update(p*2,l,m,ql,qr,op);update(p*2+1,m+1,r,ql,qr,op);tr[static_cast<size_t>(p)]=merge_node(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static Node query(int p,int l,int r,int ql,int qr){if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)];push(p);int m=(l+r)/2;if(qr<=m)return query(p*2,l,m,ql,qr);if(ql>m)return query(p*2+1,m+1,r,ql,qr);return merge_node(query(p*2,l,m,ql,qr),query(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;vector<int>a(static_cast<size_t>(n));for(int&x:a)cin>>x;tr.assign(static_cast<size_t>(4*n+4),Node{});build(1,0,n-1,a);while(m--){int op,l,r;cin>>op>>l>>r;if(op<=2)update(1,0,n-1,l,r,op);else{Node ans=query(1,0,n-1,l,r);cout<<(op==3?ans.ones:ans.b1)<<'\n';}}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct Node{int len=0,ones=0,p1=0,s1=0,b1=0,p0=0,s0=0,b0=0,assign=-1;bool flip=false;};static vector<Node>tr;static Node merge_node(const Node&a,const Node&b){Node c;c.len=a.len+b.len;c.ones=a.ones+b.ones;c.p1=a.p1+(a.p1==a.len?b.p1:0);c.s1=b.s1+(b.s1==b.len?a.s1:0);c.b1=max({a.b1,b.b1,a.s1+b.p1});c.p0=a.p0+(a.p0==a.len?b.p0:0);c.s0=b.s0+(b.s0==b.len?a.s0:0);c.b0=max({a.b0,b.b0,a.s0+b.p0});return c;}static void set_value(int p,int v){Node&x=tr[static_cast<size_t>(p)];x.ones=v*x.len;x.p1=x.s1=x.b1=v?x.len:0;x.p0=x.s0=x.b0=v?0:x.len;x.assign=v;x.flip=false;}static void flip_value(int p){Node&x=tr[static_cast<size_t>(p)];x.ones=x.len-x.ones;swap(x.p1,x.p0);swap(x.s1,x.s0);swap(x.b1,x.b0);if(x.assign>=0)x.assign^=1;else x.flip=!x.flip;}static void build(int p,int l,int r,const vector<int>&a){tr[static_cast<size_t>(p)].len=r-l+1;if(l==r){set_value(p,a[static_cast<size_t>(l)]);tr[static_cast<size_t>(p)].assign=-1;return;}int m=(l+r)/2;build(p*2,l,m,a);build(p*2+1,m+1,r,a);tr[static_cast<size_t>(p)]=merge_node(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static void push(int p){if(tr[static_cast<size_t>(p)].assign>=0){set_value(p*2,tr[static_cast<size_t>(p)].assign);set_value(p*2+1,tr[static_cast<size_t>(p)].assign);tr[static_cast<size_t>(p)].assign=-1;}if(tr[static_cast<size_t>(p)].flip){flip_value(p*2);flip_value(p*2+1);tr[static_cast<size_t>(p)].flip=false;}}static void update(int p,int l,int r,int ql,int qr,int op){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){if(op<2)set_value(p,op);else flip_value(p);return;}push(p);int m=(l+r)/2;update(p*2,l,m,ql,qr,op);update(p*2+1,m+1,r,ql,qr,op);tr[static_cast<size_t>(p)]=merge_node(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static Node query(int p,int l,int r,int ql,int qr){if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)];push(p);int m=(l+r)/2;if(qr<=m)return query(p*2,l,m,ql,qr);if(ql>m)return query(p*2+1,m+1,r,ql,qr);return merge_node(query(p*2,l,m,ql,qr),query(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;vector<int>a(static_cast<size_t>(n));for(int&x:a)cin>>x;tr.assign(static_cast<size_t>(4*n+4),Node{});build(1,0,n-1,a);while(m--){int op,l,r;cin>>op>>l>>r;if(op<=2)update(1,0,n-1,l,r,op);else{Node ans=query(1,0,n-1,l,r);cout<<(op==3?ans.ones:ans.b1)<<'\n';}}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3397
external_platform: HDU
external_problem_id: '3397'
external_title: HDU 3397 Sequence operation
external_relation: original
source_book_pages:
  - 182
  - 192
source_pdf_pages:
  - 200
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
