---
id: hdu-5976
volume: lower
source_file: lower-volume
source_book_pages:
  - 427
source_pdf_pages:
  - 57
title: HDU 5976 Detachment
chapter: 6
section: '6.9'
kind: external-oj
difficulty: 3
topics:
  - 線性代數
  - 數論
prerequisites:
  - 高斯消元與圖論
statement: 把長度 x 分成互不相同的正整數，使乘積最大；輸出最大乘積模 10^9+7。
constraints:
  - 1<=T<=10^6
  - 1<=x<=10^9
input_format: 依題面讀入維度、矩陣、圖或測試資料。
output_format: 依指定精度與固定字串輸出答案。
samples:
  - input: |
      1
      4
    output: |
      4
    explanation: 依操作定義或方程直接驗算，可得到所示結果。
core_knowledge:
  - 不變量與代數建模
  - 消元或狀態搜尋
judgment: 先取互異最小段 2..p，再把餘量由大端均勻分配；所得連續乘積僅缺一項，可用階乘及逆元計算。
hints:
  - 先將幾何、操作或連通條件寫成方程或有限狀態。
  - 選擇符合代數結構的消元、矩陣樹或 BFS。
  - 最後處理唯一性、模數、精度與輸出方案。
solution_outline: 先取互異最小段 2..p，再把餘量由大端均勻分配；所得連續乘積僅缺一項，可用階乘及逆元計算。
proof_or_invariant: 固定段數時乘積在各段盡量接近時最大；互異限制使最優值為相鄰整數，餘量加到較大項避免相等。枚舉最大可行段數即全域最優。
complexity:
  time: 預處理 O(sqrt(max x))，每組 O(log x+log M)
  space: O(sqrt(max x))
common_errors:
  - 主元或狀態編號錯誤
  - 非質數模數誤用逆元
  - 輸出精度或固定字串不符
cpp_skeleton: |
  // TODO：依證明自行重寫核心步驟。
  #include <bits/stdc++.h>
  using namespace std;
  const long long M=1000000007;long long pw(long long a,long long e){long long r=1;for(;e;e>>=1,a=a*a%M)if(e&1)r=r*a%M;return r;}int main(){const int V=50000;vector<long long>s(V),f(V,1);for(int i=2;i<V;++i)s[i]=s[i-1]+i,f[i]=f[i-1]*i%M;int T;cin>>T;while(T--){long long x;cin>>x;if(x<5){cout<<x<<"\n";continue;}int p=int(upper_bound(s.begin()+2,s.end(),x)-s.begin())-1;long long d=x-s[p],ans;if(d==p)ans=f[p]*pw(2,M-2)%M*(p+2)%M;else ans=f[p+1]*pw(p+1-d,M-2)%M;cout<<ans<<"\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  const long long M=1000000007;long long pw(long long a,long long e){long long r=1;for(;e;e>>=1,a=a*a%M)if(e&1)r=r*a%M;return r;}int main(){const int V=50000;vector<long long>s(V),f(V,1);for(int i=2;i<V;++i)s[i]=s[i-1]+i,f[i]=f[i-1]*i%M;int T;cin>>T;while(T--){long long x;cin>>x;if(x<5){cout<<x<<"\n";continue;}int p=int(upper_bound(s.begin()+2,s.end(),x)-s.begin())-1;long long d=x-s[p],ans;if(d==p)ans=f[p]*pw(2,M-2)%M*(p+2)%M;else ans=f[p+1]*pw(p+1-d,M-2)%M;cout<<ans<<"\n";}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5976
external_platform: HDU
external_problem_id: '5976'
external_title: Detachment
external_relation: original
review_status: verified
---

將題意轉成可驗證的代數或圖模型。
