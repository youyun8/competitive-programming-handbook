---
id: hdu-5421
volume: lower
source_file: lower-volume
title: HDU 5421 Victor and String
chapter: 9
section: '9.4'
kind: external-oj
difficulty: 5
topics: [double-ended-pam, palindrome-count, online-string]
prerequisites: [palindromic-tree, failure-link]
statement: '從空字串開始支援首端加字、尾端加字、詢問本質不同迴文數、詢問所有迴文區間數。'
constraints:
  - '測資不超過 5 組'
  - '每組操作數 1 到 100000'
  - '加入字元為小寫英文字母'
input_format: '每組先 n，接著 n 行操作；1 c 加首端、2 c 加尾端、3/4 查詢。'
output_format: '操作 3 輸出不同迴文數；操作 4 輸出含位置重複的迴文總數。'
samples:
  - input: "6\n1 a\n1 b\n2 a\n2 c\n3\n4\n"
    output: "4\n5"
    explanation: '最後字串 baac 的不同迴文為 a、b、c、aa，共四種；迴文區間共五個。'
core_knowledge:
  - 'double-ended-pam'
  - 'palindrome-count'
  - 'online-string'
judgment: '操作 3 輸出不同迴文數；操作 4 輸出含位置重複的迴文總數。'
hints:
  - '維護 pre 與 suf，分別是目前最長迴文前綴與後綴。'
  - '向某端加入時沿該端 fail 找可擴展節點；節點 fail 深度是新增的迴文區間數。'
  - '若加入後整串成為迴文，另一端 last 也必須同步到同一節點。'
solution_outline: '在雙端陣列上共用一棵 PAM，依方向擴展；新節點增加不同迴文數，每次把 last 的 fail 深度加入總區間數。'
proof_or_invariant: 'PAM 節點一一對應本質迴文；每次端點加入只新增以該端結束的區間，其種類恰為 last 的 fail 鏈。整串迴文時同步兩端維持兩個 last 不變量。'
common_errors:
  - '首端索引方向寫反'
  - '整串迴文後未同步另一端 last'
  - '查詢 4 只輸出節點數'
complexity:
  time: 'O(n)'
  space: 'O(n*26)'
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：完成 PAM 狀態與轉移。*/return 0;}
cpp_solution: |
  #include <array>
  #include <iostream>
  #include <vector>
  using namespace std;
  struct Node{array<int,26> next{};int fail=0;int length=0;int depth=0;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int operations=0;while(cin>>operations){vector<Node>a(2);a[0].fail=1;a[1].fail=1;a[1].length=-1;vector<int>s(static_cast<size_t>(2*operations+3),-1);int left=operations+1,right=operations,front=0,back=0;long long total=0;auto extend=[&](int position,int direction,int&last,int c){int p=last;while(s[static_cast<size_t>(position)]!=s[static_cast<size_t>(position-direction*a[static_cast<size_t>(p)].length-direction)])p=a[static_cast<size_t>(p)].fail;if(a[static_cast<size_t>(p)].next[static_cast<size_t>(c)]==0){int created=static_cast<int>(a.size()),q=a[static_cast<size_t>(p)].fail;a.push_back({});a[static_cast<size_t>(created)].length=a[static_cast<size_t>(p)].length+2;while(s[static_cast<size_t>(position)]!=s[static_cast<size_t>(position-direction*a[static_cast<size_t>(q)].length-direction)])q=a[static_cast<size_t>(q)].fail;a[static_cast<size_t>(created)].fail=a[static_cast<size_t>(q)].next[static_cast<size_t>(c)];a[static_cast<size_t>(created)].depth=a[static_cast<size_t>(a[static_cast<size_t>(created)].fail)].depth+1;a[static_cast<size_t>(p)].next[static_cast<size_t>(c)]=created;}last=a[static_cast<size_t>(p)].next[static_cast<size_t>(c)];total+=a[static_cast<size_t>(last)].depth;if(a[static_cast<size_t>(last)].length==right-left+1)front=back=last;};while(operations--){int op=0;cin>>op;if(op<=2){char ch='a';cin>>ch;int c=ch-'a';if(op==1){--left;s[static_cast<size_t>(left)]=c;extend(left,-1,front,c);}else{++right;s[static_cast<size_t>(right)]=c;extend(right,1,back,c);}}else if(op==3)cout<<static_cast<int>(a.size())-2<<'\n';else cout<<total<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5421
external_platform: HDU
external_problem_id: '5421'
external_title: 'HDU 5421 Victor and String'
external_relation: original
source_book_pages: [580, 595]
source_pdf_pages: [210, 225]
review_status: verified
---

迴文樹把所有本質不同的迴文壓成線性數量狀態。
