---
id: hdu-2544
volume: lower
source_file: lower-volume
original_label: HDU 2544
title: HDU 2544 最短路：Dijkstra 入門
chapter: 10
section: '10.8'
kind: external-oj
difficulty: 3
topics: [最短路徑, Dijkstra, 優先佇列]
prerequisites: [dijkstra]
core_knowledge: [非負權最短路, 無向圖建邊, 惰性刪除]
judgment: 所有路權為正，適合由 1 號點執行 Dijkstra。
statement: 多組帶正權無向圖，求商店 1 到賽場 n 的最短時間；0 0 結束。
constraints: ['n <= 100', 'm <= 10000', '1 <= 邊權 <= 1000']
input_format: 每組先給 n、m，再給 m 條 a、b、c；0 0 結束。
output_format: 每組輸出 1 到 n 的最短距離。
samples:
  - input: |-
      2 1
      1 2 3
      3 3
      1 2 5
      2 3 5
      3 1 2
      0 0
    output: |-
      3
      2
    explanation: 第一組只有一條路；第二組直接由 1 到 3 的時間 2 優於經過 2 的時間 10。
hints:
  - 道路雙向，讀入一條要加入兩個方向。
  - 小根堆存「目前距離、節點」，取出過期項目時直接略過。
  - 正權保證最小距離項目一旦確定，之後不可能被繞路改善。
solution_outline: 鄰接表存雙向邊，從 1 執行優先佇列版 Dijkstra，輸出 dist[n]。
proof_or_invariant: 每次取出的未過期堆頂具有全體暫定距離最小值；因邊權非負，任何尚未探索路徑都不可能使它更短。逐點確定後 dist[n] 即最短距離。
complexity: { time: 'O((n+m) log n)', space: 'O(n+m)' }
common_errors: [只加入單向邊, 沒有處理多組資料與 0 0, 未略過堆中過期項目]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      int n, m;
      while (cin >> n >> m && (n || m)) {
          // TODO：建雙向圖並由 1 執行 Dijkstra。
      }
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      int n, m;
      while (cin >> n >> m && (n != 0 || m != 0)) {
          vector<vector<pair<int, int>>> graph(static_cast<size_t>(n + 1));
          for (int i = 0; i < m; ++i) {
              int a, b, cost; cin >> a >> b >> cost;
              graph[static_cast<size_t>(a)].push_back({b, cost});
              graph[static_cast<size_t>(b)].push_back({a, cost});
          }
          const long long inf = LLONG_MAX / 4;
          vector<long long> dist(static_cast<size_t>(n + 1), inf);
          priority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> queue;
          dist[1] = 0; queue.push({0, 1});
          while (!queue.empty()) {
              const auto [distance, node] = queue.top(); queue.pop();
              if (distance != dist[static_cast<size_t>(node)]) continue;
              for (const auto& [next, weight] : graph[static_cast<size_t>(node)])
                  if (distance + weight < dist[static_cast<size_t>(next)]) {
                      dist[static_cast<size_t>(next)] = distance + weight;
                      queue.push({dist[static_cast<size_t>(next)], next});
                  }
          }
          cout << dist[static_cast<size_t>(n)] << '\n';
      }
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2544
external_platform: HDU
external_problem_id: '2544'
external_title: 最短路
external_relation: original
source_book_pages: [648, 650, 655]
source_pdf_pages: [278, 280, 285]
review_status: verified
---

這題適合用來檢查 Dijkstra 的建圖、堆與多組資料三個基本細節。
