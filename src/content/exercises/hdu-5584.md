---
id: hdu-5584
volume: lower
source_file: lower-volume
source_book_pages:
  - 387
  - 461
source_pdf_pages:
  - 17
  - 91
title: HDU 5584 LCM Walk
chapter: 6
section: '6.7'
kind: external-oj
difficulty: 2
topics:
  - 最大公因數
  - 數論建模
prerequisites:
  - 整除與同餘
statement: 青蛙由正座標 (x,y) 每次將一個座標增加 lcm(x,y)。給定終點，求能到達它的可能起點數。
constraints:
  - 1<=T<=100
  - 1<=ex,ey<=10^9
input_format: 依題目格式讀入測試組數與各組參數；多組至 EOF 或終止值者見敘述。
output_format: 依題目指定格式逐組輸出答案。
samples:
  - input: |
      2
      1 1
      2 1
    output: |
      Case #1: 1
      Case #2: 2
    explanation: 依定義計算或套用推導後的充要條件，可得上述結果。
core_knowledge:
  - gcd/lcm 性質
  - 等價轉換
judgment: 逆推時較大座標必是最後被增加者；保持 gcd 不變後，其前值唯一為 larger/(smaller/g+1)，整除時才合法。
hints:
  - 先確認哪些量在一次操作或遞推中保持不變。
  - 用 gcd、質因數或同餘式將候選集合縮小。
  - 證明轉換雙向成立後，再處理邊界、溢位及固定輸出格式。
solution_outline: 逆推時較大座標必是最後被增加者；保持 gcd 不變後，其前值唯一為 larger/(smaller/g+1)，整除時才合法。
proof_or_invariant: 一次正向跳躍不改 gcd，且新增量至少為另一座標，所以終點較大座標唯一指出最後方向；逆式充要且每次嚴格減小。
complexity:
  time: 每組 O(log max(ex,ey))
  space: O(1)
common_errors:
  - 只證明必要條件而未證明充分性
  - 中間乘積溢位或負餘數未正規化
  - 漏掉最小輸入與終止條件
cpp_skeleton: |
  // TODO：依證明自行重寫核心轉換。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;for(int tc=1;tc<=T;++tc){long long x,y,ans=1;cin>>x>>y;while(true){if(x<y)swap(x,y);long long g=gcd(x,y),q=y/g+1;if(x%q)break;x/=q;++ans;}cout<<"Case #"<<tc<<": "<<ans<<"\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;for(int tc=1;tc<=T;++tc){long long x,y,ans=1;cin>>x>>y;while(true){if(x<y)swap(x,y);long long g=gcd(x,y),q=y/g+1;if(x%q)break;x/=q;++ans;}cout<<"Case #"<<tc<<": "<<ans<<"\n";}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5584
external_platform: HDU
external_problem_id: '5584'
external_title: LCM Walk
external_relation: original
review_status: verified
---

本題重點是把操作轉成可驗證的數論條件。
