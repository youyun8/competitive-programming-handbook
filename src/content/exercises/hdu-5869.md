---
id: hdu-5869
volume: upper
source_file: upper-volume
title: HDU 5869 Different GCD Subarray Query：區間子段 GCD 種數
chapter: 4
section: '4.2'
kind: external-oj
difficulty: 4
topics: &id001
  - gcd-compression
  - offline-query
  - fenwick-tree
prerequisites:
  - fenwick-tree
statement: 每次詢問 [L,R] 內所有連續子段能產生多少種不同最大公因數。
constraints:
  - N,Q <= 100000
  - 1 <= ai <= 1000000
  - 多組讀到 EOF
input_format: 每組 N Q、陣列與 Q 個 L R。
output_format: 每問輸出不同 GCD 數。
samples:
  - input: |
      4 2
      2 4 6 3
      1 3
      2 4
    output: |
      3
      5
    explanation: '[1,3] 的所有子段 GCD 只有 2、4、6 三種；[2,4] 則有 1、2、3、4、6 五種。'
core_knowledge: *id001
judgment: 壓縮每個右端點的後綴 GCD，離線 BIT 維護每種 GCD 的最大起點。
hints:
  - 固定右端點時，不同後綴 GCD 每次嚴格變小，種類為對數級。
  - 對每個 GCD 只保留能服務最靠右 L 的最大起點。
  - 按 R 掃描，BIT 中移除舊標記、加入新起點，查 [L,R] 標記數。
solution_outline: 壓縮每個右端點的後綴 GCD，離線 BIT 維護每種 GCD 的最大起點。
proof_or_invariant: 某 GCD 能出現在 [L,R] 當且僅當截至 R 的最大合法起點至少 L；每種只留一個標記即可不重不漏。
common_errors:
  - 索引基準或閉區間端點處理錯誤
  - 更新資料結構後忘記同步原始狀態
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O((N log A+Q) log N)
  space: O(N+A+Q)
cpp_skeleton: |
  // TODO：先依三階段提示自行完成核心；以下框架可嚴格編譯。
  #include <bits/stdc++.h>
  using namespace std;
  class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}long long sum(int x)const{long long s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<long long>t;};
  struct Query{int l,r,id;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,q;while(cin>>n>>q){vector<int>a(static_cast<size_t>(n+1));for(int i=1;i<=n;++i)cin>>a[static_cast<size_t>(i)];vector<Query>qs(static_cast<size_t>(q));for(int i=0;i<q;++i){cin>>qs[static_cast<size_t>(i)].l>>qs[static_cast<size_t>(i)].r;qs[static_cast<size_t>(i)].id=i;}sort(qs.begin(),qs.end(),[](const Query&x,const Query&y){return x.r<y.r;});Fenwick bit(n);vector<int>last(1000001),ans(static_cast<size_t>(q));vector<pair<int,int>>prev;size_t qi=0;for(int r=1;r<=n;++r){vector<pair<int,int>>cur{{a[static_cast<size_t>(r)],r}};for(auto [g,l]:prev){int ng=gcd(g,a[static_cast<size_t>(r)]);if(cur.back().first==ng)cur.back().second=max(cur.back().second,l);else cur.push_back({ng,l});}for(auto [g,l]:cur){if(last[static_cast<size_t>(g)]!=0)bit.add(last[static_cast<size_t>(g)],-1);last[static_cast<size_t>(g)]=l;bit.add(l,1);}prev.swap(cur);while(qi<qs.size()&&qs[qi].r==r){ans[static_cast<size_t>(qs[qi].id)]=static_cast<int>(bit.sum(r)-bit.sum(qs[qi].l-1));++qi;}}for(int x:ans)cout<<x<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}long long sum(int x)const{long long s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<long long>t;};
  struct Query{int l,r,id;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,q;while(cin>>n>>q){vector<int>a(static_cast<size_t>(n+1));for(int i=1;i<=n;++i)cin>>a[static_cast<size_t>(i)];vector<Query>qs(static_cast<size_t>(q));for(int i=0;i<q;++i){cin>>qs[static_cast<size_t>(i)].l>>qs[static_cast<size_t>(i)].r;qs[static_cast<size_t>(i)].id=i;}sort(qs.begin(),qs.end(),[](const Query&x,const Query&y){return x.r<y.r;});Fenwick bit(n);vector<int>last(1000001),ans(static_cast<size_t>(q));vector<pair<int,int>>prev;size_t qi=0;for(int r=1;r<=n;++r){vector<pair<int,int>>cur{{a[static_cast<size_t>(r)],r}};for(auto [g,l]:prev){int ng=gcd(g,a[static_cast<size_t>(r)]);if(cur.back().first==ng)cur.back().second=max(cur.back().second,l);else cur.push_back({ng,l});}for(auto [g,l]:cur){if(last[static_cast<size_t>(g)]!=0)bit.add(last[static_cast<size_t>(g)],-1);last[static_cast<size_t>(g)]=l;bit.add(l,1);}prev.swap(cur);while(qi<qs.size()&&qs[qi].r==r){ans[static_cast<size_t>(qs[qi].id)]=static_cast<int>(bit.sum(r)-bit.sum(qs[qi].l-1));++qi;}}for(int x:ans)cout<<x<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5869
external_platform: HDU
external_problem_id: '5869'
external_title: HDU 5869 Different GCD Subarray Query
external_relation: original
source_book_pages:
  - 151
  - 170
source_pdf_pages:
  - 169
  - 188
review_status: verified
---

本卡片依外部題面與限制獨立整理。
