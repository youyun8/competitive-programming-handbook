---
id: hdu-5970
volume: lower
source_file: lower-volume
source_book_pages:
  - 387
  - 461
source_pdf_pages:
  - 17
  - 91
title: HDU 5970 最大公約數
chapter: 6
section: '6.7'
kind: external-oj
difficulty: 5
topics:
  - 最大公因數
  - 數論建模
prerequisites:
  - 整除與同餘
statement: 依題目定義 f(i,j)=歐幾里得迭代次數乘 gcd(i,j)^2，求雙重總和 floor(i·j/f(i,j)) 模 p。
constraints:
  - T 組資料
  - n<=666666666，m<=666，p<=666666666
input_format: 依題目格式讀入測試組數與各組參數；多組至 EOF 或終止值者見敘述。
output_format: 依題目指定格式逐組輸出答案。
samples:
  - input: |
      1
      1 1 100
    output: |
      1
    explanation: 依定義計算或套用推導後的充要條件，可得上述結果。
core_knowledge:
  - gcd/lcm 性質
  - 等價轉換
judgment: 固定 j 與 i mod j，f 週期不變；再按歐幾里得步數 c 分成 c 條等差數列，以公式加總。
hints:
  - 先確認哪些量在一次操作或遞推中保持不變。
  - 用 gcd、質因數或同餘式將候選集合縮小。
  - 證明轉換雙向成立後，再處理邊界、溢位及固定輸出格式。
solution_outline: 固定 j 與 i mod j，f 週期不變；再按歐幾里得步數 c 分成 c 條等差數列，以公式加總。
proof_or_invariant: gcd 與歐幾里得步數在 i 增加 j 時不變；跨 c·j 後商的整數部分恰增加 c·j²/f，故每條子序列為等差數列。
complexity:
  time: 每組 O(m² log m)
  space: O(1)
common_errors:
  - 只證明必要條件而未證明充分性
  - 中間乘積溢位或負餘數未正規化
  - 漏掉最小輸入與終止條件
cpp_skeleton: |
  // TODO：依證明自行重寫核心轉換。
  #include <bits/stdc++.h>
  using namespace std;
  long long fun(long long x,long long y,long long&c){c=0;while(y){++c;long long r=x%y;x=y;y=r;}return c*x*x;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){long long n,m,mod,ans=0;cin>>n>>m>>mod;for(long long j=1;j<=m;++j)for(long long i=1;i<=j&&i<=n;++i){long long c=0,f=fun(i,j,c);for(long long k=0;k<c&&i+k*j<=n;++k){long long first=(i+k*j)*j/f,d=c*j*j/f,num=(n-i-k*j)/(c*j)+1;ans=(ans+first%mod*num%mod+(num*(num-1)/2)%mod*(d%mod))%mod;}}cout<<ans<<"\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  long long fun(long long x,long long y,long long&c){c=0;while(y){++c;long long r=x%y;x=y;y=r;}return c*x*x;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){long long n,m,mod,ans=0;cin>>n>>m>>mod;for(long long j=1;j<=m;++j)for(long long i=1;i<=j&&i<=n;++i){long long c=0,f=fun(i,j,c);for(long long k=0;k<c&&i+k*j<=n;++k){long long first=(i+k*j)*j/f,d=c*j*j/f,num=(n-i-k*j)/(c*j)+1;ans=(ans+first%mod*num%mod+(num*(num-1)/2)%mod*(d%mod))%mod;}}cout<<ans<<"\n";}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5970
external_platform: HDU
external_problem_id: '5970'
external_title: 最大公約數
external_relation: original
review_status: verified
---

本題重點是把操作轉成可驗證的數論條件。
