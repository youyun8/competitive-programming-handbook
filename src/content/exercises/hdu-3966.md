---
id: hdu-3966
volume: upper
source_file: upper-volume
title: HDU 3966 Aragorn's Story：路徑增減與單點查詢
chapter: 4
section: '4.10'
kind: external-oj
difficulty: 3
topics: [heavy-light-decomposition, fenwick-tree, path-update]
prerequisites: [heavy-light-decomposition, fenwick-tree]
statement: 多組測資。每組給一棵帶點權樹，支援 `I u v k` 將 u-v 路徑所有點加 k、`D u v k` 將路徑所有點減 k，以及 `Q u` 查詢單點目前值。
constraints: ['1 <= n <= 50000', 'm=n-1', '1 <= p <= 100000', '0 <= 初值、k <= 1000；更新後可為負']
input_format: 讀至 EOF；每組先給 n、m、p，再給 n 個初值、m 條邊與 p 個操作。
output_format: 每個 Q 操作輸出一行指定節點目前值。
samples:
  - input: |
      3 2 5
      1 2 3
      2 1
      2 3
      I 1 3 5
      Q 2
      D 1 2 2
      Q 1
      Q 3
    output: |
      7
      4
      8
    explanation: 全路徑加 5 後值為 6、7、8；再將 1-2 減 2，因此節點 1 為 4，而節點 3 保持 8。
core_knowledge: [重鏈剖分路徑拆段, Fenwick 差分區間加單點查, 多測資重置]
judgment: 路徑更新包含兩個端點；D 是減法，點權允許降為負數。
hints:
  - 查詢只問單點，因此不必維護區間和；保留初值，再另存所有更新增量即可。
  - 重鏈剖分將 u-v 路徑拆成 O(log n) 個 DFS 序區間。
  - Fenwick 對每段 `[l,r]` 在 l 加 delta、r+1 減 delta；單點前綴和就是累積增量。
solution_outline: 迭代完成重鏈剖分。每次 I/D 沿鏈跳躍，對對應 DFS 區間做差分加；Q 輸出初值加上該位置的 Fenwick 前綴和。
proof_or_invariant: 重鏈拆段完整且不重複覆蓋指定路徑。差分 Fenwick 保證位置 p 的前綴和等於所有包含 p 的更新增量總和，故加回不變的初值即為當前點權。
common_errors: [D 忘記取負值, 多組測資未清空圖與 Fenwick, 最後同鏈區間漏掉端點]
complexity: { time: '更新 O(log^2 n)，查詢 O(log n)', space: 'O(n)' }
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      int n, m, operation_count;
      while (cin >> n >> m >> operation_count) {
          // TODO：每組重新建立重鏈剖分與差分 Fenwick。
      }
      return 0;
  }
cpp_solution: |
  #include <algorithm>
  #include <iostream>
  #include <utility>
  #include <vector>
  using namespace std;

  struct Fenwick {
      vector<long long> bit;
      explicit Fenwick(int n) : bit(static_cast<size_t>(n) + 2U) {}
      void add(int index, long long value) {
          for (int n = static_cast<int>(bit.size()); index < n; index += index & -index)
              bit[static_cast<size_t>(index)] += value;
      }
      void range_add(int left, int right, long long value) {
          add(left, value);
          add(right + 1, -value);
      }
      long long point(int index) const {
          long long result = 0;
          for (; index > 0; index -= index & -index) result += bit[static_cast<size_t>(index)];
          return result;
      }
  };

  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      int n, edge_count, operation_count;
      while (cin >> n >> edge_count >> operation_count) {
          vector<long long> initial(static_cast<size_t>(n) + 1U);
          for (int node = 1; node <= n; ++node) cin >> initial[static_cast<size_t>(node)];
          vector<vector<int>> graph(static_cast<size_t>(n) + 1U);
          for (int i = 0; i < edge_count; ++i) {
              int u, v;
              cin >> u >> v;
              graph[static_cast<size_t>(u)].push_back(v);
              graph[static_cast<size_t>(v)].push_back(u);
          }
          vector<int> parent(static_cast<size_t>(n) + 1U), depth(static_cast<size_t>(n) + 1U), order{1};
          depth[1] = 1;
          for (size_t index = 0; index < order.size(); ++index) {
              const int node = order[index];
              for (int next : graph[static_cast<size_t>(node)]) {
                  if (next == parent[static_cast<size_t>(node)]) continue;
                  parent[static_cast<size_t>(next)] = node;
                  depth[static_cast<size_t>(next)] = depth[static_cast<size_t>(node)] + 1;
                  order.push_back(next);
              }
          }
          vector<int> size(static_cast<size_t>(n) + 1U, 1), heavy(static_cast<size_t>(n) + 1U);
          for (size_t index = order.size(); index-- > 0;) {
              const int node = order[index];
              for (int next : graph[static_cast<size_t>(node)]) if (parent[static_cast<size_t>(next)] == node) {
                  size[static_cast<size_t>(node)] += size[static_cast<size_t>(next)];
                  if (size[static_cast<size_t>(next)] > size[static_cast<size_t>(heavy[static_cast<size_t>(node)])])
                      heavy[static_cast<size_t>(node)] = next;
              }
          }
          vector<int> head(static_cast<size_t>(n) + 1U), position(static_cast<size_t>(n) + 1U);
          vector<pair<int, int>> stack{{1, 1}};
          int timer = 0;
          while (!stack.empty()) {
              auto [start, chain_head] = stack.back();
              stack.pop_back();
              for (int node = start; node != 0; node = heavy[static_cast<size_t>(node)]) {
                  head[static_cast<size_t>(node)] = chain_head;
                  position[static_cast<size_t>(node)] = ++timer;
                  for (int next : graph[static_cast<size_t>(node)])
                      if (parent[static_cast<size_t>(next)] == node && next != heavy[static_cast<size_t>(node)])
                          stack.push_back({next, next});
              }
          }
          Fenwick fenwick(n + 1);
          auto path_add = [&](int x, int y, long long value) {
              while (head[static_cast<size_t>(x)] != head[static_cast<size_t>(y)]) {
                  if (depth[static_cast<size_t>(head[static_cast<size_t>(x)])] <
                      depth[static_cast<size_t>(head[static_cast<size_t>(y)])]) swap(x, y);
                  fenwick.range_add(position[static_cast<size_t>(head[static_cast<size_t>(x)])],
                                    position[static_cast<size_t>(x)], value);
                  x = parent[static_cast<size_t>(head[static_cast<size_t>(x)])];
              }
              if (depth[static_cast<size_t>(x)] > depth[static_cast<size_t>(y)]) swap(x, y);
              fenwick.range_add(position[static_cast<size_t>(x)], position[static_cast<size_t>(y)], value);
          };
          while (operation_count--) {
              char operation;
              int x;
              cin >> operation >> x;
              if (operation == 'Q') {
                  cout << initial[static_cast<size_t>(x)] + fenwick.point(position[static_cast<size_t>(x)]) << '\n';
              } else {
                  int y;
                  long long value;
                  cin >> y >> value;
                  path_add(x, y, operation == 'I' ? value : -value);
              }
          }
      }
      return 0;
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3966
external_platform: HDU
external_problem_id: '3966'
external_title: Aragorn's Story
external_relation: original
source_book_pages: [277, 292]
source_pdf_pages: [295, 310]
review_status: verified
---

## 只有路徑更新、單點查詢時，樹剖加差分 Fenwick 比完整懶線段樹更短也更不易錯。

id: hdu-3966
volume: upper
source_file: upper-volume
title: HDU 3966
chapter: 4
section: '4.1'
kind: external-oj
difficulty: 3
topics: ['union-find-basic', '按秩合併', '路徑壓縮', '帶權並查集', 'suffix-array-concept']
prerequisites: ['union-find-basic', '按秩合併']
statement: 本題為線上評測系統的外部題目，重點練習 基本操作, 按秩合併, 路徑壓縮。
constraints:

- 請參閱外部題目頁面取得完整限制條件
  input_format: 請參閱外部題目頁面
  output_format: 請參閱外部題目頁面
  samples: []
  hints:
- 本題核心技巧與章節 4.1 的內容直接相關。
- 可以先回顧對應課程的 C++ 模板再著手實作。
  solution_outline: 請先閱讀對應課程的教學筆記與模板，再參考外部題目描述設計解法。
  complexity:
  time: 請參考課程對應技巧的複雜度分析
  space: 請參考課程對應技巧的複雜度分析
  external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3966
  external_platform: HDU
  external_problem_id: '3966'
  external_title: HDU 3966
  external_relation: related
  source_book_pages: [258]
  source_pdf_pages: [276]
  review_status: verified

---

維護可合併摘要，處理大量動態更新、區間查詢或樹上操作。
