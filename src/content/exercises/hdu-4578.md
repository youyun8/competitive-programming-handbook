---
id: hdu-4578
volume: upper
source_file: upper-volume
title: HDU 4578 Transformation
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - lazy-segment-tree
  - affine-transform
  - power-sums
prerequisites:
  - segment-tree
statement: 全零序列支援區間加、乘、賦值，以及查一至三次方和模 10007。
constraints:
  - n,m <= 100000
  - 測資不超過 10 組
  - p=1..3
  - 0 0 結束
input_format: 操作 1/2/3 x y c 為加/乘/賦值；4 x y p 查 p 次方和。
output_format: 每個操作 4 輸出模 10007 結果。
samples:
  - input: |
      5 5
      3 3 5 7
      1 2 4 4
      4 1 5 2
      2 2 5 8
      4 3 5 3
      0 0
    output: |
      307
      7489
    explanation: 依序套用三種仿射轉換後計算冪和。
core_knowledge: *id001
judgment: 懶線段樹維護前三次冪和與仿射標記。
hints:
  - 加、乘、賦值都可寫成 x -> ax+b；賦值就是 a=0。
  - 節點維護 S1、S2、S3，以二項式展開更新。
  - 新轉換套在舊標記後，合成為 mul*=a、add=add*a+b。
solution_outline: 懶線段樹維護前三次冪和與仿射標記。
proof_or_invariant: 冪和更新直接來自 (ax+b)^p 展開；仿射函數合成保持所有尚未下推操作的精確效果。
common_errors:
  - 懶標記合成順序錯誤
  - 閉區間端點或 0/1 起始索引混淆
  - 合併節點摘要時漏掉跨左右子樹的候選
complexity:
  time: O(m log n)
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成資料結構。
  #include <bits/stdc++.h>
  using namespace std;constexpr long long MOD=10007;struct Node{array<long long,4>s{};long long mul=1,add=0;};static vector<Node>tr;static void apply(int p,int len,long long a,long long b){a%=MOD;b%=MOD;auto old=tr[static_cast<size_t>(p)].s;tr[static_cast<size_t>(p)].s[1]=(a*old[1]+b*len)%MOD;tr[static_cast<size_t>(p)].s[2]=(a*a%MOD*old[2]+2*a%MOD*b%MOD*old[1]+b*b%MOD*len)%MOD;tr[static_cast<size_t>(p)].s[3]=(a*a%MOD*a%MOD*old[3]+3*a%MOD*a%MOD*b%MOD*old[2]+3*a%MOD*b%MOD*b%MOD*old[1]+b*b%MOD*b%MOD*len)%MOD;tr[static_cast<size_t>(p)].mul=tr[static_cast<size_t>(p)].mul*a%MOD;tr[static_cast<size_t>(p)].add=(tr[static_cast<size_t>(p)].add*a+b)%MOD;}static void push(int p,int l,int r){if(l==r)return;int m=(l+r)/2;apply(p*2,m-l+1,tr[static_cast<size_t>(p)].mul,tr[static_cast<size_t>(p)].add);apply(p*2+1,r-m,tr[static_cast<size_t>(p)].mul,tr[static_cast<size_t>(p)].add);tr[static_cast<size_t>(p)].mul=1;tr[static_cast<size_t>(p)].add=0;}static void update(int p,int l,int r,int ql,int qr,long long a,long long b){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,r-l+1,a,b);return;}push(p,l,r);int m=(l+r)/2;update(p*2,l,m,ql,qr,a,b);update(p*2+1,m+1,r,ql,qr,a,b);for(int k=1;k<=3;++k)tr[static_cast<size_t>(p)].s[static_cast<size_t>(k)]=(tr[static_cast<size_t>(p*2)].s[static_cast<size_t>(k)]+tr[static_cast<size_t>(p*2+1)].s[static_cast<size_t>(k)])%MOD;}static long long query(int p,int l,int r,int ql,int qr,int power){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].s[static_cast<size_t>(power)];push(p,l,r);int m=(l+r)/2;return(query(p*2,l,m,ql,qr,power)+query(p*2+1,m+1,r,ql,qr,power))%MOD;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m&&(n!=0||m!=0)){tr.assign(static_cast<size_t>(4*n+4),Node{});while(m--){int op,l,r,c;cin>>op>>l>>r>>c;if(op==1)update(1,1,n,l,r,1,c);else if(op==2)update(1,1,n,l,r,c,0);else if(op==3)update(1,1,n,l,r,0,c);else cout<<query(1,1,n,l,r,c)<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;constexpr long long MOD=10007;struct Node{array<long long,4>s{};long long mul=1,add=0;};static vector<Node>tr;static void apply(int p,int len,long long a,long long b){a%=MOD;b%=MOD;auto old=tr[static_cast<size_t>(p)].s;tr[static_cast<size_t>(p)].s[1]=(a*old[1]+b*len)%MOD;tr[static_cast<size_t>(p)].s[2]=(a*a%MOD*old[2]+2*a%MOD*b%MOD*old[1]+b*b%MOD*len)%MOD;tr[static_cast<size_t>(p)].s[3]=(a*a%MOD*a%MOD*old[3]+3*a%MOD*a%MOD*b%MOD*old[2]+3*a%MOD*b%MOD*b%MOD*old[1]+b*b%MOD*b%MOD*len)%MOD;tr[static_cast<size_t>(p)].mul=tr[static_cast<size_t>(p)].mul*a%MOD;tr[static_cast<size_t>(p)].add=(tr[static_cast<size_t>(p)].add*a+b)%MOD;}static void push(int p,int l,int r){if(l==r)return;int m=(l+r)/2;apply(p*2,m-l+1,tr[static_cast<size_t>(p)].mul,tr[static_cast<size_t>(p)].add);apply(p*2+1,r-m,tr[static_cast<size_t>(p)].mul,tr[static_cast<size_t>(p)].add);tr[static_cast<size_t>(p)].mul=1;tr[static_cast<size_t>(p)].add=0;}static void update(int p,int l,int r,int ql,int qr,long long a,long long b){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,r-l+1,a,b);return;}push(p,l,r);int m=(l+r)/2;update(p*2,l,m,ql,qr,a,b);update(p*2+1,m+1,r,ql,qr,a,b);for(int k=1;k<=3;++k)tr[static_cast<size_t>(p)].s[static_cast<size_t>(k)]=(tr[static_cast<size_t>(p*2)].s[static_cast<size_t>(k)]+tr[static_cast<size_t>(p*2+1)].s[static_cast<size_t>(k)])%MOD;}static long long query(int p,int l,int r,int ql,int qr,int power){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].s[static_cast<size_t>(power)];push(p,l,r);int m=(l+r)/2;return(query(p*2,l,m,ql,qr,power)+query(p*2+1,m+1,r,ql,qr,power))%MOD;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m&&(n!=0||m!=0)){tr.assign(static_cast<size_t>(4*n+4),Node{});while(m--){int op,l,r,c;cin>>op>>l>>r>>c;if(op==1)update(1,1,n,l,r,1,c);else if(op==2)update(1,1,n,l,r,c,0);else if(op==3)update(1,1,n,l,r,0,c);else cout<<query(1,1,n,l,r,c)<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4578
external_platform: HDU
external_problem_id: '4578'
external_title: HDU 4578 Transformation
external_relation: original
source_book_pages:
  - 182
  - 192
source_pdf_pages:
  - 200
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
