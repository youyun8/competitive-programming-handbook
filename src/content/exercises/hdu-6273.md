---
id: hdu-6273
volume: upper
source_file: upper-volume
title: HDU 6273 Master of GCD
chapter: 2
section: '2.6'
kind: external-oj
difficulty: 3
topics: ['差分陣列', '質因數指數']
prerequisites: []
statement: |-
  全 1 序列接受區間乘 2 或 3，求最終全體 GCD 模 998244353。
constraints:
  - 'T<=10；n,m<=100000；乘數僅 2 或 3。'
input_format: '輸入 T；每組 n m 與 m 行 l r x。'
output_format: '每組輸出 GCD 模值。'
samples:
  - input: |
      1
      5 3
      1 3 2
      3 5 2
      1 5 3
    output: |
      6
    explanation: '範例依題意逐步套用操作後得到所示結果。'
core_knowledge: ['差分陣列', '質因數指數']
judgment: '資料規模要求避免逐區間逐元素重算，應利用本節的差分、前綴或離散化技巧。'
hints:
  [
    '先把一次區間操作對邊界造成的變化寫出來。',
    '選擇能避免逐項重做的前綴／差分狀態。',
    '維護狀態不變量並特別檢查端點與輸出格式。'
  ]
solution_outline: '依核心狀態處理所有操作，再以一次掃描還原答案；完整實作見解答。'
proof_or_invariant: '每次更新只改變其影響範圍的邊界狀態；依序累積後，每個位置得到的值恰等於所有涵蓋它的操作貢獻，因此輸出正確。'
common_errors: ['端點差一', '未使用足夠寬的整數型別', '多組測資狀態未重設']
complexity:
  time: '符合官方資料範圍，至多線性或二維網格線性'
  space: '與輸入序列或網格大小同階'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：依提示完成演算法。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;long long pw(long long a,int b){long long r=1;while(b){if(b&1)r=r*a%998244353;a=a*a%998244353;b/=2;}return r;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int n,m;cin>>n>>m;vector<int>a(n+2),b(n+2);while(m--){int l,r,x;cin>>l>>r>>x;auto&d=x==2?a:b;++d[l];--d[r+1];}int x=0,y=0,p=INT_MAX,q=INT_MAX;for(int i=1;i<=n;++i){x+=a[i];y+=b[i];p=min(p,x);q=min(q,y);}cout<<pw(2,p)*pw(3,q)%998244353<<'\n';}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=6273
external_platform: HDU
external_problem_id: '6273'
external_title: 'Master of GCD'
external_relation: original
source_book_pages: [75]
source_pdf_pages: [93]
review_status: verified
---

本題以獨立敘述與 C++17 解法整理。
