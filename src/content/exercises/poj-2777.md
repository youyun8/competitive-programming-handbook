---
id: poj-2777
volume: upper
source_file: upper-volume
title: POJ 2777 Count Color
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 3
topics: &id001
  - bitmask
  - range-assignment
  - segment-tree
prerequisites:
  - segment-tree
statement: 長板初始為顏色 1，支援區間塗色與查詢區間內不同顏色數。
constraints:
  - L <= 100000
  - T <= 30
  - O <= 100000
input_format: C A B C 塗色；P A B 查詢。
output_format: 每個 P 輸出色數。
samples:
  - input: |
      5 3 4
      P 1 5
      C 2 4 2
      P 1 5
      P 2 3
    output: |
      1
      2
      1
    explanation: 塗色後全段有色一與二。
core_knowledge: *id001
judgment: 線段樹維護顏色 bitmask。
hints:
  - 顏色種類少，可用位元集合表示。
  - 合併區間只需 OR。
  - 整段塗色是覆蓋型懶標記。
solution_outline: 線段樹維護顏色 bitmask。
proof_or_invariant: 葉的位元即顏色，OR 恰為聯集；覆蓋標記保證區間全為單一顏色。
common_errors:
  - 同座標事件順序或開閉邊界處理錯誤
  - 區間為空時仍遞迴更新
  - 合併時忘記保留跨左右區間候選
complexity:
  time: O(O log L)
  space: O(L)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;static vector<unsigned long long>mask;static vector<int>tag;static void paint(int p,int l,int r,int ql,int qr,int c){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){mask[static_cast<size_t>(p)]=1ULL<<static_cast<unsigned>(c-1);tag[static_cast<size_t>(p)]=c;return;}if(tag[static_cast<size_t>(p)]!=0){int z=tag[static_cast<size_t>(p)];mask[static_cast<size_t>(p*2)]=mask[static_cast<size_t>(p*2+1)]=1ULL<<static_cast<unsigned>(z-1);tag[static_cast<size_t>(p*2)]=tag[static_cast<size_t>(p*2+1)]=z;tag[static_cast<size_t>(p)]=0;}int m=(l+r)/2;paint(p*2,l,m,ql,qr,c);paint(p*2+1,m+1,r,ql,qr,c);mask[static_cast<size_t>(p)]=mask[static_cast<size_t>(p*2)]|mask[static_cast<size_t>(p*2+1)];}static unsigned long long ask(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return mask[static_cast<size_t>(p)];if(tag[static_cast<size_t>(p)]!=0)return mask[static_cast<size_t>(p)];int m=(l+r)/2;return ask(p*2,l,m,ql,qr)|ask(p*2+1,m+1,r,ql,qr);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int L,T,O;while(cin>>L>>T>>O){mask.assign(static_cast<size_t>(4*L+4),1);tag.assign(static_cast<size_t>(4*L+4),1);while(O--){char op;int a,b;cin>>op>>a>>b;if(a>b)swap(a,b);if(op=='C'){int c;cin>>c;paint(1,1,L,a,b,c);}else cout<<__builtin_popcountll(ask(1,1,L,a,b))<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;static vector<unsigned long long>mask;static vector<int>tag;static void paint(int p,int l,int r,int ql,int qr,int c){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){mask[static_cast<size_t>(p)]=1ULL<<static_cast<unsigned>(c-1);tag[static_cast<size_t>(p)]=c;return;}if(tag[static_cast<size_t>(p)]!=0){int z=tag[static_cast<size_t>(p)];mask[static_cast<size_t>(p*2)]=mask[static_cast<size_t>(p*2+1)]=1ULL<<static_cast<unsigned>(z-1);tag[static_cast<size_t>(p*2)]=tag[static_cast<size_t>(p*2+1)]=z;tag[static_cast<size_t>(p)]=0;}int m=(l+r)/2;paint(p*2,l,m,ql,qr,c);paint(p*2+1,m+1,r,ql,qr,c);mask[static_cast<size_t>(p)]=mask[static_cast<size_t>(p*2)]|mask[static_cast<size_t>(p*2+1)];}static unsigned long long ask(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return mask[static_cast<size_t>(p)];if(tag[static_cast<size_t>(p)]!=0)return mask[static_cast<size_t>(p)];int m=(l+r)/2;return ask(p*2,l,m,ql,qr)|ask(p*2+1,m+1,r,ql,qr);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int L,T,O;while(cin>>L>>T>>O){mask.assign(static_cast<size_t>(4*L+4),1);tag.assign(static_cast<size_t>(4*L+4),1);while(O--){char op;int a,b;cin>>op>>a>>b;if(a>b)swap(a,b);if(op=='C'){int c;cin>>c;paint(1,1,L,a,b,c);}else cout<<__builtin_popcountll(ask(1,1,L,a,b))<<'\n';}}}
external_url: http://poj.org/problem?id=2777
external_platform: POJ
external_problem_id: '2777'
external_title: POJ 2777 Count Color
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
