---
id: hdu-4028
volume: upper
source_file: upper-volume
title: HDU 4028 The time of a day
chapter: 2
section: '2.7'
kind: external-oj
difficulty: 4
topics: ['離散狀態 DP', '最小公倍數']
prerequisites: []
statement: |-
  指針 i 的週期為 i 秒；從前 n 個指針選非空子集，計算 LCM 不小於 M 的方案數。
constraints:
  - 'n<=40；1<=M<=2^63-1。'
input_format: '依上列敘述順序輸入所有參數與操作。'
output_format: '依題意輸出每組答案。'
samples:
  - input: |
      2
      2 1
      3 3
    output: |
      Case #1: 3
      Case #2: 4
    explanation: '依操作或定義直接計算可得此結果。'
core_knowledge: ['離散狀態 DP', '最小公倍數']
judgment: '座標或狀態範圍大，但實際事件有限，應用本節離散化、差分或狀態壓縮。'
hints:
  [
    '先找出真正會改變答案的事件或邊界。',
    '將大座標／大量區間轉成有限狀態後依序處理。',
    '維持狀態不變量，最後掃描所有候選並取最優。'
  ]
solution_outline: '依核心知識壓縮資料，再按事件順序更新並輸出答案。'
proof_or_invariant: '壓縮後每個原始事件仍有唯一對應，且事件間狀態不變；逐段計算涵蓋所有候選，因此所得值與原問題相同。'
common_errors: ['閉區間端點處理錯誤', '多組資料未清空', '計數或面積使用 int 溢位']
complexity:
  time: 'O(n log n) 或與壓縮後網格大小同階'
  space: 'O(n) 或與壓縮後網格大小同階'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：依提示完成。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;int main(){ios::sync_with_stdio(false);cin.tie(nullptr);vector<map<unsigned long long,unsigned long long>>dp(41);dp[0][1]=1;for(int i=1;i<=40;++i){dp[i]=dp[i-1];for(auto [v,c]:dp[i-1])dp[i][lcm(v,static_cast<unsigned long long>(i))]+=c;}int T;cin>>T;for(int cs=1;cs<=T;++cs){int n;unsigned long long m;cin>>n>>m,cout<<"Case #"<<cs<<": ";unsigned long long ans=0;for(auto [v,c]:dp[n])if(v>=m)ans+=c;if(m<=1)--ans;cout<<ans<<'\n';}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4028
external_platform: HDU
external_problem_id: '4028'
external_title: 'The time of a day'
external_relation: original
source_book_pages: [78]
source_pdf_pages: [96]
review_status: verified
---

本題採獨立敘述與 C++17 解法。
