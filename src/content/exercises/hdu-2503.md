---
id: hdu-2503
volume: lower
source_file: lower-volume
source_book_pages:
  - 416
source_pdf_pages:
  - 46
title: HDU 2503 a/b + c/d
chapter: 6
section: '6.7'
kind: external-oj
difficulty: 1
topics:
  - 分數約分
  - 最大公因數
prerequisites:
  - 最大公因數與整除
statement: 計算兩個分數 a/b 與 c/d 的和，並輸出最簡分數。
constraints:
  - 輸入整數均為正且乘加結果可用 64 位元有號整數表示
input_format: 第一行為 T；每組四個整數 a、b、c、d。
output_format: 每組輸出最簡分子與分母，以空格分隔。
samples:
  - input: |
      2
      1 2 1 3
      1 2 1 2
    output: |
      5 6
      1 1
    explanation: 第一組通分得 5/6；第二組為 1。
core_knowledge:
  - 通分
  - gcd 約分
judgment: 和的分子為 a·d+b·c，分母為 b·d，再同除 gcd。
hints:
  - 先用交叉相乘建立共同分母。
  - 分子與分母可能超過 32 位元。
  - 最後同除兩者的 gcd。
solution_outline: 和的分子為 a·d+b·c，分母為 b·d，再同除 gcd。
proof_or_invariant: 通分保持兩分數值不變；除以分子分母的 gcd 後值不變且兩者互質，故為唯一最簡形式。
complexity:
  time: 每組 O(log V)
  space: O(1)
common_errors:
  - 交叉項寫錯
  - 只約分原本兩個分數
cpp_skeleton: |
  // TODO：理解證明後，可嘗試自行重寫核心步驟。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){long long a,b,c,d;cin>>a>>b>>c>>d;long long x=a*d+b*c,y=b*d,g=gcd(x,y);cout<<x/g<<' '<<y/g<<"\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){long long a,b,c,d;cin>>a>>b>>c>>d;long long x=a*d+b*c,y=b*d,g=gcd(x,y);cout<<x/g<<' '<<y/g<<"\n";}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2503
external_platform: HDU
external_problem_id: '2503'
external_title: a/b + c/d
external_relation: original
review_status: verified
---

本題以可驗證的數論性質化簡後實作。
