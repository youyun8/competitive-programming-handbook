---
id: luogu-p4705
volume: lower
source_file: lower-volume
title: 洛谷 P4705 隨機兩數和的各階動差
chapter: 7
section: '7.8'
kind: external-oj
difficulty: 9
topics: [expectation, power-sum, formal-power-series, ntt]
prerequisites: [generating-functions, ntt, polynomial-log]
statement: Alice、Bob 分別從長 n、m 的序列均勻隨機取一數 a_x、b_y。對 i=1..t，求 (a_x+b_y)^i 的期望，所有運算模 998244353。
constraints: [1 <= n, m, t <= 100000, 0 <= a_i, b_i < 998244353]
input_format: 第一行 n、m；第二行 n 個 a_i；第三行 m 個 b_i；第四行 t。
output_format: 共 t 行，第 i 行輸出 i 次價值的期望。
samples:
  - input: |-
      1 1
      1
      2
      3
    output: |-
      3
      9
      27
    explanation: 唯一可能的和是 3，所以各階動差為 3、9、27。
core_knowledge: [二項式展開, 冪和生成函數, 乘積多項式取對數, EGF 卷積]
judgment: 直接枚舉 nmt 不可行；只要分別求兩序列前 t 階冪和，二項式展開就化成一次卷積。
hints:
  - 令 P(x)=Π_j(1-a_jx)，則 -xP'(x)/P(x)=Σ_(k>=1)(Σ_j a_j^k)x^k。
  - 把第 k 階冪和除以 k!，二項式係數就消失成普通卷積。
  - 兩序列卷積後乘回 k!，再除以 nm。
solution_outline: 分治 NTT 建立兩個 Π(1-vx)，全程截斷到 t+1；用多項式 ln 的係數取出冪和。將 power_sum[k]/k! 組成兩多項式（第零項分別是 n、m），卷積後第 k 項乘 k!/(nm) 即答案。
proof_or_invariant: log Π(1-vx)=Σlog(1-vx)=-Σ_(k>=1)(Σv^k)x^k/k，故可精確恢復所有冪和。二項式定理給 Σ_x,y(a_x+b_y)^k=Σ_l C(k,l)A_lB_(k-l)；除階乘後右側正是兩序列的普通卷積。最後除樣本對數 nm 即均勻期望。
common_errors: [第零階冪和誤設為一而非序列長度, log 係數漏乘負 k, 卷積後漏乘 k!, 忘記除 nm]
complexity: { time: 'O((n+m+t) log^2(n+m+t))', space: 'O(n+m+t)' }
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main() { int n, m; cin >> n >> m; /* TODO: 乘積多項式取 log 與 EGF 卷積。 */ return 0; }
cpp_solution: |
  #include <algorithm>
  #include <functional>
  #include <iostream>
  #include <vector>
  using namespace std;
  constexpr int mod_value = 998244353;
  constexpr int primitive_root = 3;
  int power_mod(int base, int exponent) {
      long long result = 1, value = base;
      while (exponent > 0) {
          if ((exponent & 1) != 0) result = result * value % mod_value;
          value = value * value % mod_value;
          exponent >>= 1;
      }
      return static_cast<int>(result);
  }
  void ntt(vector<int> &a, bool inverse) {
      const int n = static_cast<int>(a.size());
      for (int i = 1, j = 0; i < n; ++i) {
          int bit = n >> 1;
          while ((j & bit) != 0) { j ^= bit; bit >>= 1; }
          j ^= bit;
          if (i < j) swap(a[static_cast<size_t>(i)], a[static_cast<size_t>(j)]);
      }
      for (int len = 2; len <= n; len <<= 1) {
          int root = power_mod(primitive_root, (mod_value - 1) / len);
          if (inverse) root = power_mod(root, mod_value - 2);
          for (int start = 0; start < n; start += len) {
              long long factor = 1;
              for (int j = 0; j < len / 2; ++j) {
                  const int u = a[static_cast<size_t>(start + j)];
                  const int v = static_cast<int>(
                      factor * a[static_cast<size_t>(start + j + len / 2)] % mod_value);
                  int low = u + v; if (low >= mod_value) low -= mod_value;
                  int high = u - v; if (high < 0) high += mod_value;
                  a[static_cast<size_t>(start + j)] = low;
                  a[static_cast<size_t>(start + j + len / 2)] = high;
                  factor = factor * root % mod_value;
              }
          }
      }
      if (inverse) {
          const int inv_n = power_mod(n, mod_value - 2);
          for (int &value : a)
              value = static_cast<int>(static_cast<long long>(value) * inv_n % mod_value);
      }
  }
  vector<int> multiply(vector<int> a, vector<int> b, int need) {
      if (need <= 0 || a.empty() || b.empty()) return {};
      const int result_size = min(need, static_cast<int>(a.size() + b.size() - 1U));
      int size = 1;
      while (size < static_cast<int>(a.size() + b.size() - 1U)) size <<= 1;
      a.resize(static_cast<size_t>(size)); b.resize(static_cast<size_t>(size));
      ntt(a, false); ntt(b, false);
      for (int i = 0; i < size; ++i)
          a[static_cast<size_t>(i)] = static_cast<int>(
              static_cast<long long>(a[static_cast<size_t>(i)]) * b[static_cast<size_t>(i)] % mod_value);
      ntt(a, true);
      a.resize(static_cast<size_t>(result_size));
      return a;
  }
  vector<int> inverse_series(const vector<int> &a, int need) {
      vector<int> result{power_mod(a[0], mod_value - 2)};
      while (static_cast<int>(result.size()) < need) {
          const int target = min(need, 2 * static_cast<int>(result.size()));
          vector<int> prefix(a.begin(), a.begin() + min(target, static_cast<int>(a.size())));
          vector<int> correction = multiply(prefix, result, target);
          correction.resize(static_cast<size_t>(target));
          for (int &value : correction) value = value == 0 ? 0 : mod_value - value;
          correction[0] = (correction[0] + 2) % mod_value;
          result = multiply(result, correction, target);
          result.resize(static_cast<size_t>(target));
      }
      return result;
  }
  vector<int> logarithm(const vector<int> &a, int need) {
      if (need == 1) return {0};
      vector<int> derivative(static_cast<size_t>(need - 1));
      for (int i = 1; i < need && i < static_cast<int>(a.size()); ++i)
          derivative[static_cast<size_t>(i - 1)] =
              static_cast<int>(static_cast<long long>(a[static_cast<size_t>(i)]) * i % mod_value);
      const vector<int> product = multiply(derivative, inverse_series(a, need), need - 1);
      vector<int> result(static_cast<size_t>(need));
      for (int i = 1; i < need; ++i)
          result[static_cast<size_t>(i)] = static_cast<int>(
              static_cast<long long>(product[static_cast<size_t>(i - 1)]) *
              power_mod(i, mod_value - 2) % mod_value);
      return result;
  }
  vector<int> product_polynomial(const vector<int> &values, int need) {
      function<vector<int>(int, int)> build = [&](int left, int right) -> vector<int> {
          if (left == right) {
              const int negative = values[static_cast<size_t>(left)] == 0 ? 0 :
                  mod_value - values[static_cast<size_t>(left)];
              return {1, negative};
          }
          const int middle = (left + right) / 2;
          return multiply(build(left, middle), build(middle + 1, right), need);
      };
      vector<int> result = build(0, static_cast<int>(values.size()) - 1);
      result.resize(static_cast<size_t>(need));
      return result;
  }
  vector<int> normalized_power_sums(const vector<int> &values, int t,
                                    const vector<int> &inverse_factorial) {
      const vector<int> product = product_polynomial(values, t + 1);
      const vector<int> log_product = logarithm(product, t + 1);
      vector<int> result(static_cast<size_t>(t) + 1U);
      result[0] = static_cast<int>(values.size());
      for (int i = 1; i <= t; ++i) {
          const int power_sum = log_product[static_cast<size_t>(i)] == 0 ? 0 :
              mod_value - static_cast<int>(
                  static_cast<long long>(log_product[static_cast<size_t>(i)]) * i % mod_value);
          result[static_cast<size_t>(i)] = static_cast<int>(
              static_cast<long long>(power_sum) * inverse_factorial[static_cast<size_t>(i)] %
              mod_value);
      }
      return result;
  }
  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      int n;
      int m;
      cin >> n >> m;
      vector<int> a(static_cast<size_t>(n));
      vector<int> b(static_cast<size_t>(m));
      for (int &value : a) cin >> value;
      for (int &value : b) cin >> value;
      int t;
      cin >> t;
      vector<int> factorial(static_cast<size_t>(t) + 1U, 1);
      vector<int> inverse_factorial(static_cast<size_t>(t) + 1U, 1);
      for (int i = 1; i <= t; ++i)
          factorial[static_cast<size_t>(i)] = static_cast<int>(
              static_cast<long long>(factorial[static_cast<size_t>(i - 1)]) * i % mod_value);
      inverse_factorial[static_cast<size_t>(t)] =
          power_mod(factorial[static_cast<size_t>(t)], mod_value - 2);
      for (int i = t; i > 0; --i)
          inverse_factorial[static_cast<size_t>(i - 1)] = static_cast<int>(
              static_cast<long long>(inverse_factorial[static_cast<size_t>(i)]) * i % mod_value);
      const vector<int> first = normalized_power_sums(a, t, inverse_factorial);
      const vector<int> second = normalized_power_sums(b, t, inverse_factorial);
      const vector<int> convolution = multiply(first, second, t + 1);
      const int inverse_pairs = power_mod(static_cast<int>(
          static_cast<long long>(n) * m % mod_value), mod_value - 2);
      for (int i = 1; i <= t; ++i)
          cout << static_cast<int>(
              static_cast<long long>(convolution[static_cast<size_t>(i)]) *
              factorial[static_cast<size_t>(i)] % mod_value * inverse_pairs % mod_value) << '\n';
      return 0;
  }
external_url: https://www.luogu.com.cn/problem/P4705
external_platform: 洛谷
external_problem_id: P4705
external_title: 玩遊戲
external_relation: original
source_book_pages: [501]
source_pdf_pages: [131]
review_status: verified
---

乘積多項式的對數一次取出所有冪和，再由 EGF 卷積完成全部動差。
