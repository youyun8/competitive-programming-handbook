---
id: hdu-1823
volume: upper
source_file: upper-volume
title: HDU 1823 Luck and Love
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - two-dimensional-segment-tree
  - point-update
  - rectangle-maximum
prerequisites:
  - segment-tree
statement: 插入身高 H、活潑度 A、緣分 L；查指定 H、A 矩形內最大 L。
constraints:
  - 100 <= H <= 200
  - 0.0 <= A,L <= 100.0
  - 皆一位小數
  - m=0 結束
input_format: I H A L 插入；Q H1 H2 A1 A2 查詢。
output_format: 有答案輸出一位小數，否則 -1。
samples:
  - input: |
      4
      I 160 50.5 60.0
      I 165 30.0 80.5
      Q 150 166 10.0 60.0
      Q 170 180 0.0 100.0
      0
    output: |
      80.5
      -1
    explanation: 第一矩形含兩筆且最大 80.5；第二矩形無資料。
core_knowledge: *id001
judgment: 線段樹套線段樹處理二維單點取 max 與矩形 max。
hints:
  - 把一位小數乘十轉整數座標。
  - 外層身高線段樹每個節點配置一棵活潑度最大值樹。
  - 插入沿身高路徑更新內樹；查詢分解身高區間並查各內樹。
solution_outline: 線段樹套線段樹處理二維單點取 max 與矩形 max。
proof_or_invariant: 每筆插入更新恰含其身高的所有外節點；查詢外節點不重不漏分割身高範圍，內查詢再限制活潑度。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O(log H log A) 每操作
  space: O(HA)
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;constexpr int HN=101,AN=1001;static vector<int>tr;static size_t idx(int hp,int ap){return static_cast<size_t>(hp)*(4*AN)+static_cast<size_t>(ap);}static void inner_update(int hp,int ap,int l,int r,int pos,int value){int&cell=tr[idx(hp,ap)];cell=max(cell,value);if(l==r)return;int m=(l+r)/2;if(pos<=m)inner_update(hp,ap*2,l,m,pos,value);else inner_update(hp,ap*2+1,m+1,r,pos,value);}static int inner_query(int hp,int ap,int l,int r,int ql,int qr){if(qr<l||r<ql)return -1;if(ql<=l&&r<=qr)return tr[idx(hp,ap)];int m=(l+r)/2;return max(inner_query(hp,ap*2,l,m,ql,qr),inner_query(hp,ap*2+1,m+1,r,ql,qr));}static void update(int hp,int l,int r,int h,int a,int love){inner_update(hp,1,0,1000,a,love);if(l==r)return;int m=(l+r)/2;if(h<=m)update(hp*2,l,m,h,a,love);else update(hp*2+1,m+1,r,h,a,love);}static int query(int hp,int l,int r,int h1,int h2,int a1,int a2){if(h2<l||r<h1)return -1;if(h1<=l&&r<=h2)return inner_query(hp,1,0,1000,a1,a2);int m=(l+r)/2;return max(query(hp*2,l,m,h1,h2,a1,a2),query(hp*2+1,m+1,r,h1,h2,a1,a2));}static int scaled(const string&s){size_t p=s.find('.');int whole=stoi(p==string::npos?s:s.substr(0,p));int tenth=p==string::npos?0:s[p+1]-'0';return whole*10+tenth;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int m;while(cin>>m&&m!=0){tr.assign(static_cast<size_t>(4*HN*4*AN),-1);while(m--){char op;cin>>op;if(op=='I'){int h;string a,love;cin>>h>>a>>love;update(1,100,200,h,scaled(a),scaled(love));}else{int h1,h2;string a1s,a2s;cin>>h1>>h2>>a1s>>a2s;if(h1>h2)swap(h1,h2);int a1=scaled(a1s),a2=scaled(a2s);if(a1>a2)swap(a1,a2);int ans=query(1,100,200,h1,h2,a1,a2);if(ans<0)cout<<-1<<'\n';else cout<<ans/10<<'.'<<ans%10<<'\n';}}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;constexpr int HN=101,AN=1001;static vector<int>tr;static size_t idx(int hp,int ap){return static_cast<size_t>(hp)*(4*AN)+static_cast<size_t>(ap);}static void inner_update(int hp,int ap,int l,int r,int pos,int value){int&cell=tr[idx(hp,ap)];cell=max(cell,value);if(l==r)return;int m=(l+r)/2;if(pos<=m)inner_update(hp,ap*2,l,m,pos,value);else inner_update(hp,ap*2+1,m+1,r,pos,value);}static int inner_query(int hp,int ap,int l,int r,int ql,int qr){if(qr<l||r<ql)return -1;if(ql<=l&&r<=qr)return tr[idx(hp,ap)];int m=(l+r)/2;return max(inner_query(hp,ap*2,l,m,ql,qr),inner_query(hp,ap*2+1,m+1,r,ql,qr));}static void update(int hp,int l,int r,int h,int a,int love){inner_update(hp,1,0,1000,a,love);if(l==r)return;int m=(l+r)/2;if(h<=m)update(hp*2,l,m,h,a,love);else update(hp*2+1,m+1,r,h,a,love);}static int query(int hp,int l,int r,int h1,int h2,int a1,int a2){if(h2<l||r<h1)return -1;if(h1<=l&&r<=h2)return inner_query(hp,1,0,1000,a1,a2);int m=(l+r)/2;return max(query(hp*2,l,m,h1,h2,a1,a2),query(hp*2+1,m+1,r,h1,h2,a1,a2));}static int scaled(const string&s){size_t p=s.find('.');int whole=stoi(p==string::npos?s:s.substr(0,p));int tenth=p==string::npos?0:s[p+1]-'0';return whole*10+tenth;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int m;while(cin>>m&&m!=0){tr.assign(static_cast<size_t>(4*HN*4*AN),-1);while(m--){char op;cin>>op;if(op=='I'){int h;string a,love;cin>>h>>a>>love;update(1,100,200,h,scaled(a),scaled(love));}else{int h1,h2;string a1s,a2s;cin>>h1>>h2>>a1s>>a2s;if(h1>h2)swap(h1,h2);int a1=scaled(a1s),a2=scaled(a2s);if(a1>a2)swap(a1,a2);int ans=query(1,100,200,h1,h2,a1,a2);if(ans<0)cout<<-1<<'\n';else cout<<ans/10<<'.'<<ans%10<<'\n';}}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1823
external_platform: HDU
external_problem_id: '1823'
external_title: HDU 1823 Luck and Love
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
