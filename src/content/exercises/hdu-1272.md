---
id: hdu-1272
volume: upper
source_file: upper-volume
title: HDU 1272 小希的迷宮：無向樹判定
chapter: 4
section: '4.1'
kind: external-oj
difficulty: 3
topics: &id001
  - disjoint-set-union
  - tree-recognition
  - cycle-detection
prerequisites:
  - disjoint-set-union
statement: 每條通道雙向。判斷每組設計是否讓任意兩個出現的房間之間恰有一條簡單路徑；空設計也視為合法。
constraints:
  - 房間編號 1 到 100000
  - 每組以 0 0 結束；全部輸入以 -1 -1 結束
input_format: 連續讀取房間對；0 0 分隔測資，-1 -1 結束。
output_format: 每組輸出 Yes 或 No。
samples:
  - input: |
      6 8 5 3 5 2 6 4 5 6 0 0
      8 1 7 3 6 2 8 9 7 5 7 4 7 8 7 6 0 0
      3 8 6 8 6 4 5 3 5 6 5 2 0 0
      -1 -1
    output: |
      Yes
      Yes
      No
    explanation: 前兩組連通且無環；第三組存在替代路徑形成環。
core_knowledge: *id001
judgment: 合法樹必須同時無環且所有出現節點連通；空圖依題意為樹。
hints:
  - 加入邊時兩端若已同根便形成環。
  - 沒有環仍可能是多棵樹組成的森林。
  - 資料結束時再確認出現節點只有一個根。
solution_outline: 每組逐邊合併並記錄環，最後統計出現節點的代表元數。
proof_or_invariant: 無向有限圖為樹當且僅當連通且無環；並查集合併失敗精確偵測環，根數精確偵測連通性。
common_errors:
  - 只判斷無環而接受森林
  - 把空測資判 No
  - 混淆 0 0 與 -1 -1
complexity:
  time: O(E alpha(V))
  space: O(V)
cpp_skeleton: |
  // TODO：先自行重建核心不變量，再與下列可編譯框架比較。
  #include <bits/stdc++.h>
  using namespace std;
  class DisjointSet{public:explicit DisjointSet(int n):p(static_cast<size_t>(n+1)){iota(p.begin(),p.end(),0);}int root(int x){return p[static_cast<size_t>(x)]==x?x:p[static_cast<size_t>(x)]=root(p[static_cast<size_t>(x)]);}bool unite(int a,int b){a=root(a);b=root(b);if(a==b)return false;p[static_cast<size_t>(a)]=b;return true;}private:vector<int>p;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);vector<pair<int,int>> edges;int a,b;while(cin>>a>>b){if(a==-1&&b==-1)break;if(a==0&&b==0){if(edges.empty()){cout<<"Yes\n";continue;}int mx=0;for(auto e:edges)mx=max(mx,max(e.first,e.second));DisjointSet dsu(mx);set<int> nodes;bool ok=true;for(auto e:edges){nodes.insert(e.first);nodes.insert(e.second);if(!dsu.unite(e.first,e.second))ok=false;}int roots=0;for(int x:nodes)if(dsu.root(x)==x)++roots;cout<<(ok&&roots==1?"Yes":"No")<<'\n';edges.clear();}else edges.push_back({a,b});}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  class DisjointSet{public:explicit DisjointSet(int n):p(static_cast<size_t>(n+1)){iota(p.begin(),p.end(),0);}int root(int x){return p[static_cast<size_t>(x)]==x?x:p[static_cast<size_t>(x)]=root(p[static_cast<size_t>(x)]);}bool unite(int a,int b){a=root(a);b=root(b);if(a==b)return false;p[static_cast<size_t>(a)]=b;return true;}private:vector<int>p;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);vector<pair<int,int>> edges;int a,b;while(cin>>a>>b){if(a==-1&&b==-1)break;if(a==0&&b==0){if(edges.empty()){cout<<"Yes\n";continue;}int mx=0;for(auto e:edges)mx=max(mx,max(e.first,e.second));DisjointSet dsu(mx);set<int> nodes;bool ok=true;for(auto e:edges){nodes.insert(e.first);nodes.insert(e.second);if(!dsu.unite(e.first,e.second))ok=false;}int roots=0;for(int x:nodes)if(dsu.root(x)==x)++roots;cout<<(ok&&roots==1?"Yes":"No")<<'\n';edges.clear();}else edges.push_back({a,b});}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1272
external_platform: HDU
external_problem_id: '1272'
external_title: 小希的迷宮
external_relation: original
source_book_pages:
  - 151
  - 314
source_pdf_pages:
  - 169
  - 332
review_status: verified
---

本題卡片依官方題面重新敘述，程式採 C++17。
