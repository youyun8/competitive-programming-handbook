---
id: luogu-p5395
volume: lower
source_file: lower-volume
title: 洛谷 P5395 第二類 Stirling 數整行
chapter: 7
section: '7.6'
kind: external-oj
difficulty: 5
topics: [stirling-number, inclusion-exclusion, ntt]
prerequisites: [catalan-stirling]
statement: 給 n，輸出把 n 個不同元素分成 k 個非空無標號集合的方案數 S(n,k)，其中 k=0..n，答案模 167772161。
constraints: [1 <= n < 262144]
input_format: 一個正整數 n。
output_format: 一行 n+1 個整數，依序為 S(n,0)..S(n,n) 模 167772161。
samples:
  - input: '3'
    output: '0 1 3 1'
    explanation: 三元素分成一、二、三個非空集合的方案數分別為 1、3、1。
core_knowledge: [滿射容斥公式, 將整行寫成卷積, NTT]
judgment: 集合本身無標號，因此滿射數要除以 k!。
hints:
  - S(n,k)=1/k!·Σ_(j=0..k)(-1)^(k-j)C(k,j)j^n。
  - 展開組合數後，令 A_i=(-1)^i/i!、B_j=j^n/j!，答案是 A*B 的第 k 項。
  - 模數為 5·2^25+1，原根 3，可直接 NTT。
solution_outline: 預處理階乘逆元，建立 A、B 至 n，做一次 NTT 卷積並輸出前 n+1 項。
proof_or_invariant: 先為 k 個有標號盒做容斥，排除空盒後再除 k! 忘掉盒標號。把 C(k,j)/k!=1/(j!(k-j)!) 代入並令 i=k-j，恰為兩序列卷積。
common_errors: [使用 998244353 而非題目模數, 0^0 處理錯誤, NTT 長度不足而發生循環卷積]
complexity: { time: 'O(n log n)', space: 'O(n)' }
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main() { int n; cin >> n; /* TODO: 容斥序列 NTT 卷積。 */ }
cpp_solution: |
  #include <algorithm>
  #include <iostream>
  #include <vector>
  using namespace std;
  constexpr int mod_value = 167772161;
  constexpr int primitive_root = 3;
  int power_mod(int base, int exponent) {
      long long result = 1, value = base;
      while (exponent > 0) {
          if ((exponent & 1) != 0) result = result * value % mod_value;
          value = value * value % mod_value;
          exponent >>= 1;
      }
      return static_cast<int>(result);
  }
  void ntt(vector<int> &values, bool inverse) {
      const int size = static_cast<int>(values.size());
      for (int i = 1, j = 0; i < size; ++i) {
          int bit = size >> 1;
          for (; (j & bit) != 0; bit >>= 1) j ^= bit;
          j ^= bit;
          if (i < j) swap(values[static_cast<size_t>(i)], values[static_cast<size_t>(j)]);
      }
      for (int length = 2; length <= size; length <<= 1) {
          int root = power_mod(primitive_root, (mod_value - 1) / length);
          if (inverse) root = power_mod(root, mod_value - 2);
          for (int start = 0; start < size; start += length) {
              long long factor = 1;
              for (int offset = 0; offset < length / 2; ++offset) {
                  const int even = values[static_cast<size_t>(start + offset)];
                  const int odd = static_cast<int>(
                      factor * values[static_cast<size_t>(start + offset + length / 2)] % mod_value);
                  int &low = values[static_cast<size_t>(start + offset)];
                  int &high = values[static_cast<size_t>(start + offset + length / 2)];
                  low = even + odd;
                  if (low >= mod_value) low -= mod_value;
                  high = even - odd;
                  if (high < 0) high += mod_value;
                  factor = factor * root % mod_value;
              }
          }
      }
      if (inverse) {
          const int inverse_size = power_mod(size, mod_value - 2);
          for (int &value : values)
              value = static_cast<int>(static_cast<long long>(value) * inverse_size % mod_value);
      }
  }
  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      int n;
      cin >> n;
      vector<int> factorial(static_cast<size_t>(n) + 1U, 1);
      vector<int> inverse_factorial(static_cast<size_t>(n) + 1U);
      for (int i = 1; i <= n; ++i)
          factorial[static_cast<size_t>(i)] =
              static_cast<int>(static_cast<long long>(factorial[static_cast<size_t>(i - 1)]) * i % mod_value);
      inverse_factorial[static_cast<size_t>(n)] =
          power_mod(factorial[static_cast<size_t>(n)], mod_value - 2);
      for (int i = n; i > 0; --i)
          inverse_factorial[static_cast<size_t>(i - 1)] =
              static_cast<int>(static_cast<long long>(inverse_factorial[static_cast<size_t>(i)]) * i % mod_value);
      int size = 1;
      while (size <= 2 * n) size <<= 1;
      vector<int> first(static_cast<size_t>(size));
      vector<int> second(static_cast<size_t>(size));
      for (int i = 0; i <= n; ++i) {
          first[static_cast<size_t>(i)] =
              (i & 1) != 0 ? mod_value - inverse_factorial[static_cast<size_t>(i)]
                           : inverse_factorial[static_cast<size_t>(i)];
          second[static_cast<size_t>(i)] =
              static_cast<int>(static_cast<long long>(power_mod(i, n)) *
                               inverse_factorial[static_cast<size_t>(i)] % mod_value);
      }
      ntt(first, false);
      ntt(second, false);
      for (int i = 0; i < size; ++i)
          first[static_cast<size_t>(i)] =
              static_cast<int>(static_cast<long long>(first[static_cast<size_t>(i)]) *
                               second[static_cast<size_t>(i)] % mod_value);
      ntt(first, true);
      for (int i = 0; i <= n; ++i)
          cout << first[static_cast<size_t>(i)] << (i == n ? '\n' : ' ');
      return 0;
  }
external_url: https://www.luogu.com.cn/problem/P5395
external_platform: 洛谷
external_problem_id: P5395
external_title: 第二類斯特林數·行
external_relation: original
source_book_pages: [484]
source_pdf_pages: [114]
review_status: verified
---

整行 Stirling 數的容斥式具有卷積形狀，因此一次 NTT 即可全部求出。
