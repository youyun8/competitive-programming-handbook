---
id: hdu-3255
volume: upper
source_file: upper-volume
title: HDU 3255 Farming
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - layer-cake
  - scanline
  - weighted-union
prerequisites:
  - segment-tree
statement: 矩形地塊各播一種種子；重疊處只留下價格最高者，求總收入。
constraints:
  - T <= 10
  - N <= 30000
  - 種類 M <= 3
  - 價格 <= 100
input_format: 每組 N M、M 個價格、N 個 x1 y1 x2 y2 s。
output_format: '輸出 Case k: 收入。'
samples:
  - input: |
      1
      2 2
      2 5
      0 0 2 2 1
      1 1 3 3 2
    output: |
      Case 1: 26
    explanation: 高價區收入20，低價獨有三格收入6。
core_knowledge: *id001
judgment: 至多三次面積聯集，套用 layer-cake 分解。
hints:
  - 把價格視為第三維高度。
  - 按價格層級，統計價格至少為該層的矩形聯集面積。
  - 每層面積乘相鄰價格差後相加。
solution_outline: 至多三次面積聯集，套用 layer-cake 分解。
proof_or_invariant: 任一平面點被計入從零到其最高價格的每個價格薄層，層貢獻總和恰為該點最高價格。
common_errors:
  - 端點、開閉區間或 0/1 起始索引處理錯誤
  - 合併摘要時遺漏跨左右區間的候選
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(MN log N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;
  struct R{long long x1,y1,x2,y2;};struct E{long long y,x1,x2;int d;};struct Node{int c=0;long long len=0;};static long long union_area(const vector<R>&rect){vector<E>e;vector<long long>xs;for(auto r:rect)if(r.x1<r.x2&&r.y1<r.y2){e.push_back({r.y1,r.x1,r.x2,1});e.push_back({r.y2,r.x1,r.x2,-1});xs.push_back(r.x1);xs.push_back(r.x2);}if(e.empty())return 0;sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(e.begin(),e.end(),[](const E&a,const E&b){return a.y<b.y;});vector<Node>tr(xs.size()*4+4);function<void(int,int,int)>pull=[&](int p,int l,int r){if(tr[static_cast<size_t>(p)].c>0)tr[static_cast<size_t>(p)].len=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)];else if(l==r)tr[static_cast<size_t>(p)].len=0;else tr[static_cast<size_t>(p)].len=tr[static_cast<size_t>(p*2)].len+tr[static_cast<size_t>(p*2+1)].len;};function<void(int,int,int,int,int,int)>upd=[&](int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].c+=d;else{int m=(l+r)/2;upd(p*2,l,m,ql,qr,d);upd(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);};long long ans=0;for(size_t i=0;i<e.size();++i){int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x1)-xs.begin()),r=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x2)-xs.begin())-1;upd(1,0,static_cast<int>(xs.size())-2,l,r,e[i].d);if(i+1<e.size())ans+=tr[1].len*(e[i+1].y-e[i].y);}return ans;}
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;for(int cs=1;cs<=T;++cs){int n,m;cin>>n>>m;vector<int>price(static_cast<size_t>(m+1));for(int i=1;i<=m;++i)cin>>price[static_cast<size_t>(i)];struct Plot{R r;int value;};vector<Plot>plots;for(int i=0;i<n;++i){long long x1,y1,x2,y2;int s;cin>>x1>>y1>>x2>>y2>>s;plots.push_back({{x1,y1,x2,y2},price[static_cast<size_t>(s)]});}vector<int>levels(price.begin()+1,price.end());sort(levels.begin(),levels.end());long long ans=0;int previous=0;for(int level:levels){vector<R>active;for(auto p:plots)if(p.value>=level)active.push_back(p.r);ans+=union_area(active)*static_cast<long long>(level-previous);previous=level;}cout<<"Case "<<cs<<": "<<ans<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct R{long long x1,y1,x2,y2;};struct E{long long y,x1,x2;int d;};struct Node{int c=0;long long len=0;};static long long union_area(const vector<R>&rect){vector<E>e;vector<long long>xs;for(auto r:rect)if(r.x1<r.x2&&r.y1<r.y2){e.push_back({r.y1,r.x1,r.x2,1});e.push_back({r.y2,r.x1,r.x2,-1});xs.push_back(r.x1);xs.push_back(r.x2);}if(e.empty())return 0;sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(e.begin(),e.end(),[](const E&a,const E&b){return a.y<b.y;});vector<Node>tr(xs.size()*4+4);function<void(int,int,int)>pull=[&](int p,int l,int r){if(tr[static_cast<size_t>(p)].c>0)tr[static_cast<size_t>(p)].len=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)];else if(l==r)tr[static_cast<size_t>(p)].len=0;else tr[static_cast<size_t>(p)].len=tr[static_cast<size_t>(p*2)].len+tr[static_cast<size_t>(p*2+1)].len;};function<void(int,int,int,int,int,int)>upd=[&](int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].c+=d;else{int m=(l+r)/2;upd(p*2,l,m,ql,qr,d);upd(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);};long long ans=0;for(size_t i=0;i<e.size();++i){int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x1)-xs.begin()),r=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x2)-xs.begin())-1;upd(1,0,static_cast<int>(xs.size())-2,l,r,e[i].d);if(i+1<e.size())ans+=tr[1].len*(e[i+1].y-e[i].y);}return ans;}
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;for(int cs=1;cs<=T;++cs){int n,m;cin>>n>>m;vector<int>price(static_cast<size_t>(m+1));for(int i=1;i<=m;++i)cin>>price[static_cast<size_t>(i)];struct Plot{R r;int value;};vector<Plot>plots;for(int i=0;i<n;++i){long long x1,y1,x2,y2;int s;cin>>x1>>y1>>x2>>y2>>s;plots.push_back({{x1,y1,x2,y2},price[static_cast<size_t>(s)]});}vector<int>levels(price.begin()+1,price.end());sort(levels.begin(),levels.end());long long ans=0;int previous=0;for(int level:levels){vector<R>active;for(auto p:plots)if(p.value>=level)active.push_back(p.r);ans+=union_area(active)*static_cast<long long>(level-previous);previous=level;}cout<<"Case "<<cs<<": "<<ans<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3255
external_platform: HDU
external_problem_id: '3255'
external_title: HDU 3255 Farming
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
