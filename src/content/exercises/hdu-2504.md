---
id: hdu-2504
volume: lower
source_file: lower-volume
source_book_pages:
  - 416
source_pdf_pages:
  - 46
title: HDU 2504 又⻅GCD
chapter: 6
section: '6.7'
kind: external-oj
difficulty: 1
topics:
  - 最大公因數
  - 數論建模
prerequisites:
  - 整除與同餘
statement: 已知正整數 a、b，求最小且不等於 b 的正整數 c，使 gcd(a,c)=b。
constraints:
  - 0<a,b,c<10^6
  - 輸入保證存在答案
input_format: 依題目格式讀入測試組數與各組參數；多組至 EOF 或終止值者見敘述。
output_format: 依題目指定格式逐組輸出答案。
samples:
  - input: |
      2
      6 2
      12 4
    output: |
      4
      8
    explanation: 依定義計算或套用推導後的充要條件，可得上述結果。
core_knowledge:
  - gcd/lcm 性質
  - 等價轉換
judgment: 令 c=bk，條件等價於 gcd(a/b,k)=1；由 k=2 起找第一個互質者。
hints:
  - 先確認哪些量在一次操作或遞推中保持不變。
  - 用 gcd、質因數或同餘式將候選集合縮小。
  - 證明轉換雙向成立後，再處理邊界、溢位及固定輸出格式。
solution_outline: 令 c=bk，條件等價於 gcd(a/b,k)=1；由 k=2 起找第一個互質者。
proof_or_invariant: 任何可行 c 必為 b 的倍數；縮放 gcd 後得到充要條件，遞增枚舉首次成功即最小。
complexity:
  time: 每組 O(answer/b · log a)
  space: O(1)
common_errors:
  - 只證明必要條件而未證明充分性
  - 中間乘積溢位或負餘數未正規化
  - 漏掉最小輸入與終止條件
cpp_skeleton: |
  // TODO：依證明自行重寫核心轉換。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int a,b;cin>>a>>b;for(int k=2;;++k)if(gcd(a/b,k)==1){cout<<1LL*b*k<<"\n";break;}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int a,b;cin>>a>>b;for(int k=2;;++k)if(gcd(a/b,k)==1){cout<<1LL*b*k<<"\n";break;}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2504
external_platform: HDU
external_problem_id: '2504'
external_title: 又⻅GCD
external_relation: original
review_status: verified
---

本題重點是把操作轉成可驗證的數論條件。
