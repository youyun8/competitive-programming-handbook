---
id: hdu-5057
volume: upper
source_file: upper-volume
title: HDU 5057 Argestes and Sequence：動態數位區間計數
chapter: 4
section: '4.2'
kind: external-oj
difficulty: 4
topics: &id001
  - offline-processing
  - fenwick-tree
  - digit-extraction
prerequisites:
  - fenwick-tree
statement: 序列支援單點賦值；查詢 [L,R] 中第 D 個十進位數位（最低位為第 1 位）等於 P 的元素數。
constraints:
  - T <= 50
  - N,M <= 100000
  - 數值為非負 32 位元整數
  - 1 <= D <= 10
input_format: 每組 N M、初值；S X Y 賦值或 Q L R D P 查詢。
output_format: 依序輸出 Q 答案。
samples:
  - input: |
      1
      3 3
      11 12 23
      Q 1 3 1 3
      S 2 13
      Q 1 3 1 3
    output: |
      1
      2
    explanation: 初始個位為 3 的只有 23；更新後 13 也符合。
core_knowledge: *id001
judgment: 對每個數位重播操作，以含十種計數的 BIT 回答。
hints:
  - 直接開 100 棵 BIT 會超出記憶體。
  - 離線保存操作，逐一處理十個數位位置。
  - 每一輪重播所有更新，只回答 D 等於本輪的查詢。
solution_outline: 對每個數位重播操作，以含十種計數的 BIT 回答。
proof_or_invariant: 每輪 BIT 精確維護該時刻每位置的指定數位；所有查詢恰在自己的 D 輪回答，時間順序不變。
common_errors:
  - 索引基準或閉區間端點處理錯誤
  - 更新資料結構後忘記同步原始狀態
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(10(N+M) log N)
  space: O(N+M)
cpp_skeleton: |
  // TODO：先依三階段提示自行完成核心；以下框架可嚴格編譯。
  #include <bits/stdc++.h>
  using namespace std;
  struct Op{char type;int a,b,d,p;};class DigitFenwick{public:explicit DigitFenwick(int n):n_(n),t(static_cast<size_t>((n+1)*10)){}void add(int x,int digit,int v){for(int i=x;i<=n_;i+=i&-i)t[static_cast<size_t>(i*10+digit)]+=v;}int sum(int x,int digit)const{int s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i*10+digit)];return s;}private:int n_;vector<int>t;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;vector<int>initial(static_cast<size_t>(n+1));for(int i=1;i<=n;++i)cin>>initial[static_cast<size_t>(i)];vector<Op>ops(static_cast<size_t>(m));for(auto&o:ops){cin>>o.type;if(o.type=='S'){cin>>o.a>>o.b;o.d=o.p=0;}else cin>>o.a>>o.b>>o.d>>o.p;}vector<int>answer(static_cast<size_t>(m),-1);long long power=1;for(int digit=1;digit<=10;++digit){DigitFenwick bit(n);vector<int>current=initial;for(int i=1;i<=n;++i)bit.add(i,static_cast<int>(current[static_cast<size_t>(i)]/power%10),1);for(int i=0;i<m;++i){Op o=ops[static_cast<size_t>(i)];if(o.type=='S'){bit.add(o.a,static_cast<int>(current[static_cast<size_t>(o.a)]/power%10),-1);current[static_cast<size_t>(o.a)]=o.b;bit.add(o.a,static_cast<int>(current[static_cast<size_t>(o.a)]/power%10),1);}else if(o.d==digit)answer[static_cast<size_t>(i)]=bit.sum(o.b,o.p)-bit.sum(o.a-1,o.p);}power*=10;}for(int x:answer)if(x>=0)cout<<x<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Op{char type;int a,b,d,p;};class DigitFenwick{public:explicit DigitFenwick(int n):n_(n),t(static_cast<size_t>((n+1)*10)){}void add(int x,int digit,int v){for(int i=x;i<=n_;i+=i&-i)t[static_cast<size_t>(i*10+digit)]+=v;}int sum(int x,int digit)const{int s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i*10+digit)];return s;}private:int n_;vector<int>t;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;vector<int>initial(static_cast<size_t>(n+1));for(int i=1;i<=n;++i)cin>>initial[static_cast<size_t>(i)];vector<Op>ops(static_cast<size_t>(m));for(auto&o:ops){cin>>o.type;if(o.type=='S'){cin>>o.a>>o.b;o.d=o.p=0;}else cin>>o.a>>o.b>>o.d>>o.p;}vector<int>answer(static_cast<size_t>(m),-1);long long power=1;for(int digit=1;digit<=10;++digit){DigitFenwick bit(n);vector<int>current=initial;for(int i=1;i<=n;++i)bit.add(i,static_cast<int>(current[static_cast<size_t>(i)]/power%10),1);for(int i=0;i<m;++i){Op o=ops[static_cast<size_t>(i)];if(o.type=='S'){bit.add(o.a,static_cast<int>(current[static_cast<size_t>(o.a)]/power%10),-1);current[static_cast<size_t>(o.a)]=o.b;bit.add(o.a,static_cast<int>(current[static_cast<size_t>(o.a)]/power%10),1);}else if(o.d==digit)answer[static_cast<size_t>(i)]=bit.sum(o.b,o.p)-bit.sum(o.a-1,o.p);}power*=10;}for(int x:answer)if(x>=0)cout<<x<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5057
external_platform: HDU
external_problem_id: '5057'
external_title: HDU 5057 Argestes and Sequence
external_relation: original
source_book_pages:
  - 170
source_pdf_pages:
  - 188
review_status: verified
---

本卡片依外部題面與限制獨立整理。
