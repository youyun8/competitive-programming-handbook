---
id: hdu-4421
volume: lower
source_file: lower-volume
original_label: HDU 4421
title: HDU 4421 Bit Magic：逐位 2-SAT
chapter: 10
section: '10.7'
kind: external-oj
difficulty: 4
topics: [2-SAT, 位元運算, 強連通分量]
prerequisites: [directed-connectivity]
core_knowledge: [逐位獨立, AND OR XOR 真值限制, 禁止組合建邊]
judgment: 31 個二進位位彼此獨立；每一位上每個 a_i 只有 0、1 兩種狀態。
statement: 給定矩陣 b，判斷是否存在整數陣列 a，使不同索引奇偶組合分別以 AND、OR、XOR 產生 b；完整運算定義以原題程式為準。
constraints: ['n <= 500', '0 <= b[i][j] < 2^31', '多組直到 EOF']
input_format: 每組先給 n，再給 n×n 整數矩陣。
output_format: 存在陣列輸出 YES，否則 NO。
samples:
  - input: |-
      2
      0 1
      1 0
    output: 'YES'
    explanation: 取 a 的最低位分別為 0、1，異奇偶索引間 XOR 為 1，對角依題意為 0。
hints:
  - 先檢查矩陣對稱且對角為 0。
  - 對每一位獨立建立 n 個布林變數；依索引奇偶決定 AND、OR 或 XOR。
  - 將不允許的取值組合 (x_i=p,x_j=q) 加成兩條蘊含：該組合不能同時成立。
solution_outline: 驗證矩陣基本性質；逐位枚舉所有 i<j，按運算真值表加入禁止組合，Tarjan 判每一位的 2-SAT，任一位無解即 NO。
proof_or_invariant: 位元運算不跨位進位，因此整數解存在當且僅當 31 個位元公式皆可滿足。每個禁止組合等價於二元子句 (x_i≠p)∨(x_j≠q)，兩條蘊含完全等價；SCC 判定給出每位充要條件。
complexity: { time: 'O(31n^2)', space: 'O(n^2)' }
common_errors: [未先檢查對稱與對角, AND 與 OR 的索引奇偶條件寫反, 強制單一值時只加一個方向]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){int n;while(cin>>n){/* TODO：逐位真值表建 2-SAT。 */}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){
      ios::sync_with_stdio(false);cin.tie(nullptr);
      int n;while(cin>>n){vector<vector<unsigned int>> b(static_cast<size_t>(n),vector<unsigned int>(static_cast<size_t>(n)));for(auto& row:b)for(unsigned int& x:row)cin>>x;bool possible=true;
          for(int i=0;i<n;++i){if(b[static_cast<size_t>(i)][static_cast<size_t>(i)]!=0)possible=false;for(int j=i+1;j<n;++j)if(b[static_cast<size_t>(i)][static_cast<size_t>(j)]!=b[static_cast<size_t>(j)][static_cast<size_t>(i)])possible=false;}
          for(int bit=0;bit<31&&possible;++bit){vector<vector<int>> graph(static_cast<size_t>(2*n));auto forbid=[&](int i,int x,int j,int y){graph[static_cast<size_t>(2*i+x)].push_back(2*j+(y^1));graph[static_cast<size_t>(2*j+y)].push_back(2*i+(x^1));};
              for(int i=0;i<n;++i)for(int j=i+1;j<n;++j){int value=static_cast<int>((b[static_cast<size_t>(i)][static_cast<size_t>(j)]>>bit)&1U);if(i%2==0&&j%2==0){if(value==1){forbid(i,0,i,0);forbid(j,0,j,0);}else forbid(i,1,j,1);}else if(i%2==1&&j%2==1){if(value==0){forbid(i,1,i,1);forbid(j,1,j,1);}else forbid(i,0,j,0);}else{if(value==0){forbid(i,0,j,1);forbid(i,1,j,0);}else{forbid(i,0,j,0);forbid(i,1,j,1);}}}
              vector<int> dfn(static_cast<size_t>(2*n)),low(static_cast<size_t>(2*n)),component(static_cast<size_t>(2*n),-1),stack_nodes;vector<char> on_stack(static_cast<size_t>(2*n));int timer=0,count=0;auto dfs=[&](auto&& self,int u)->void{dfn[static_cast<size_t>(u)]=low[static_cast<size_t>(u)]=++timer;stack_nodes.push_back(u);on_stack[static_cast<size_t>(u)]=1;for(int v:graph[static_cast<size_t>(u)])if(!dfn[static_cast<size_t>(v)]){self(self,v);low[static_cast<size_t>(u)]=min(low[static_cast<size_t>(u)],low[static_cast<size_t>(v)]);}else if(on_stack[static_cast<size_t>(v)])low[static_cast<size_t>(u)]=min(low[static_cast<size_t>(u)],dfn[static_cast<size_t>(v)]);if(low[static_cast<size_t>(u)]==dfn[static_cast<size_t>(u)]){while(true){int v=stack_nodes.back();stack_nodes.pop_back();on_stack[static_cast<size_t>(v)]=0;component[static_cast<size_t>(v)]=count;if(v==u)break;}++count;}};
              for(int u=0;u<2*n;++u){if(!dfn[static_cast<size_t>(u)])dfs(dfs,u);}
              for(int i=0;i<n;++i){if(component[static_cast<size_t>(2*i)]==component[static_cast<size_t>(2*i+1)])possible=false;}
          }
          cout<<(possible?"YES":"NO")<<'\n';
      }
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4421
external_platform: HDU
external_problem_id: '4421'
external_title: Bit Magic
external_relation: original
source_book_pages: [600, 683]
source_pdf_pages: [230, 313]
review_status: verified
---

位元運算矩陣看似龐大，但拆成 31 個互不相干的布林可滿足性問題後結構非常規整。
