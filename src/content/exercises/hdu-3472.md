---
id: hdu-3472
volume: lower
source_file: lower-volume
title: 'HDU 3472 HS BDC：混合圖歐拉路與最大流'
chapter: 10
section: '10.10'
kind: external-oj
difficulty: 5
topics: ['混合圖歐拉路', '最大流', '連通性']
prerequisites: ['max-flow']
statement: '每個單字視為首字母到尾字母的邊；部分單字可反轉。判斷能否排列全部單字，使相鄰首尾字母相接。'
constraints: ['T<=50', 'N<=1000', '單字長度<20', 'type 0 不可反轉、1 可反轉']
input_format: 'T；每組 N，接著 N 行 word、type。'
output_format: '`Case t: Well done!` 或 `Case t: Poor boy!`。'
samples:
  - input: |
      1
      2
      ab 0
      bc 0
    output: |
      Case 1: Well done!
    explanation: '兩條固定方向邊可排列成 ab、bc 的歐拉路。 此例已以窮舉可行路徑、割或配置的獨立小資料程式對拍。'
core_knowledge: ['weak connectivity', 'degree balancing', 'orientation by max flow']
judgment: '每字只用一次即歐拉路；可反轉單字是尚待定向的無向邊。'
hints:
  - '先忽略方向檢查所有活躍字母弱連通。'
  - '任意定向可反轉邊；反轉 u->v 會令 balance[u] 減 2、balance[v] 加 2。'
  - '枚舉歐拉路起終點（或回路），以單位容量邊做平衡流，檢查供給是否滿流。'
solution_outline: '讀入時依原方向算 out-in 並保存可反轉邊。弱連通後，嘗試回路及所有起終點目標 balance；對差值除二建立流網，任一滿流即成功。'
proof_or_invariant: '固定起終點後，歐拉度數條件指定目標 balance。每單位經可反轉邊 u->v 的流恰代表把該邊反向，精確修正兩端各 2；滿流等價於存在滿足度數的定向。弱連通加歐拉度數條件即存在使用所有邊的歐拉路。'
common_errors:
  ['只檢查奇度數而不驗證可反轉邊能否實現方向', '漏弱連通', '反轉對 balance 的改變誤作 1', 'Case 固定字串錯誤']
complexity: { time: 'O(26^2 * flow(28,N))', space: 'O(N)' }
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;

  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依三階段提示完成建模與演算法。 */}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Dinic {
      struct Edge { int to; long long capacity; };
      vector<Edge> edges; vector<vector<int>> graph; vector<int> level; vector<size_t> current;
      explicit Dinic(int n):graph(static_cast<size_t>(n)),level(static_cast<size_t>(n)),current(static_cast<size_t>(n)){}
      int add_edge(int u,int v,long long c){int id=static_cast<int>(edges.size());graph[static_cast<size_t>(u)].push_back(id);edges.push_back({v,c});graph[static_cast<size_t>(v)].push_back(id+1);edges.push_back({u,0});return id;}
      bool bfs(int s,int t){fill(level.begin(),level.end(),-1);queue<int>q;q.push(s);level[static_cast<size_t>(s)]=0;while(!q.empty()){int u=q.front();q.pop();for(int id:graph[static_cast<size_t>(u)]){const auto&e=edges[static_cast<size_t>(id)];if(e.capacity>0&&level[static_cast<size_t>(e.to)]<0){level[static_cast<size_t>(e.to)]=level[static_cast<size_t>(u)]+1;q.push(e.to);}}}return level[static_cast<size_t>(t)]>=0;}
      long long dfs(int u,int t,long long limit){if(u==t)return limit;for(size_t&i=current[static_cast<size_t>(u)];i<graph[static_cast<size_t>(u)].size();++i){int id=graph[static_cast<size_t>(u)][i];auto&e=edges[static_cast<size_t>(id)];if(e.capacity<=0||level[static_cast<size_t>(e.to)]!=level[static_cast<size_t>(u)]+1)continue;long long sent=dfs(e.to,t,min(limit,e.capacity));if(sent){e.capacity-=sent;edges[static_cast<size_t>(id^1)].capacity+=sent;return sent;}}return 0;}
      long long max_flow(int s,int t){long long result=0;while(bfs(s,t)){fill(current.begin(),current.end(),0);while(long long sent=dfs(s,t,LLONG_MAX/4))result+=sent;}return result;}
  };
  struct Dsu{array<int,26>p{};Dsu(){iota(p.begin(),p.end(),0);}int find(int x){return p[static_cast<size_t>(x)]==x?x:p[static_cast<size_t>(x)]=find(p[static_cast<size_t>(x)]);}void unite(int x,int y){p[static_cast<size_t>(find(x))]=find(y);}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n;cin>>n;array<int,26>balance{};array<char,26>active{};vector<pair<int,int>>reversible;Dsu dsu;for(int i=0;i<n;++i){string word;int type;cin>>word>>type;int u=word.front()-'a',v=word.back()-'a';++balance[static_cast<size_t>(u)];--balance[static_cast<size_t>(v)];active[static_cast<size_t>(u)]=active[static_cast<size_t>(v)]=1;dsu.unite(u,v);if(type==1)reversible.push_back({u,v});}bool connected=true;int root=-1;for(int i=0;i<26;++i)if(active[static_cast<size_t>(i)]){if(root<0)root=dsu.find(i);else if(dsu.find(i)!=root)connected=false;}auto feasible=[&](int start,int finish){array<int,26>target{};if(start>=0){++target[static_cast<size_t>(start)];--target[static_cast<size_t>(finish)];}int source=26,sink=27;Dinic flow(28);long long demand=0;for(auto [u,v]:reversible)flow.add_edge(u,v,1);for(int i=0;i<26;++i){int difference=balance[static_cast<size_t>(i)]-target[static_cast<size_t>(i)];if((difference&1)!=0)return false;if(difference>0){flow.add_edge(source,i,difference/2);demand+=difference/2;}else if(difference<0)flow.add_edge(i,sink,-difference/2);}return flow.max_flow(source,sink)==demand;};bool ok=false;if(connected){ok=feasible(-1,-1);for(int start=0;start<26&&!ok;++start)if(active[static_cast<size_t>(start)])for(int finish=0;finish<26&&!ok;++finish)if(start!=finish&&active[static_cast<size_t>(finish)])ok=feasible(start,finish);}cout<<"Case "<<cs<<": "<<(ok?"Well done!":"Poor boy!")<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3472
external_platform: HDU
external_problem_id: '3472'
external_title: 'HS BDC'
external_relation: original
source_book_pages: [663, 675]
source_pdf_pages: [293, 305]
review_status: verified
---

本卡片依官方題面或可信原賽事存檔獨立整理，未以 OCR 猜測題意。
