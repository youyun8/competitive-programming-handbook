---
id: 洛谷-p3199
volume: lower
source_file: lower-volume
source_book_pages:
  - 413
source_pdf_pages:
  - 43
title: Luogu P3199 最小圈
chapter: 6
section: '6.6'
kind: external-oj
difficulty: 5
topics:
  - 線性基
  - 分數規劃
prerequisites:
  - GF(2) 與二分答案
statement: 給定帶實數邊權的有向圖，求所有有向環中平均邊權的最小值。
constraints:
  - 1<=n<=3000，1<=m<=10000
input_format: 依題意讀入測試組、陣列或圖資料。
output_format: 逐組輸出指定答案。
samples:
  - input: |
      2 2
      1 2 1
      2 1 3
    output: |
      2.00000000
    explanation: 直接列舉小型案例或依判定式計算可得。
core_knowledge:
  - 等價判定
  - 線性獨立或負環
judgment: 二分平均值 x，將每邊減 x；存在平均小於 x 的環當且僅當新圖存在負環，以 Bellman-Ford 第 n 輪是否仍鬆弛判斷。
hints:
  - 先把目標改寫成線性獨立或「答案至少為 x」的判定。
  - 找出判定中需要最大化的加總量。
  - 使用線性基、背包或負環檢測完成判定，並處理精度。
solution_outline: 二分平均值 x，將每邊減 x；存在平均小於 x 的環當且僅當新圖存在負環，以 Bellman-Ford 第 n 輪是否仍鬆弛判斷。
proof_or_invariant: 環的新權和為原權和-x·邊數，為負恰等價於原平均小於 x；此性質對 x 單調。
complexity:
  time: O(nm log precision)
  space: O(n+m)
common_errors:
  - 線性基主元方向錯誤
  - 二分可行方向顛倒
  - 64 位元或浮點精度不足
cpp_skeleton: |
  // TODO：依提示重寫核心資料結構。
  #include <bits/stdc++.h>
  using namespace std;
  struct E{int u,v;double w;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;cin>>n>>m;vector<E>e(m);double l=1e9,r=-1e9;for(auto&x:e){cin>>x.u>>x.v>>x.w;l=min(l,x.w);r=max(r,x.w);}auto neg=[&](double x){vector<double>d(n+1);for(int k=1;k<=n;++k){bool any=false;for(auto q:e)if(d[q.v]>d[q.u]+q.w-x){d[q.v]=d[q.u]+q.w-x;any=true;if(k==n)return true;}if(!any)return false;}return false;};for(int z=0;z<70;++z){double x=(l+r)/2;if(neg(x))r=x;else l=x;}cout<<fixed<<setprecision(8)<<r<<"\n";}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct E{int u,v;double w;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;cin>>n>>m;vector<E>e(m);double l=1e9,r=-1e9;for(auto&x:e){cin>>x.u>>x.v>>x.w;l=min(l,x.w);r=max(r,x.w);}auto neg=[&](double x){vector<double>d(n+1);for(int k=1;k<=n;++k){bool any=false;for(auto q:e)if(d[q.v]>d[q.u]+q.w-x){d[q.v]=d[q.u]+q.w-x;any=true;if(k==n)return true;}if(!any)return false;}return false;};for(int z=0;z<70;++z){double x=(l+r)/2;if(neg(x))r=x;else l=x;}cout<<fixed<<setprecision(8)<<r<<"\n";}
external_url: https://www.luogu.com.cn/problem/P3199
external_platform: Luogu
external_problem_id: P3199
external_title: 最小圈
external_relation: original
review_status: verified
---

以代數結構或單調判定取代直接枚舉。
