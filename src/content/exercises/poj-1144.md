---
id: poj-1144
volume: lower
source_file: lower-volume
original_label: OpenJ_Bailian 1144
title: OpenJudge 1144 Network
chapter: 10
section: '10.4'
kind: external-oj
difficulty: 3
topics: [articulation-point, low-link]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  給定無向通訊網路，求移除後會使網路分裂的節點數。
constraints: [1 <= n <= 100, 鄰接資料以單獨的 0 結束, 所有案例以 n=0 結束]
input_format: >-
  每組先給 n；其後每行先給 u，再列出與 u 相鄰的所有 v；單行 0 結束。
output_format: >-
  每組輸出割點數量。
samples:
  - input: |
      5
      1 2 3
      3 4 5
      0
      0
    output: |
      2
    explanation: >-
      移除 1 或 3 都會增加連通塊數。
core_knowledge: [割點, DFS 樹, low-link]
judgment: >-
  非根 u 有子樹 v 滿足 low[v]>=dfn[u] 即為割點；根需至少兩棵 DFS 子樹。
hints:
  - 以整行解析不定長鄰接表。
  - 根節點另計 DFS 子樹數。
  - 同一割點只計一次。
solution_outline: >-
  對每個未訪點做 Tarjan 割點 DFS，最後統計標記。
proof_or_invariant: >-
  low[v]>=dfn[u] 表示 v 子樹無法繞過 u 到祖先，移除 u 必分裂；若小於則有替代路。根無祖先，恰在至少兩棵子樹時分裂。
complexity: { time: 'O(n + m)', space: 'O(n + m)' }
common_errors: [根套用非根公式, 一個割點按子樹重複計數, 把輸入每行誤認只有一條邊]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;string line;while(cin>>n&&n){getline(cin,line);vector<vector<int>>g(n);while(getline(cin,line)&&line!="0"){istringstream in(line);int u,v;in>>u;while(in>>v)g[u-1].push_back(v-1),g[v-1].push_back(u-1);}vector<int>d(n),low(n);vector<char>cut(n);int timer=0;auto dfs=[&](auto&&self,int u,int parent)->void{d[u]=low[u]=++timer;int child=0;for(int v:g[u])if(v!=parent){if(!d[v]){++child;self(self,v,u);low[u]=min(low[u],low[v]);if(parent>=0&&low[v]>=d[u])cut[u]=1;}else low[u]=min(low[u],d[v]);}if(parent<0&&child>1)cut[u]=1;};for(int i=0;i<n;++i)if(!d[i])dfs(dfs,i,-1);cout<<count(cut.begin(),cut.end(),1)<<'\n';}}
external_url: http://bailian.openjudge.cn/practice/1144/
external_platform: OpenJ_Bailian
external_problem_id: '1144'
external_title: 'Network'
external_relation: original
source_book_pages: [617]
source_pdf_pages: [247]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
