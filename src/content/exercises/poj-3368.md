---
id: poj-3368
volume: upper
source_file: upper-volume
title: POJ 3368 Frequent values：有序序列區間眾數頻率
chapter: 4
section: '4.2'
kind: external-oj
difficulty: 4
topics: &id001
  - run-length-encoding
  - sparse-table
  - range-mode
prerequisites:
  - fenwick-tree
statement: 非遞減整數序列中，多次查詢區間內任何值的最大出現次數。
constraints:
  - n,q <= 100000
  - 序列非遞減
  - n=0 結束
input_format: 每組 n q、n 個值、q 個 l r。
output_format: 每問輸出最高頻率。
samples:
  - input: |
      10 3
      -1 -1 1 1 1 1 3 10 10 10
      2 3
      1 10
      5 10
      0
    output: |
      1
      4
      3
    explanation: 完整區間中值 1 出現四次；末區間值 10 出現三次。
core_knowledge: *id001
judgment: 壓縮等值段並對段長建立 Sparse Table。
hints:
  - 非遞減使同值形成單一連續 run。
  - 查詢答案來自左右殘缺 run 或中間完整 run。
  - 完整 run 長度用 RMQ 取最大值。
solution_outline: 壓縮等值段並對段長建立 Sparse Table。
proof_or_invariant: 區間與 run 的交集只有左右兩段可能不完整，其餘完整；三者取最大涵蓋所有候選。
common_errors:
  - 索引基準或閉區間端點處理錯誤
  - 更新資料結構後忘記同步原始狀態
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(n log n+q)
  space: O(n log n)
cpp_skeleton: |
  // TODO：先依三階段提示自行完成核心；以下框架可嚴格編譯。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,q;while(cin>>n&&n!=0){cin>>q;vector<int>a(static_cast<size_t>(n+1)),rid(static_cast<size_t>(n+1)),start(1),finish(1),len(1);int runs=0;for(int i=1;i<=n;++i){cin>>a[static_cast<size_t>(i)];if(i==1||a[static_cast<size_t>(i)]!=a[static_cast<size_t>(i-1)]){++runs;start.push_back(i);finish.push_back(i);len.push_back(0);}rid[static_cast<size_t>(i)]=runs;finish[static_cast<size_t>(runs)]=i;++len[static_cast<size_t>(runs)];}int levels=1;while((1<<levels)<=runs)++levels;vector<vector<int>>sp(static_cast<size_t>(levels),vector<int>(static_cast<size_t>(runs+1)));sp[0]=len;for(int k=1;k<levels;++k)for(int i=1;i+(1<<k)-1<=runs;++i)sp[static_cast<size_t>(k)][static_cast<size_t>(i)]=max(sp[static_cast<size_t>(k-1)][static_cast<size_t>(i)],sp[static_cast<size_t>(k-1)][static_cast<size_t>(i+(1<<(k-1)))]);auto range_max=[&](int l,int r){if(l>r)return 0;int k=31-__builtin_clz(r-l+1);return max(sp[static_cast<size_t>(k)][static_cast<size_t>(l)],sp[static_cast<size_t>(k)][static_cast<size_t>(r-(1<<k)+1)]);};while(q--){int l,r;cin>>l>>r;int left_run=rid[static_cast<size_t>(l)],right_run=rid[static_cast<size_t>(r)];if(left_run==right_run)cout<<r-l+1<<'\n';else cout<<max({finish[static_cast<size_t>(left_run)]-l+1,r-start[static_cast<size_t>(right_run)]+1,range_max(left_run+1,right_run-1)})<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,q;while(cin>>n&&n!=0){cin>>q;vector<int>a(static_cast<size_t>(n+1)),rid(static_cast<size_t>(n+1)),start(1),finish(1),len(1);int runs=0;for(int i=1;i<=n;++i){cin>>a[static_cast<size_t>(i)];if(i==1||a[static_cast<size_t>(i)]!=a[static_cast<size_t>(i-1)]){++runs;start.push_back(i);finish.push_back(i);len.push_back(0);}rid[static_cast<size_t>(i)]=runs;finish[static_cast<size_t>(runs)]=i;++len[static_cast<size_t>(runs)];}int levels=1;while((1<<levels)<=runs)++levels;vector<vector<int>>sp(static_cast<size_t>(levels),vector<int>(static_cast<size_t>(runs+1)));sp[0]=len;for(int k=1;k<levels;++k)for(int i=1;i+(1<<k)-1<=runs;++i)sp[static_cast<size_t>(k)][static_cast<size_t>(i)]=max(sp[static_cast<size_t>(k-1)][static_cast<size_t>(i)],sp[static_cast<size_t>(k-1)][static_cast<size_t>(i+(1<<(k-1)))]);auto range_max=[&](int l,int r){if(l>r)return 0;int k=31-__builtin_clz(r-l+1);return max(sp[static_cast<size_t>(k)][static_cast<size_t>(l)],sp[static_cast<size_t>(k)][static_cast<size_t>(r-(1<<k)+1)]);};while(q--){int l,r;cin>>l>>r;int left_run=rid[static_cast<size_t>(l)],right_run=rid[static_cast<size_t>(r)];if(left_run==right_run)cout<<r-l+1<<'\n';else cout<<max({finish[static_cast<size_t>(left_run)]-l+1,r-start[static_cast<size_t>(right_run)]+1,range_max(left_run+1,right_run-1)})<<'\n';}}}
external_url: http://poj.org/problem?id=3368
external_platform: POJ
external_problem_id: '3368'
external_title: POJ 3368 Frequent values
external_relation: original
source_book_pages:
  - 151
  - 170
source_pdf_pages:
  - 169
  - 188
review_status: verified
---

本卡片依外部題面與限制獨立整理。
