---
id: hdu-3065
volume: lower
source_file: lower-volume
title: HDU 3065 病毒侵襲持續中
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [aho-corasick, trie, failure-link]
prerequisites: [kmp, trie]
statement: '給定互異的大寫病毒碼與一段可見 ASCII 網站源碼，輸出每種病毒的重疊出現次數。'
constraints:
  - '1 <= n <= 1000'
  - '病毒碼長度 1 到 50且只含大寫字母'
  - '文本長度不超過 2000000'
input_format: '先 n 與 n 個病毒碼，最後一行網站源碼；多組直到 EOF。'
output_format: '依輸入順序輸出出現次數正的 pattern: count。'
samples:
  - input: "3\nAA\nBB\nCC\nooxxCC%dAAAoen....END\n"
    output: "AA: 2\nCC: 1"
    explanation: 'AA 在 AAA 中重疊出現兩次，CC 出現一次。'
core_knowledge:
  - 多模式 Trie
  - fail 後綴跳轉
  - 線性文本掃描
judgment: '依輸入順序輸出出現次數正的 pattern: count。'
hints:
  - '非大寫文本字元會讓自動機回到根。'
  - '掃描只增加目前狀態造訪數，避免逐位置走 fail。'
  - '反向 BFS 把造訪數加給 fail，模式終點即其出現次數。'
solution_outline: '大寫 AC 自動機掃文本記 visit，逆 BFS 沿 fail 聚合後依終點輸出。'
proof_or_invariant: '每次文本位置的目前狀態代表最長匹配前綴，其所有模式後綴是 fail 祖先；逆序聚合等價於把每次造訪貢獻給全部祖先，故終點計數精確。'
common_errors:
  - '非大寫字元索引越界'
  - '正向而非反向聚合'
  - '漏掉重疊出現'
complexity:
  time: O(模式總長 + 文本長 + 輸出量)
  space: O(模式總長乘字元集)
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：建立 AC 自動機。*/return 0;}
cpp_solution: |
  #include <array>
  #include <iostream>
  #include <queue>
  #include <string>
  #include <vector>
  using namespace std;
  struct Node{array<int,26> next{};int fail=0;long long visits=0;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n=0;while(cin>>n){vector<Node>a(1);vector<string>patterns(static_cast<size_t>(n));vector<int>end(static_cast<size_t>(n));for(int i=0;i<n;++i){cin>>patterns[static_cast<size_t>(i)];int u=0;for(char c:patterns[static_cast<size_t>(i)]){size_t x=static_cast<size_t>(c-'A');if(a[static_cast<size_t>(u)].next[x]==0){a[static_cast<size_t>(u)].next[x]=static_cast<int>(a.size());a.push_back({});}u=a[static_cast<size_t>(u)].next[x];}end[static_cast<size_t>(i)]=u;}queue<int>q;vector<int>order;for(int c=0;c<26;++c){int v=a[0].next[static_cast<size_t>(c)];if(v!=0)q.push(v);}while(!q.empty()){int u=q.front();q.pop();order.push_back(u);for(int c=0;c<26;++c){int&v=a[static_cast<size_t>(u)].next[static_cast<size_t>(c)];if(v!=0){a[static_cast<size_t>(v)].fail=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].next[static_cast<size_t>(c)];q.push(v);}else v=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].next[static_cast<size_t>(c)];}}string text;cin>>text;int u=0;for(unsigned char c:text){if(c>='A'&&c<='Z')u=a[static_cast<size_t>(u)].next[static_cast<size_t>(c-'A')];else u=0;++a[static_cast<size_t>(u)].visits;}for(auto it=order.rbegin();it!=order.rend();++it)a[static_cast<size_t>(a[static_cast<size_t>(*it)].fail)].visits+=a[static_cast<size_t>(*it)].visits;for(int i=0;i<n;++i){long long count=a[static_cast<size_t>(end[static_cast<size_t>(i)])].visits;if(count>0)cout<<patterns[static_cast<size_t>(i)]<<": "<<count<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3065
external_platform: HDU
external_problem_id: '3065'
external_title: '病毒侵襲持續中'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

AC 自動機把多個模式的共同前綴與失配後綴集中到同一個狀態圖。
