---
id: 洛谷-p4377
volume: lower
source_file: lower-volume
source_book_pages:
  - 411
source_pdf_pages:
  - 41
title: Luogu P4377 Talent Show G
chapter: 6
section: '6.6'
kind: external-oj
difficulty: 4
topics:
  - 線性基
  - 分數規劃
prerequisites:
  - GF(2) 與二分答案
statement: 每頭牛有重量與才藝；選若干頭使總重量至少 W，最大化 floor(1000·總才藝/總重量)。
constraints:
  - 1<=n<=250，1<=W<=1000
  - 重量、才藝<=1000
input_format: 依題意讀入測試組、陣列或圖資料。
output_format: 逐組輸出指定答案。
samples:
  - input: |
      2 10
      10 5
      5 20
    output: |
      1666
    explanation: 直接列舉小型案例或依判定式計算可得。
core_knowledge:
  - 等價判定
  - 線性獨立或負環
judgment: 二分答案 x，每頭牛價值改為 1000t-xw；做 0/1 背包，重量超過 W 飽和到 W，判斷是否有非負總價值。
hints:
  - 先把目標改寫成線性獨立或「答案至少為 x」的判定。
  - 找出判定中需要最大化的加總量。
  - 使用線性基、背包或負環檢測完成判定，並處理精度。
solution_outline: 二分答案 x，每頭牛價值改為 1000t-xw；做 0/1 背包，重量超過 W 飽和到 W，判斷是否有非負總價值。
proof_or_invariant: 選集比率至少 x 與轉換價值和非負等價；背包枚舉所有重量至少 W 的選集，判定單調。
complexity:
  time: O(nW log answer)
  space: O(W)
common_errors:
  - 線性基主元方向錯誤
  - 二分可行方向顛倒
  - 64 位元或浮點精度不足
cpp_skeleton: |
  // TODO：依提示重寫核心資料結構。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,W;cin>>n>>W;vector<int>w(n),t(n);for(int i=0;i<n;++i)cin>>w[i]>>t[i];int l=0,r=1000000;while(l<r){int x=(l+r+1)/2;const long long N=-4000000000000000000LL;vector<long long>dp(W+1,N);dp[0]=0;for(int i=0;i<n;++i)for(int j=W;j>=0;--j)if(dp[j]>N/2){int nj=min(W,j+w[i]);dp[nj]=max(dp[nj],dp[j]+1000LL*t[i]-1LL*x*w[i]);}if(dp[W]>=0)l=x;else r=x-1;}cout<<l<<"\n";}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,W;cin>>n>>W;vector<int>w(n),t(n);for(int i=0;i<n;++i)cin>>w[i]>>t[i];int l=0,r=1000000;while(l<r){int x=(l+r+1)/2;const long long N=-4000000000000000000LL;vector<long long>dp(W+1,N);dp[0]=0;for(int i=0;i<n;++i)for(int j=W;j>=0;--j)if(dp[j]>N/2){int nj=min(W,j+w[i]);dp[nj]=max(dp[nj],dp[j]+1000LL*t[i]-1LL*x*w[i]);}if(dp[W]>=0)l=x;else r=x-1;}cout<<l<<"\n";}
external_url: https://www.luogu.com.cn/problem/P4377
external_platform: Luogu
external_problem_id: P4377
external_title: Talent Show G
external_relation: original
review_status: verified
---

以代數結構或單調判定取代直接枚舉。
