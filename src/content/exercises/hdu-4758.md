---
id: hdu-4758
volume: lower
source_file: lower-volume
title: HDU 4758 Walk Through Squares
chapter: 9
section: '9.6'
kind: external-oj
difficulty: 4
topics: [aho-corasick, grid-dp, bitmask]
prerequisites: [ac-automaton, dynamic-programming]
statement: 從矩形左上走到右下，恰走 n 次 D 與 m 次 R；求移動字串同時包含兩個指定動作串的路徑數 modulo 1000000007。
constraints: ['測試組數約 100', '1 <= n,m <= 100', '兩個動作串只含 D、R，長度不超過 100']
input_format: 第一行 T；每組先 n、m，接著兩個動作串。
output_format: 每組輸出一行合法路徑數 modulo 1000000007。
samples:
  - input: "1\n1 1\nD\nR\n"
    output: '2'
    explanation: DR 與 RD 都同時含 D、R；另以枚舉小網格全部二項路徑對拍。
core_knowledge: [二字元 AC, 二維路徑 DP, 兩位命中遮罩]
judgment: 動作串可重疊，兩者內容相同時一次出現可同時滿足兩個條件。
hints:
  - 把兩個動作串插入同一 AC，終點分別標記位元 1、2，沿 fail 取 OR。
  - dp[d][r][node][mask] 計數已走 d 次 D、r 次 R 的路徑。
  - 尚可向下或向右時沿對應轉移，最後只加總 mask=3。
solution_outline: 建字母表 {D,R} 的 AC 與兩位輸出遮罩。按 d、r 遞增做 DP，每個狀態嘗試未超界的 D/R 並更新節點及遮罩；終點加總同時命中兩模式的方案。
proof_or_invariant: 每條單調路徑唯一對應含 n 個 D、m 個 R 的字串，DP 座標精確記錄兩字元使用量，AC 與遮罩精確記錄模式出現歷史；故每條合法路徑恰計一次。
common_errors: [把 n、m 當節點數而少走一步, 輸出遮罩未沿 fail 傳遞, D/R 對應維度顛倒]
complexity: { time: 'O(nmS×4)', space: 'O(nmS×4)' }
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：二字元 AC 上做網格與遮罩 DP。*/return 0;}
cpp_solution: |
  #include <array>
  #include <iostream>
  #include <queue>
  #include <string>
  #include <vector>
  using namespace std;
  constexpr int mod=1000000007;
  struct Node{array<int,2> next{};int fail=0;int output=0;};
  static size_t code(char ch){return ch=='D'?0U:1U;}
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests=0;cin>>tests;while(tests--){int down=0,right=0;cin>>down>>right;vector<Node>a(1);for(int id=0;id<2;++id){string s;cin>>s;int u=0;for(char ch:s){size_t c=code(ch);if(a[static_cast<size_t>(u)].next[c]==0){a[static_cast<size_t>(u)].next[c]=static_cast<int>(a.size());a.push_back({});}u=a[static_cast<size_t>(u)].next[c];}a[static_cast<size_t>(u)].output|=1<<id;}queue<int>q;for(size_t c=0;c<2;++c)if(a[0].next[c]!=0)q.push(a[0].next[c]);while(!q.empty()){int u=q.front();q.pop();a[static_cast<size_t>(u)].output|=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].output;for(size_t c=0;c<2;++c){int&v=a[static_cast<size_t>(u)].next[c];if(v!=0){a[static_cast<size_t>(v)].fail=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].next[c];q.push(v);}else v=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].next[c];}}size_t width=static_cast<size_t>(right+1),states=a.size()*4;vector<vector<vector<int>>>dp(static_cast<size_t>(down+1),vector<vector<int>>(width,vector<int>(states)));dp[0][0][0]=1;for(int d=0;d<=down;++d)for(int r=0;r<=right;++r)for(size_t u=0;u<a.size();++u)for(int mask=0;mask<4;++mask){int value=dp[static_cast<size_t>(d)][static_cast<size_t>(r)][u*4+static_cast<size_t>(mask)];if(value==0)continue;for(size_t c=0;c<2;++c){int nd=d+(c==0),nr=r+(c==1);if(nd>down||nr>right)continue;int v=a[u].next[c],nm=mask|a[static_cast<size_t>(v)].output;int&target=dp[static_cast<size_t>(nd)][static_cast<size_t>(nr)][static_cast<size_t>(v)*4+static_cast<size_t>(nm)];target=static_cast<int>((static_cast<long long>(target)+value)%mod);}}int answer=0;for(size_t u=0;u<a.size();++u)answer=static_cast<int>((static_cast<long long>(answer)+dp[static_cast<size_t>(down)][static_cast<size_t>(right)][u*4+3])%mod);cout<<answer<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4758
external_platform: HDU
external_problem_id: '4758'
external_title: Walk Through Squares
external_relation: original
source_book_pages: [579, 586]
source_pdf_pages: [209, 216]
review_status: verified
---

網格座標就是 D/R 使用量；把兩個動作的出現歷史壓成兩個位元即可。
