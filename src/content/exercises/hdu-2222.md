---
id: hdu-2222
volume: lower
source_file: lower-volume
title: HDU 2222 Keywords Search
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [aho-corasick, trie, failure-link]
prerequisites: [kmp, trie]
statement: '給定多個關鍵字與一段文本，求所有關鍵字在文本中的出現總次數；重複輸入的關鍵字各自計算。'
constraints:
  - '1 <= T'
  - '1 <= n <= 10000'
  - '字串只含小寫英文字母'
input_format: '第一行 T；每組先 n 與 n 個模式，再給文本。'
output_format: '每組輸出總匹配次數。'
samples:
  - input: "1\n4\nhe\nshe\nhers\nhis\nahishers\n"
    output: '4'
    explanation: '文本中 his、she、he、hers 各出現一次。'
core_knowledge:
  - 多模式 Trie
  - fail 後綴跳轉
  - 線性文本掃描
judgment: '每組輸出總匹配次數。'
hints:
  - '終止節點保存以此結束的模式數，重複模式要累加。'
  - 'BFS 建 fail，並把 output 累加自 fail 節點。'
  - '掃文本每字走一個補全轉移，把目前狀態 output 加入答案。'
solution_outline: '建 AC 自動機，BFS 時令 out[v]+=out[fail[v]]，掃描文本累加 out。'
proof_or_invariant: '目前狀態的 fail 祖先恰代表所有匹配的模式後綴；預先累加 output 等價於逐次走 fail，且每個輸入模式依終點 multiplicity 計數。'
common_errors:
  - '把重複模式覆蓋成一個'
  - '掃描後破壞終止標記導致後續漏算'
  - '只計目前 Trie 節點不計 fail 祖先'
complexity:
  time: O(模式總長 + 文本長 + 輸出量)
  space: O(模式總長乘字元集)
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：建立 AC 自動機。*/return 0;}
cpp_solution: |
  #include <array>
  #include <iostream>
  #include <queue>
  #include <string>
  #include <vector>
  using namespace std;
  struct Node{array<int,26> next{};int fail=0;int output=0;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests=0;cin>>tests;while(tests--){int n=0;cin>>n;vector<Node>a(1);while(n--){string p;cin>>p;int u=0;for(char c:p){size_t x=static_cast<size_t>(c-'a');if(a[static_cast<size_t>(u)].next[x]==0){a[static_cast<size_t>(u)].next[x]=static_cast<int>(a.size());a.push_back({});}u=a[static_cast<size_t>(u)].next[x];}++a[static_cast<size_t>(u)].output;}queue<int>q;for(int c=0;c<26;++c){int v=a[0].next[static_cast<size_t>(c)];if(v!=0)q.push(v);}while(!q.empty()){int u=q.front();q.pop();a[static_cast<size_t>(u)].output+=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].output;for(int c=0;c<26;++c){int&v=a[static_cast<size_t>(u)].next[static_cast<size_t>(c)];if(v!=0){a[static_cast<size_t>(v)].fail=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].next[static_cast<size_t>(c)];q.push(v);}else v=a[static_cast<size_t>(a[static_cast<size_t>(u)].fail)].next[static_cast<size_t>(c)];}}string text;cin>>text;long long answer=0;int u=0;for(char c:text){u=a[static_cast<size_t>(u)].next[static_cast<size_t>(c-'a')];answer+=a[static_cast<size_t>(u)].output;}cout<<answer<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2222
external_platform: HDU
external_problem_id: '2222'
external_title: 'Keywords Search'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

AC 自動機把多個模式的共同前綴與失配後綴集中到同一個狀態圖。
