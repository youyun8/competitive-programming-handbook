---
id: hdu-1166
volume: upper
source_file: upper-volume
title: HDU 1166 敵兵佈陣
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 2
topics: &id001
  - fenwick-tree
  - point-update
  - range-sum
prerequisites:
  - segment-tree
statement: 營地人數支援單點增、減與區間總數查詢。
constraints:
  - T <= 20
  - N <= 50000
input_format: 每組初值；Add i j、Sub i j、Query i j、End。
output_format: 每組印 Case k:，Query 逐行輸出。
samples:
  - input: |
      1
      3
      1 2 3
      Query 1 3
      Sub 2 1
      Query 2 3
      End
    output: |
      Case 1:
      6
      4
    explanation: 總數先為六，第二營減一後 [2,3] 為四。
core_knowledge: *id001
judgment: 樹狀陣列維護人數。
hints:
  - 單點增減正是 BIT 的基本更新。
  - BIT 查前綴 [1,x]。
  - 區間 [l,r] 用兩前綴相減。
solution_outline: 樹狀陣列維護人數。
proof_or_invariant: BIT 前綴分解不重不漏，單點更新同步所有包含該位置的摘要。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O((N+M) log N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}int sum(int x)const{int s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<int>t;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n;cin>>n;Fenwick bit(n);for(int i=1;i<=n;++i){int x;cin>>x;bit.add(i,x);}cout<<"Case "<<cs<<":\n";string op;while(cin>>op&&op!="End"){int x,y;cin>>x>>y;if(op=="Add")bit.add(x,y);else if(op=="Sub")bit.add(x,-y);else cout<<bit.sum(y)-bit.sum(x-1)<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}int sum(int x)const{int s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<int>t;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n;cin>>n;Fenwick bit(n);for(int i=1;i<=n;++i){int x;cin>>x;bit.add(i,x);}cout<<"Case "<<cs<<":\n";string op;while(cin>>op&&op!="End"){int x,y;cin>>x>>y;if(op=="Add")bit.add(x,y);else if(op=="Sub")bit.add(x,-y);else cout<<bit.sum(y)-bit.sum(x-1)<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1166
external_platform: HDU
external_problem_id: '1166'
external_title: HDU 1166 敵兵佈陣
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
