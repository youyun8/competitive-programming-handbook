---
id: hdu-3749
volume: lower
source_file: lower-volume
original_label: 'HDU 3749'
title: 'HDU 3749 Financial Crisis'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 4
topics: [vertex-biconnected-components, queries]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  對無向企業網路的每組詢問，判定兩企業間內部頂點互不重疊的路徑數為零、一、或至少二。
constraints: [3 <= n <= 5000, 0 <= m <= 10000, 1 <= q <= 1000, 無重邊]
input_format: >-
  多組 n m q，後接 m 行邊與 q 行詢問；0 0 0 結束。
output_format: >-
  每案輸出 Case k:，各詢問輸出 zero、one 或 two or more。
samples:
  - input: |
      3 3 1
      0 1
      1 2
      2 0
      0 2
      0 0 0
    output: |
      Case 1:
      two or more
    explanation: >-
      三角形中 0 到 2 有直達與經 1 的兩條內點不交路。
core_knowledge: [點雙連通分量, 連通分量, Menger 定理]
judgment: >-
  兩點連通且同屬一個至少三點的點雙分量時答案至少二，否則恰一。
hints:
  - 先以一般連通分量排除 zero。
  - Tarjan 以邊堆疊列出各點雙分量。
  - 割點可同時屬於多個分量。
solution_outline: >-
  分解點雙分量並記每點所屬分量編號；詢問先判連通，再找兩點共同且大小大於二的點雙分量。
proof_or_invariant: >-
  Menger 定理指出兩條內點不交路存在等價於兩點同在無割點分隔的 block；單邊 block 只有兩點，僅提供一條路。
complexity: { time: 'O(n + m + q*B)', space: 'O(n + m)' }
common_errors: [割點只記一個分量, 兩點 block 誤判至少二, 未先處理不連通]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m,q,cs=0;while(cin>>n>>m>>q&&(n||m||q)){vector<vector<pair<int,int>>>g(n);vector<pair<int,int>>e(m);for(int i=0;i<m;++i){auto&[u,v]=e[i];cin>>u>>v;g[u].push_back({v,i});g[v].push_back({u,i});}vector<int>d(n),low(n),stack_edges,connected(n);vector<vector<int>>belongs(n);vector<int>block_size;int timer=0,component=0;auto dfs=[&](auto&&self,int u,int pe)->void{d[u]=low[u]=++timer;connected[u]=component;for(auto [v,id]:g[u])if(id!=pe){if(!d[v]){stack_edges.push_back(id);self(self,v,id);low[u]=min(low[u],low[v]);if(low[v]>=d[u]){set<int>vertices;int last;do{last=stack_edges.back();stack_edges.pop_back();vertices.insert(e[last].first);vertices.insert(e[last].second);}while(last!=id);int b=static_cast<int>(block_size.size());block_size.push_back(static_cast<int>(vertices.size()));for(int x:vertices)belongs[x].push_back(b);}}else if(d[v]<d[u])stack_edges.push_back(id),low[u]=min(low[u],d[v]);}};for(int i=0;i<n;++i)if(!d[i])++component,dfs(dfs,i,-1);cout<<"Case "<<++cs<<":\n";while(q--){int u,v;cin>>u>>v;if(connected[u]!=connected[v]){cout<<"zero\n";continue;}bool many=false;for(int a:belongs[u])if(block_size[a]>2&&find(belongs[v].begin(),belongs[v].end(),a)!=belongs[v].end()){many=true;break;}cout<<(many?"two or more":"one")<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3749
external_platform: HDU
external_problem_id: '3749'
external_title: 'Financial Crisis'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
