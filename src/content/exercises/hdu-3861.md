---
id: hdu-3861
volume: lower
source_file: lower-volume
original_label: 'HDU 3861'
title: "HDU 3861 The King's Problem"
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 4
topics: [strongly-connected-components, minimum-path-cover, bipartite-matching]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  將有向圖城市分州：互相可達者必須同州，且同州任兩城至少有一方向可達；求最少州數。
constraints: [1 <= n <= 5000, 0 <= m <= 100000, 第一行為測試組數]
input_format: >-
  先給 T；每組 n m 與 m 行有向邊。
output_format: >-
  每組輸出最少州數。
samples:
  - input: |
      1
      3 2
      1 2
      2 3
    output: |
      1
    explanation: >-
      三個縮點在同一路徑上，可歸為一州。
core_knowledge: [SCC 縮點, DAG 最小路徑覆蓋, 二分圖匹配]
judgment: >-
  縮點 DAG 的最小頂點不相交路徑覆蓋數為 C−最大匹配。
hints:
  - 互相可達者先強制縮點。
  - 每州在 DAG 上必是一條鏈。
  - 拆左右點後以每條 DAG 邊做匹配邊。
solution_outline: >-
  Tarjan 得 C 個分量，建立縮點 DAG 的二分圖並求最大匹配，輸出 C-match。
proof_or_invariant: >-
  一組路徑覆蓋的每條相鄰連接對應一條匹配邊；反之任一匹配因 DAG 無環可串成互不交疊路徑，故最少路徑數等於頂點數減最大匹配。
complexity: { time: 'O(n + m + C*m)', space: 'O(n + m)' }
common_errors: [未先縮 SCC, 把最小點覆蓋公式套入, 匹配時重用右側頂點]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Scc{int node_count,t=0,c=0;vector<vector<int>>g;vector<int>d,l,st,id;vector<char>on;explicit Scc(int count):node_count(count),g(static_cast<size_t>(count)),d(static_cast<size_t>(count)),l(static_cast<size_t>(count)),id(static_cast<size_t>(count)),on(static_cast<size_t>(count)){}void dfs(int u){d[u]=l[u]=++t;st.push_back(u);on[u]=1;for(int v:g[u])if(!d[v])dfs(v),l[u]=min(l[u],l[v]);else if(on[v])l[u]=min(l[u],d[v]);if(d[u]==l[u]){while(true){int v=st.back();st.pop_back();on[v]=0;id[v]=c;if(v==u)break;}++c;}}void run(){for(int i=0;i<node_count;++i)if(!d[i])dfs(i);}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int t;cin>>t;while(t--){int n,m;cin>>n>>m;Scc s(n);vector<pair<int,int>>e;while(m--){int u,v;cin>>u>>v;--u;--v;s.g[u].push_back(v);e.push_back({u,v});}s.run();vector<vector<int>>b(s.c);for(auto [u,v]:e)if(s.id[u]!=s.id[v])b[s.id[u]].push_back(s.id[v]);vector<int>mt(s.c,-1);int match=0;for(int start=0;start<s.c;++start){vector<char>seen(s.c);auto aug=[&](auto&&self,int u)->bool{for(int v:b[u])if(!seen[v]){seen[v]=1;if(mt[v]<0||self(self,mt[v])){mt[v]=u;return true;}}return false;};match+=aug(aug,start);}cout<<s.c-match<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3861
external_platform: HDU
external_problem_id: '3861'
external_title: "The King's Problem"
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
