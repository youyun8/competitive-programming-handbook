---
id: hdu-4718
volume: upper
source_file: upper-volume
title: HDU 4718 The LCIS on the Tree
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - heavy-light-decomposition
  - bidirectional-summary
  - LCIS
prerequisites:
  - segment-tree
statement: 每次給有向樹路徑 u 到 v，求路徑值序列最長連續嚴格遞增段長。
constraints:
  - T <= 10
  - N,Q <= 100000
  - 點值 <= 1000000000
input_format: 每組點值、節點 2..N 的父親、Q 個 u v。
output_format: 每問輸出 LCIS 長度。
samples:
  - input: |
      1
      5
      1 3 2 4 5
      1 1 2 2
      3
      4 5
      3 5
      5 3
    output: |
      2
      3
      2
    explanation: 有向路徑方向不同會交換遞增與遞減資訊。
core_knowledge: *id001
judgment: HLD 將路徑拆段，線段樹回傳可合併雙向摘要。
hints:
  - 區間摘要維護遞增與遞減的前綴、後綴、最佳值。
  - 反轉摘要時，遞增資訊對應原摘要的遞減資訊。
  - 樹鏈剖分依 u→LCA 與 LCA→v 的方向有序合併。
solution_outline: HLD 將路徑拆段，線段樹回傳可合併雙向摘要。
proof_or_invariant: merge 只需檢查接縫端值；HLD 收集順序保持原有向路徑，因此最後 ib 即題目序列 LCIS。
common_errors:
  - 端點、開閉區間或 0/1 起始索引處理錯誤
  - 合併摘要時遺漏跨左右區間的候選
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O((N+Q) log²N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;struct S{int len=0,lv=0,rv=0,ip=0,is=0,ib=0,dp=0,ds=0,db=0;};static S merge_s(const S&a,const S&b){if(a.len==0)return b;if(b.len==0)return a;S c;c.len=a.len+b.len;c.lv=a.lv;c.rv=b.rv;c.ip=a.ip+(a.ip==a.len&&a.rv<b.lv?b.ip:0);c.is=b.is+(b.is==b.len&&a.rv<b.lv?a.is:0);c.ib=max(a.ib,b.ib);if(a.rv<b.lv)c.ib=max(c.ib,a.is+b.ip);c.dp=a.dp+(a.dp==a.len&&a.rv>b.lv?b.dp:0);c.ds=b.ds+(b.ds==b.len&&a.rv>b.lv?a.ds:0);c.db=max(a.db,b.db);if(a.rv>b.lv)c.db=max(c.db,a.ds+b.dp);return c;}static S reversed(S x){swap(x.lv,x.rv);int ip=x.ip,is=x.is,ib=x.ib,dp=x.dp,ds=x.ds,db=x.db;x.ip=ds;x.is=dp;x.ib=db;x.dp=is;x.ds=ip;x.db=ib;return x;}static vector<S>seg;static vector<int>val;static void build(int p,int l,int r,const vector<int>&at){if(l==r){int v=val[static_cast<size_t>(at[static_cast<size_t>(l)])];seg[static_cast<size_t>(p)]={1,v,v,1,1,1,1,1,1};return;}int m=(l+r)/2;build(p*2,l,m,at);build(p*2+1,m+1,r,at);seg[static_cast<size_t>(p)]=merge_s(seg[static_cast<size_t>(p*2)],seg[static_cast<size_t>(p*2+1)]);}static S query(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return S{};if(ql<=l&&r<=qr)return seg[static_cast<size_t>(p)];int m=(l+r)/2;return merge_s(query(p*2,l,m,ql,qr),query(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int n;cin>>n;val.assign(static_cast<size_t>(n+1),0);for(int i=1;i<=n;++i)cin>>val[static_cast<size_t>(i)];vector<vector<int>>ch(static_cast<size_t>(n+1));vector<int>parent(static_cast<size_t>(n+1)),depth(static_cast<size_t>(n+1)),size(static_cast<size_t>(n+1),1),heavy(static_cast<size_t>(n+1));for(int i=2;i<=n;++i){cin>>parent[static_cast<size_t>(i)];ch[static_cast<size_t>(parent[static_cast<size_t>(i)])].push_back(i);depth[static_cast<size_t>(i)]=depth[static_cast<size_t>(parent[static_cast<size_t>(i)])]+1;}vector<int>order{1};for(size_t i=0;i<order.size();++i)for(int y:ch[static_cast<size_t>(order[i])])order.push_back(y);for(size_t i=order.size();i-->0;){int x=order[i];for(int y:ch[static_cast<size_t>(x)]){size[static_cast<size_t>(x)]+=size[static_cast<size_t>(y)];if(size[static_cast<size_t>(y)]>size[static_cast<size_t>(heavy[static_cast<size_t>(x)])])heavy[static_cast<size_t>(x)]=y;}}vector<int>top(static_cast<size_t>(n+1)),pos(static_cast<size_t>(n+1)),at(static_cast<size_t>(n+1));int timer=0;vector<pair<int,int>>stack{{1,1}};while(!stack.empty()){auto [start,tp]=stack.back();stack.pop_back();for(int x=start;x!=0;x=heavy[static_cast<size_t>(x)]){top[static_cast<size_t>(x)]=tp;pos[static_cast<size_t>(x)]=++timer;at[static_cast<size_t>(timer)]=x;for(int y:ch[static_cast<size_t>(x)])if(y!=heavy[static_cast<size_t>(x)])stack.push_back({y,y});}}seg.assign(static_cast<size_t>(4*n+4),S{});build(1,1,n,at);int q;cin>>q;while(q--){int u,v;cin>>u>>v;S left,right;while(top[static_cast<size_t>(u)]!=top[static_cast<size_t>(v)]){if(depth[static_cast<size_t>(top[static_cast<size_t>(u)])]>=depth[static_cast<size_t>(top[static_cast<size_t>(v)])]){left=merge_s(left,reversed(query(1,1,n,pos[static_cast<size_t>(top[static_cast<size_t>(u)])],pos[static_cast<size_t>(u)])));u=parent[static_cast<size_t>(top[static_cast<size_t>(u)])];}else{right=merge_s(query(1,1,n,pos[static_cast<size_t>(top[static_cast<size_t>(v)])],pos[static_cast<size_t>(v)]),right);v=parent[static_cast<size_t>(top[static_cast<size_t>(v)])];}}if(depth[static_cast<size_t>(u)]>=depth[static_cast<size_t>(v)])left=merge_s(left,reversed(query(1,1,n,pos[static_cast<size_t>(v)],pos[static_cast<size_t>(u)])));else right=merge_s(query(1,1,n,pos[static_cast<size_t>(u)],pos[static_cast<size_t>(v)]),right);cout<<merge_s(left,right).ib<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct S{int len=0,lv=0,rv=0,ip=0,is=0,ib=0,dp=0,ds=0,db=0;};static S merge_s(const S&a,const S&b){if(a.len==0)return b;if(b.len==0)return a;S c;c.len=a.len+b.len;c.lv=a.lv;c.rv=b.rv;c.ip=a.ip+(a.ip==a.len&&a.rv<b.lv?b.ip:0);c.is=b.is+(b.is==b.len&&a.rv<b.lv?a.is:0);c.ib=max(a.ib,b.ib);if(a.rv<b.lv)c.ib=max(c.ib,a.is+b.ip);c.dp=a.dp+(a.dp==a.len&&a.rv>b.lv?b.dp:0);c.ds=b.ds+(b.ds==b.len&&a.rv>b.lv?a.ds:0);c.db=max(a.db,b.db);if(a.rv>b.lv)c.db=max(c.db,a.ds+b.dp);return c;}static S reversed(S x){swap(x.lv,x.rv);int ip=x.ip,is=x.is,ib=x.ib,dp=x.dp,ds=x.ds,db=x.db;x.ip=ds;x.is=dp;x.ib=db;x.dp=is;x.ds=ip;x.db=ib;return x;}static vector<S>seg;static vector<int>val;static void build(int p,int l,int r,const vector<int>&at){if(l==r){int v=val[static_cast<size_t>(at[static_cast<size_t>(l)])];seg[static_cast<size_t>(p)]={1,v,v,1,1,1,1,1,1};return;}int m=(l+r)/2;build(p*2,l,m,at);build(p*2+1,m+1,r,at);seg[static_cast<size_t>(p)]=merge_s(seg[static_cast<size_t>(p*2)],seg[static_cast<size_t>(p*2+1)]);}static S query(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return S{};if(ql<=l&&r<=qr)return seg[static_cast<size_t>(p)];int m=(l+r)/2;return merge_s(query(p*2,l,m,ql,qr),query(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int n;cin>>n;val.assign(static_cast<size_t>(n+1),0);for(int i=1;i<=n;++i)cin>>val[static_cast<size_t>(i)];vector<vector<int>>ch(static_cast<size_t>(n+1));vector<int>parent(static_cast<size_t>(n+1)),depth(static_cast<size_t>(n+1)),size(static_cast<size_t>(n+1),1),heavy(static_cast<size_t>(n+1));for(int i=2;i<=n;++i){cin>>parent[static_cast<size_t>(i)];ch[static_cast<size_t>(parent[static_cast<size_t>(i)])].push_back(i);depth[static_cast<size_t>(i)]=depth[static_cast<size_t>(parent[static_cast<size_t>(i)])]+1;}vector<int>order{1};for(size_t i=0;i<order.size();++i)for(int y:ch[static_cast<size_t>(order[i])])order.push_back(y);for(size_t i=order.size();i-->0;){int x=order[i];for(int y:ch[static_cast<size_t>(x)]){size[static_cast<size_t>(x)]+=size[static_cast<size_t>(y)];if(size[static_cast<size_t>(y)]>size[static_cast<size_t>(heavy[static_cast<size_t>(x)])])heavy[static_cast<size_t>(x)]=y;}}vector<int>top(static_cast<size_t>(n+1)),pos(static_cast<size_t>(n+1)),at(static_cast<size_t>(n+1));int timer=0;vector<pair<int,int>>stack{{1,1}};while(!stack.empty()){auto [start,tp]=stack.back();stack.pop_back();for(int x=start;x!=0;x=heavy[static_cast<size_t>(x)]){top[static_cast<size_t>(x)]=tp;pos[static_cast<size_t>(x)]=++timer;at[static_cast<size_t>(timer)]=x;for(int y:ch[static_cast<size_t>(x)])if(y!=heavy[static_cast<size_t>(x)])stack.push_back({y,y});}}seg.assign(static_cast<size_t>(4*n+4),S{});build(1,1,n,at);int q;cin>>q;while(q--){int u,v;cin>>u>>v;S left,right;while(top[static_cast<size_t>(u)]!=top[static_cast<size_t>(v)]){if(depth[static_cast<size_t>(top[static_cast<size_t>(u)])]>=depth[static_cast<size_t>(top[static_cast<size_t>(v)])]){left=merge_s(left,reversed(query(1,1,n,pos[static_cast<size_t>(top[static_cast<size_t>(u)])],pos[static_cast<size_t>(u)])));u=parent[static_cast<size_t>(top[static_cast<size_t>(u)])];}else{right=merge_s(query(1,1,n,pos[static_cast<size_t>(top[static_cast<size_t>(v)])],pos[static_cast<size_t>(v)]),right);v=parent[static_cast<size_t>(top[static_cast<size_t>(v)])];}}if(depth[static_cast<size_t>(u)]>=depth[static_cast<size_t>(v)])left=merge_s(left,reversed(query(1,1,n,pos[static_cast<size_t>(v)],pos[static_cast<size_t>(u)])));else right=merge_s(query(1,1,n,pos[static_cast<size_t>(u)],pos[static_cast<size_t>(v)]),right);cout<<merge_s(left,right).ib<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4718
external_platform: HDU
external_problem_id: '4718'
external_title: HDU 4718 The LCIS on the Tree
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
