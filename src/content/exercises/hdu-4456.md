---
id: hdu-4456
volume: upper
source_file: upper-volume
title: HDU 4456 Crowd：曼哈頓菱形動態和
chapter: 4
section: '4.2'
kind: external-oj
difficulty: 4
topics: &id001
  - coordinate-transform
  - sparse-two-dimensional-fenwick-tree
prerequisites:
  - fenwick-tree
statement: N×N 網格初值零；點增量，或查詢與 (x,y) 曼哈頓距離不超過 z 的所有權值和。
constraints:
  - N <= 10000
  - M <= 80000
  - 更新 -100 到 100
  - 查詢半徑 <= 2N-1
  - N=0 結束
input_format: 每組 N M，再給 M 行 p x y z。
output_format: 每個 p=2 輸出查詢和。
samples:
  - input: |
      3 4
      1 1 1 5
      1 2 2 3
      2 1 1 1
      2 2 2 2
      0
    output: |
      5
      8
    explanation: 第一問距離一只含 (1,1)；第二問涵蓋兩個更新點。
core_knowledge: *id001
judgment: 旋轉座標後，以 Fenwick-of-vectors 支援點增量與矩形和。
hints:
  - 令 u=x-y+N、v=x+y，曼哈頓菱形會變軸對齊矩形。
  - N 大但更新點少；只收集會被更新點觸及的二維 BIT 內層座標。
  - 查詢轉換後矩形，用四個稀疏二維前綴和容斥。
solution_outline: 旋轉座標後，以 Fenwick-of-vectors 支援點增量與矩形和。
proof_or_invariant: 座標轉換把 L1 距離不超過 z 等價成 u、v 各差不超過 z；稀疏 BIT 與完整 BIT 有相同前綴不變量。
common_errors:
  - 索引基準或閉區間端點處理錯誤
  - 更新資料結構後忘記同步原始狀態
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(M log²N)
  space: O(M log N)
cpp_skeleton: |
  // TODO：先依三階段提示自行完成核心；以下框架可嚴格編譯。
  #include <bits/stdc++.h>
  using namespace std;
  struct Op{int type,x,y,z;};class SparseFenwick2D{public:SparseFenwick2D(int limit,const vector<pair<int,int>>&points):limit_(limit),ys(static_cast<size_t>(limit+1)),bit(static_cast<size_t>(limit+1)){for(auto [x,y]:points)for(int i=x;i<=limit_;i+=i&-i)ys[static_cast<size_t>(i)].push_back(y);for(int i=1;i<=limit_;++i){auto&v=ys[static_cast<size_t>(i)];sort(v.begin(),v.end());v.erase(unique(v.begin(),v.end()),v.end());bit[static_cast<size_t>(i)].assign(v.size()+1,0);}}void add(int x,int y,int value){for(int i=x;i<=limit_;i+=i&-i){auto&v=ys[static_cast<size_t>(i)];int p=static_cast<int>(lower_bound(v.begin(),v.end(),y)-v.begin())+1;for(int j=p;j<static_cast<int>(bit[static_cast<size_t>(i)].size());j+=j&-j)bit[static_cast<size_t>(i)][static_cast<size_t>(j)]+=value;}}long long prefix(int x,int y)const{long long s=0;for(int i=x;i>0;i-=i&-i){const auto&v=ys[static_cast<size_t>(i)];int p=static_cast<int>(upper_bound(v.begin(),v.end(),y)-v.begin());for(int j=p;j>0;j-=j&-j)s+=bit[static_cast<size_t>(i)][static_cast<size_t>(j)];}return s;}private:int limit_;vector<vector<int>>ys;vector<vector<long long>>bit;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n&&n!=0){cin>>m;vector<Op>ops(static_cast<size_t>(m));vector<pair<int,int>>points;for(auto&o:ops){cin>>o.type>>o.x>>o.y>>o.z;if(o.type==1)points.push_back({o.x-o.y+n,o.x+o.y});}int limit=2*n;SparseFenwick2D bit(limit,points);for(auto o:ops){int x=o.x-o.y+n,y=o.x+o.y;if(o.type==1)bit.add(x,y,o.z);else{int x1=max(1,x-o.z),y1=max(1,y-o.z),x2=min(limit,x+o.z),y2=min(limit,y+o.z);cout<<bit.prefix(x2,y2)-bit.prefix(x1-1,y2)-bit.prefix(x2,y1-1)+bit.prefix(x1-1,y1-1)<<'\n';}}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Op{int type,x,y,z;};class SparseFenwick2D{public:SparseFenwick2D(int limit,const vector<pair<int,int>>&points):limit_(limit),ys(static_cast<size_t>(limit+1)),bit(static_cast<size_t>(limit+1)){for(auto [x,y]:points)for(int i=x;i<=limit_;i+=i&-i)ys[static_cast<size_t>(i)].push_back(y);for(int i=1;i<=limit_;++i){auto&v=ys[static_cast<size_t>(i)];sort(v.begin(),v.end());v.erase(unique(v.begin(),v.end()),v.end());bit[static_cast<size_t>(i)].assign(v.size()+1,0);}}void add(int x,int y,int value){for(int i=x;i<=limit_;i+=i&-i){auto&v=ys[static_cast<size_t>(i)];int p=static_cast<int>(lower_bound(v.begin(),v.end(),y)-v.begin())+1;for(int j=p;j<static_cast<int>(bit[static_cast<size_t>(i)].size());j+=j&-j)bit[static_cast<size_t>(i)][static_cast<size_t>(j)]+=value;}}long long prefix(int x,int y)const{long long s=0;for(int i=x;i>0;i-=i&-i){const auto&v=ys[static_cast<size_t>(i)];int p=static_cast<int>(upper_bound(v.begin(),v.end(),y)-v.begin());for(int j=p;j>0;j-=j&-j)s+=bit[static_cast<size_t>(i)][static_cast<size_t>(j)];}return s;}private:int limit_;vector<vector<int>>ys;vector<vector<long long>>bit;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n&&n!=0){cin>>m;vector<Op>ops(static_cast<size_t>(m));vector<pair<int,int>>points;for(auto&o:ops){cin>>o.type>>o.x>>o.y>>o.z;if(o.type==1)points.push_back({o.x-o.y+n,o.x+o.y});}int limit=2*n;SparseFenwick2D bit(limit,points);for(auto o:ops){int x=o.x-o.y+n,y=o.x+o.y;if(o.type==1)bit.add(x,y,o.z);else{int x1=max(1,x-o.z),y1=max(1,y-o.z),x2=min(limit,x+o.z),y2=min(limit,y+o.z);cout<<bit.prefix(x2,y2)-bit.prefix(x1-1,y2)-bit.prefix(x2,y1-1)+bit.prefix(x1-1,y1-1)<<'\n';}}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4456
external_platform: HDU
external_problem_id: '4456'
external_title: HDU 4456 Crowd
external_relation: original
source_book_pages:
  - 170
source_pdf_pages:
  - 188
review_status: verified
---

本卡片依外部題面與限制獨立整理。
