---
id: 洛谷-p2152
volume: lower
source_file: lower-volume
source_book_pages:
  - 415
source_pdf_pages:
  - 45
title: Luogu P2152 SuperGCD
chapter: 6
section: '6.7'
kind: external-oj
difficulty: 3
topics:
  - 最大公因數
  - 數論建模
prerequisites:
  - 整除與同餘
statement: 給定兩個最長 10^4 位的非負十進位整數，輸出最大公因數。
constraints:
  - 輸入整數長度不超過 10000 位
input_format: 依題目格式讀入測試組數與各組參數；多組至 EOF 或終止值者見敘述。
output_format: 依題目指定格式逐組輸出答案。
samples:
  - input: |
      12
      18
    output: |
      6
    explanation: 依定義計算或套用推導後的充要條件，可得上述結果。
core_knowledge:
  - gcd/lcm 性質
  - 等價轉換
judgment: 以任意精度整數執行歐幾里得演算法。
hints:
  - 先確認哪些量在一次操作或遞推中保持不變。
  - 用 gcd、質因數或同餘式將候選集合縮小。
  - 證明轉換雙向成立後，再處理邊界、溢位及固定輸出格式。
solution_outline: 以任意精度整數執行歐幾里得演算法。
proof_or_invariant: 歐幾里得每步保持共同因數集合不變，餘數成為零時非零數即 gcd。
complexity:
  time: O(L^2 log V)
  space: O(L)
common_errors:
  - 只證明必要條件而未證明充分性
  - 中間乘積溢位或負餘數未正規化
  - 漏掉最小輸入與終止條件
cpp_skeleton: |
  // TODO：依證明自行重寫核心轉換。
  #include <bits/stdc++.h>
  using namespace std;
  #include <boost/multiprecision/cpp_int.hpp>
  using boost::multiprecision::cpp_int;int main(){string a,b;cin>>a>>b;cpp_int x=0,y=0;for(char c:a)x=x*10+c-'0';for(char c:b)y=y*10+c-'0';while(y!=0){cpp_int r=x%y;x=y;y=r;}cout<<x<<"\n";}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  #include <boost/multiprecision/cpp_int.hpp>
  using boost::multiprecision::cpp_int;int main(){string a,b;cin>>a>>b;cpp_int x=0,y=0;for(char c:a)x=x*10+c-'0';for(char c:b)y=y*10+c-'0';while(y!=0){cpp_int r=x%y;x=y;y=r;}cout<<x<<"\n";}
external_url: https://www.luogu.com.cn/problem/P2152
external_platform: Luogu
external_problem_id: P2152
external_title: SuperGCD
external_relation: original
review_status: verified
---

本題重點是把操作轉成可驗證的數論條件。
