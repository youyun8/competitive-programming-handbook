---
id: hdu-5755
volume: lower
source_file: lower-volume
source_book_pages:
  - 402
source_pdf_pages:
  - 32
title: HDU 5755 Gambler Bo
chapter: 6
section: '6.4'
kind: external-oj
difficulty: 4
topics: &a1
  - 模線性方程
  - 高斯消去
prerequisites:
  - 模運算與線性方程
  - 依題型所需的圖論或數論基礎
statement: 給 n×m 的 0、1、2 矩陣。選一格會令該格加 2、上下左右相鄰格加 1，全部模 3；輸出不超過 2nm 次操作使矩陣全零。
constraints:
  - 1 <= n,m <= 30
  - 保證存在方案
input_format: 第一行 T；每組為 n、m 與矩陣。
output_format: 先輸出操作次數，再逐行輸出每次操作的 1-based 座標。
samples:
  - input: |
      1
      1 1
      1
    output: |
      1
      1 1
    explanation: 操作唯一格一次後其值為 1+2≡0 (mod 3)。
core_knowledge: *a1
judgment: 把第一列操作次數當 m 個未知數。由某列及其上一列可唯一推出下一列；將每格表示成第一列未知數的仿射式。要求最後一列下方的虛擬列為零，只需解 m 元模 3 方程。
hints:
  - 先把隨機過程、流量調整或冪次條件寫成代數式。
  - 辨認固定維矩陣、線性方程、分數規劃或同餘系統，避免直接模擬巨大狀態。
  - 處理自由變數、非互質模數、數值精度與溢位等邊界後再輸出。
solution_outline: 把第一列操作次數當 m 個未知數。由某列及其上一列可唯一推出下一列；將每格表示成第一列未知數的仿射式。要求最後一列下方的虛擬列為零，只需解 m 元模 3 方程。
proof_or_invariant: 消去第 i 列原值的方程可直接解出第 i+1 列同欄操作，因此任選第一列後其餘列唯一。全部格清零等價於遞推後虛擬第 n+1 列全零。模 3 高斯消去求得首列後，回代遞推的操作必清零。
complexity:
  time: O(nm³)
  space: O(nm²)
common_errors:
  - 中心係數 2 寫成 1
  - 邊界相鄰格越界
  - 自由變數未設為零
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
    // TODO：依三段提示建立核心狀態與演算法。
    return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int md(int x){x%=3;if(x<0)x+=3;return x;}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tc;cin>>tc;while(tc--){int n,m;cin>>n>>m;vector<vector<int>>a(n,vector<int>(m));for(auto&r:a)for(int&x:r)cin>>x;vector<vector<vector<int>>>rep(n+1,vector<vector<int>>(m,vector<int>(m+1)));for(int j=0;j<m;++j)rep[0][j][j]=1;for(int i=0;i<n;++i)for(int j=0;j<m;++j)for(int k=0;k<=m;++k){int v=-(k==m?a[i][j]:0)-2*rep[i][j][k];if(i>0)v-=rep[i-1][j][k];if(j>0)v-=rep[i][j-1][k];if(j+1<m)v-=rep[i][j+1][k];rep[i+1][j][k]=md(v);}vector<vector<int>>e(m,vector<int>(m+1));for(int i=0;i<m;++i){for(int j=0;j<m;++j)e[i][j]=rep[n][i][j];e[i][m]=md(-rep[n][i][m]);}vector<int>where(m,-1);int row=0;for(int col=0;col<m&&row<m;++col){int sel=row;while(sel<m&&e[sel][col]==0)++sel;if(sel==m)continue;swap(e[sel],e[row]);where[col]=row;if(e[row][col]==2)for(int j=col;j<=m;++j)e[row][j]=md(e[row][j]*2);for(int i=0;i<m;++i)if(i!=row&&e[i][col]){int f=e[i][col];for(int j=col;j<=m;++j)e[i][j]=md(e[i][j]-f*e[row][j]);}++row;}vector<int>first(m);for(int j=0;j<m;++j)if(where[j]!=-1)first[j]=e[where[j]][m];vector<vector<int>>op(n,vector<int>(m));for(int i=0;i<n;++i)for(int j=0;j<m;++j){int v=rep[i][j][m];for(int k=0;k<m;++k)v+=rep[i][j][k]*first[k];op[i][j]=md(v);}int total=0;for(const auto&r:op)for(int x:r)total+=x;cout<<total<<"\n";for(int i=0;i<n;++i)for(int j=0;j<m;++j)for(int z=0;z<op[i][j];++z)cout<<i+1<<' '<<j+1<<"\n";}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5755
external_platform: HDU
external_problem_id: '5755'
external_title: Gambler Bo
external_relation: original
review_status: verified
---

把第一列操作次數當 m 個未知數。由某列及其上一列可唯一推出下一列；將每格表示成第一列未知數的仿射式。要求最後一列下方的虛擬列為零，只需解 m 元模 3 方程。
