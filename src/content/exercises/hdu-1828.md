---
id: hdu-1828
volume: upper
source_file: upper-volume
title: HDU 1828 Picture
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - sweep-line
  - segment-count
  - union-perimeter
prerequisites:
  - segment-tree
statement: 求多個軸平行整數矩形聯集的外周長。
constraints:
  - n <= 5000
  - 座標絕對值 <= 10000
  - 讀到 EOF
input_format: 每組 n 與 n 個矩形左下、右上座標。
output_format: 每組輸出聯集周長。
samples:
  - input: |
      2
      0 0 2 2
      1 1 3 3
    output: |
      12
    explanation: 兩正方形重疊後聯集周長為十二。
core_knowledge: *id001
judgment: 掃描 y，維護橫向聯集長與連通段數。
hints:
  - 線段樹除覆蓋長外，再維護覆蓋連通段數與左右端是否覆蓋。
  - 垂直邊增量為 2*連通段數*dy。
  - 處理水平事件前後覆蓋長差的絕對值；同 y 先加入再移除避免假邊。
solution_outline: 掃描 y，維護橫向聯集長與連通段數。
proof_or_invariant: 每個帶狀區間每段有左右兩條垂直邊；事件造成的聯集長變化對應真正水平邊，排序消除同高相接假邊。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O(n log n)
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;struct Event{int y,x1,x2,d;};struct Node{int cover=0,length=0,segments=0;bool left=false,right=false;};static vector<Node>tr;static vector<int>xs;static void pull(int p,int l,int r){Node&z=tr[static_cast<size_t>(p)];if(z.cover>0){z.length=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)];z.segments=1;z.left=z.right=true;}else if(l==r){z.length=z.segments=0;z.left=z.right=false;}else{Node&a=tr[static_cast<size_t>(p*2)];Node&b=tr[static_cast<size_t>(p*2+1)];z.length=a.length+b.length;z.segments=a.segments+b.segments-(a.right&&b.left?1:0);z.left=a.left;z.right=b.right;}}static void update(int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].cover+=d;else{int m=(l+r)/2;update(p*2,l,m,ql,qr,d);update(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){vector<Event>e;xs.clear();for(int i=0;i<n;++i){int x1,y1,x2,y2;cin>>x1>>y1>>x2>>y2;e.push_back({y1,x1,x2,1});e.push_back({y2,x1,x2,-1});xs.push_back(x1);xs.push_back(x2);}sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(e.begin(),e.end(),[](const Event&a,const Event&b){return a.y!=b.y?a.y<b.y:a.d>b.d;});tr.assign(xs.size()*4+4,Node{});long long perimeter=0;int previous_y=e.empty()?0:e[0].y;for(auto event:e){perimeter+=2LL*tr[1].segments*(event.y-previous_y);int old=tr[1].length;int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),event.x1)-xs.begin());int r=static_cast<int>(lower_bound(xs.begin(),xs.end(),event.x2)-xs.begin())-1;update(1,0,static_cast<int>(xs.size())-2,l,r,event.d);perimeter+=abs(tr[1].length-old);previous_y=event.y;}cout<<perimeter<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct Event{int y,x1,x2,d;};struct Node{int cover=0,length=0,segments=0;bool left=false,right=false;};static vector<Node>tr;static vector<int>xs;static void pull(int p,int l,int r){Node&z=tr[static_cast<size_t>(p)];if(z.cover>0){z.length=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)];z.segments=1;z.left=z.right=true;}else if(l==r){z.length=z.segments=0;z.left=z.right=false;}else{Node&a=tr[static_cast<size_t>(p*2)];Node&b=tr[static_cast<size_t>(p*2+1)];z.length=a.length+b.length;z.segments=a.segments+b.segments-(a.right&&b.left?1:0);z.left=a.left;z.right=b.right;}}static void update(int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].cover+=d;else{int m=(l+r)/2;update(p*2,l,m,ql,qr,d);update(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){vector<Event>e;xs.clear();for(int i=0;i<n;++i){int x1,y1,x2,y2;cin>>x1>>y1>>x2>>y2;e.push_back({y1,x1,x2,1});e.push_back({y2,x1,x2,-1});xs.push_back(x1);xs.push_back(x2);}sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(e.begin(),e.end(),[](const Event&a,const Event&b){return a.y!=b.y?a.y<b.y:a.d>b.d;});tr.assign(xs.size()*4+4,Node{});long long perimeter=0;int previous_y=e.empty()?0:e[0].y;for(auto event:e){perimeter+=2LL*tr[1].segments*(event.y-previous_y);int old=tr[1].length;int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),event.x1)-xs.begin());int r=static_cast<int>(lower_bound(xs.begin(),xs.end(),event.x2)-xs.begin())-1;update(1,0,static_cast<int>(xs.size())-2,l,r,event.d);perimeter+=abs(tr[1].length-old);previous_y=event.y;}cout<<perimeter<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1828
external_platform: HDU
external_problem_id: '1828'
external_title: HDU 1828 Picture
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
