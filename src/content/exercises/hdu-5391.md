---
id: hdu-5391
volume: lower
source_file: lower-volume
title: HDU 5391 Zball in Tina Town
chapter: 6
section: '6.11'
kind: external-oj
difficulty: 2
topics:
  - 威爾遜定理
  - 質數判定
prerequisites:
  - wilson-theorem
statement: >-
  對每個 n，計算 (n-1)! mod n。
constraints:
  - 1 <= T <= 10^5
  - 2 <= n <= 10^9
input_format: >-
  第一行 T，接著 T 行各一個 n。
output_format: >-
  每組輸出 (n-1)! mod n。
samples:
  - input: |
      2
      3
      10
    output: |
      2
      0
    explanation: >-
      2! mod 3=2；9! 含因數 2 與 5，故可被 10 整除；可信 HDU 存檔的官方樣例。
hints:
  - >-
    n 為質數時，威爾遜定理給出 (n-1)!≡-1 mod n。
  - >-
    除 n=4 外，合數 n 都有足夠因子使 n 整除 (n-1)!。
  - >-
    因此只需判質數並特判 4。
core_knowledge:
  - 威爾遜定理
  - 合數階乘餘數
judgment: >-
  T 可達十萬，不能真的計算階乘；公式把每組化為一次快速判質。
solution_outline: >-
  若 n=4 輸出 2；否則若 n 為質數輸出 n-1，其餘輸出 0。使用確定性 Miller–Rabin 快速處理大量組數。
proof_or_invariant: >-
  質數情形由威爾遜定理。合數 n=ab 且 2<=a<=b<n 時，通常 a,b 都出現在階乘中；平方情形也可由兩份質因數取得，唯一不足的是 4，直接算得 3! mod 4=2。
common_errors:
  - 忘記 n=4 的唯一例外
  - 把質數答案輸出成 -1 而非 n-1
  - 對每組線性計算階乘
complexity:
  time: 每組 O(log n)
  space: O(1)
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;

  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      // TODO：依照三個提示完成演算法；先保留可編譯的輸入輸出骨架。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  using U64 = uint64_t;
  static U64 power_mod(U64 base, U64 exponent, U64 mod) {
      U64 result = 1;
      while (exponent > 0) {
          if ((exponent & 1U) != 0U) result = result * base % mod;
          base = base * base % mod; exponent >>= 1U;
      }
      return result;
  }
  static bool is_prime(U64 n) {
      if (n < 2) return false;
      for (U64 p : {2U, 3U, 5U, 7U, 11U}) if (n % p == 0) return n == p;
      U64 odd = n - 1; int twos = 0;
      while ((odd & 1U) == 0U) { odd >>= 1U; ++twos; }
      for (U64 base : {2U, 3U, 5U, 7U, 11U}) {
          if (base >= n) continue;
          U64 value = power_mod(base, odd, n);
          if (value == 1 || value == n - 1) continue;
          bool passed = false;
          for (int i = 1; i < twos; ++i) {
              value = value * value % n;
              if (value == n - 1) { passed = true; break; }
          }
          if (!passed) return false;
      }
      return true;
  }
  int main() {
      ios::sync_with_stdio(false); cin.tie(nullptr);
      int tests; cin >> tests;
      while (tests-- > 0) {
          U64 n; cin >> n;
          if (n == 4) cout << "2\n";
          else cout << (is_prime(n) ? n - 1 : 0) << '\n';
      }
      return 0;
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5391
external_platform: HDU
external_problem_id: '5391'
external_title: 'Zball in Tina Town'
external_relation: original
original_label: 'HDU 5391'
source_book_pages: [430, 433]
source_pdf_pages: [60, 63]
review_status: verified
---

威爾遜定理把巨大階乘壓成三種輸出。

原始題單中本題位於第 6.11 節、例題 第 1 題；競賽來源記為「BestCoder Round #51」。可用小範圍直接枚舉作為對拍程式，逐一比較最佳化版本。
