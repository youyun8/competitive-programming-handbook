---
id: hdu-3394
volume: lower
source_file: lower-volume
original_label: 'HDU 3394'
title: 'HDU 3394 Railway'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 4
topics: [vertex-biconnected-components, bridge]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  公園要沿道路建鐵路並為每個環安排路線；不屬於環的道路不必建，屬於至少兩個環的道路可能衝突。求兩類道路數。
constraints: [無自環與重邊, 多組 n m 以 0 0 結束]
input_format: >-
  每組 n m，後接 m 行 0-based 無向邊。
output_format: >-
  每組輸出不必建道路數與衝突道路數。
samples:
  - input: |
      3 3
      0 1
      1 2
      2 0
      0 0
    output: |
      0 0
    explanation: >-
      三條邊各只屬於唯一的一個環。
core_knowledge: [橋, 點雙連通分量, 邊點數關係]
judgment: >-
  橋不屬於任何環；一個點雙分量若邊數大於點數，其中所有邊都屬於至少兩種環路安排。
hints:
  - DFS 樹邊 low[v]>dfn[u] 是橋。
  - 以邊堆疊切出每個點雙分量。
  - 彈出時同時計邊與去重後頂點。
solution_outline: >-
  Tarjan 過程統計橋；每逢 low[v]>=dfn[u] 彈出一個點雙分量，若 E>V 將 E 加入衝突數。
proof_or_invariant: >-
  邊在環上等價於非橋。點雙分量的耳分解表明 E=V 時只有單一簡單環；E>V 時存在額外耳，使分量內邊落入重疊環，正是衝突集合。
complexity: { time: 'O(n + m)', space: 'O(n + m)' }
common_errors: [用邊雙分量判衝突, 父邊重複推入堆疊, 只加 E-V 而非整個 E]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m&&(n||m)){vector<vector<pair<int,int>>>g(n);vector<pair<int,int>>edges(m);for(int i=0;i<m;++i){auto&[u,v]=edges[i];cin>>u>>v;g[u].push_back({v,i});g[v].push_back({u,i});}vector<int>d(n),low(n),stack_edges;int timer=0,bridges=0,conflict=0;auto dfs=[&](auto&&self,int u,int pe)->void{d[u]=low[u]=++timer;for(auto [v,id]:g[u])if(id!=pe){if(!d[v]){stack_edges.push_back(id);self(self,v,id);low[u]=min(low[u],low[v]);if(low[v]>d[u])++bridges;if(low[v]>=d[u]){set<int>vertices;int edge_total=0,last;do{last=stack_edges.back();stack_edges.pop_back();++edge_total;vertices.insert(edges[last].first);vertices.insert(edges[last].second);}while(last!=id);if(edge_total>static_cast<int>(vertices.size()))conflict+=edge_total;}}else if(d[v]<d[u])stack_edges.push_back(id),low[u]=min(low[u],d[v]);}};for(int i=0;i<n;++i)if(!d[i])dfs(dfs,i,-1);cout<<bridges<<' '<<conflict<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3394
external_platform: HDU
external_problem_id: '3394'
external_title: 'Railway'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
