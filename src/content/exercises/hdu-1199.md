---
id: hdu-1199
volume: upper
source_file: upper-volume
title: HDU 1199 Color the Ball
chapter: 2
section: '2.7'
kind: external-oj
difficulty: 4
topics: ['座標離散化', '區間覆蓋']
prerequisites: []
statement: |-
  無限整數編號球初始皆黑；依序把閉區間染白或黑，求最後最長白色連續段，並列最左者。
constraints:
  - '每組操作 n<=2000；端點小於 2^31-1；讀至 EOF。'
input_format: '依上列敘述順序輸入所有參數與操作。'
output_format: '依題意輸出每組答案。'
samples:
  - input: |
      3
      1 4 w
      2 3 b
      5 5 w
    output: |
      4 5
    explanation: '依操作或定義直接計算可得此結果。'
core_knowledge: ['座標離散化', '區間覆蓋']
judgment: '座標或狀態範圍大，但實際事件有限，應用本節離散化、差分或狀態壓縮。'
hints:
  [
    '先找出真正會改變答案的事件或邊界。',
    '將大座標／大量區間轉成有限狀態後依序處理。',
    '維持狀態不變量，最後掃描所有候選並取最優。'
  ]
solution_outline: '依核心知識壓縮資料，再按事件順序更新並輸出答案。'
proof_or_invariant: '壓縮後每個原始事件仍有唯一對應，且事件間狀態不變；逐段計算涵蓋所有候選，因此所得值與原問題相同。'
common_errors: ['閉區間端點處理錯誤', '多組資料未清空', '計數或面積使用 int 溢位']
complexity:
  time: 'O(n log n) 或與壓縮後網格大小同階'
  space: 'O(n) 或與壓縮後網格大小同階'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：依提示完成。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct O{long long l,r;char c;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){vector<O>o(n);vector<long long>x;for(auto&z:o){cin>>z.l>>z.r>>z.c;if(z.l>z.r)swap(z.l,z.r);x.push_back(z.l);x.push_back(z.r+1);}sort(x.begin(),x.end());x.erase(unique(x.begin(),x.end()),x.end());vector<char>c(x.size(),'b');for(auto z:o){size_t l=static_cast<size_t>(lower_bound(x.begin(),x.end(),z.l)-x.begin()),r=static_cast<size_t>(lower_bound(x.begin(),x.end(),z.r+1)-x.begin());fill(c.begin()+static_cast<ptrdiff_t>(l),c.begin()+static_cast<ptrdiff_t>(r),z.c);}long long best=0,bl=0,br=0,start=0;bool run=false;for(size_t i=0;i+1<x.size();++i){if(c[i]=='w'&&!run){start=x[i];run=true;}if(run&&(c[i]!='w'||i+2==x.size()||c[i+1]!='w')){long long end=c[i]=='w'?x[i+1]-1:x[i]-1,len=end-start+1;if(len>best){best=len;bl=start;br=end;}run=false;}}if(best==0)cout<<"Oh, my god\n";else cout<<bl<<' '<<br<<'\n';}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1199
external_platform: HDU
external_problem_id: '1199'
external_title: 'Color the Ball'
external_relation: original
source_book_pages: [78]
source_pdf_pages: [96]
review_status: verified
---

本題採獨立敘述與 C++17 解法。
