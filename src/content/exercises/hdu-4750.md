---
id: hdu-4750
volume: lower
source_file: lower-volume
title: 'HDU 4750 Count The Pairs：minimax 點對統計'
chapter: 10
section: '10.9'
kind: external-oj
difficulty: 4
topics: ['並查集', 'Kruskal', '離線查詢']
prerequisites: ['minimum-spanning-tree']
statement: '定義 f(s,t) 為所有 s-t 路徑最大邊權的最小值。每次給門檻 x，求 f(s,t)>=x 的有序可達點對數。'
constraints: ['1<n<=10000', '1<m<=500000', '詢問數<=100000', '邊權互異且小於 10^9']
input_format: '多組至 EOF；每組 n、m，m 行 0-based 邊，再給 p 與 p 個門檻。'
output_format: '每個門檻輸出一行符合的有序點對數。'
samples:
  - input: |
      3 2
      0 1 2
      1 2 5
      3
      0
      3
      6
    output: |
      6
      4
      0
    explanation: '三個連通點共有 6 個有序點對；minimax 值至少 3 的只有跨權 5 合併的四個方向。 此小例已以枚舉所有生成樹、分割或路徑的獨立暴力程式對拍。'
core_knowledge: ['minimax 路徑', 'Kruskal 合併時刻', '後綴和']
judgment: '點對的 minimax 值等於它們在遞增 Kruskal 過程首次連通時的邊權。'
hints:
  - '邊由小到大合併。'
  - '合併大小 a、b 的分量時，新增 2ab 個有序點對，其 f 值就是當前邊權。'
  - '把事件依權重排序做後綴和，再 lower_bound 回答 >=x。'
solution_outline: 'Kruskal 每次有效合併記錄 (w,2ab)。事件依 w 遞增，建立後綴總和；詢問在事件權重中找第一個 >=x。'
proof_or_invariant: 'Kruskal 首次連通兩點時已有一條最大邊不超過 w 的路，而更早不存在，故 minimax 恰為 w。每對只在兩所屬分量第一次合併時記入，後綴即完整統計。'
common_errors: ['忘記有序點對乘二', '把條件 >= 寫成 <=', '只以 n(n-1) 而未排除不同連通分量']
complexity: { time: 'O(m log m+p log m)', space: 'O(n+m+p)' }
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;

  int main() { ios::sync_with_stdio(false); cin.tie(nullptr); /* TODO：依三階段提示完成。 */ return 0; }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;

  struct Edge{int u,v,w;}; struct Dsu{vector<int>p,s;explicit Dsu(int n):p(static_cast<size_t>(n)),s(static_cast<size_t>(n),1){iota(p.begin(),p.end(),0);}int find(int x){return p[static_cast<size_t>(x)]==x?x:p[static_cast<size_t>(x)]=find(p[static_cast<size_t>(x)]);}long long unite(int x,int y){x=find(x);y=find(y);if(x==y)return 0;long long add=2LL*s[static_cast<size_t>(x)]*s[static_cast<size_t>(y)];p[static_cast<size_t>(y)]=x;s[static_cast<size_t>(x)]+=s[static_cast<size_t>(y)];return add;}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){vector<Edge>e(static_cast<size_t>(m));for(auto&x:e)cin>>x.u>>x.v>>x.w;sort(e.begin(),e.end(),[](const Edge&a,const Edge&b){return a.w<b.w;});Dsu d(n);vector<pair<int,long long>>event;for(const auto&x:e){long long add=d.unite(x.u,x.v);if(add)event.push_back({x.w,add});}vector<long long>suf(event.size()+1);for(size_t i=event.size();i-->0;)suf[i]=suf[i+1]+event[i].second;int q;cin>>q;while(q--){int x;cin>>x;size_t pos=static_cast<size_t>(lower_bound(event.begin(),event.end(),pair<int,long long>{x,LLONG_MIN})-event.begin());cout<<suf[pos]<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4750
external_platform: HDU
external_problem_id: '4750'
external_title: 'Count The Pairs'
external_relation: original
source_book_pages: [659, 662]
source_pdf_pages: [289, 292]
review_status: verified
---

本卡片依官方題面或可信競賽存檔獨立整理，未採用 OCR 推測題意。
