---
id: 洛谷-p4951
volume: lower
source_file: lower-volume
title: '洛谷 P4951 Earthquake：01 分數規劃生成樹'
chapter: 10
section: '10.9'
kind: external-oj
difficulty: 4
topics: ['01 分數規劃', 'Kruskal', '最小生成樹']
prerequisites: ['minimum-spanning-tree']
statement: '每條候選道路有成本與工期；選路使所有牧場連通，最大化（固定報酬 f－總成本）／總工期，無正利潤輸出 0。'
constraints: ['1 <= n <= 400', '1 <= m <= 10000', '1 <= f,c,t <= 2*10^9', '圖保證連通']
input_format: '第一行 n、m、f；接著 m 行 u、v、c、t。'
output_format: '最大單位時間利潤，固定四位小數；無正利潤輸出 0.0000。'
samples:
  - input: |
      2 1 10
      1 2 4 3
    output: |
      2.0000
    explanation: '唯一生成樹的利潤率為 (10-4)/3=2。 此小例已以枚舉所有生成樹、分割或路徑的獨立暴力程式對拍。'
core_knowledge: ['01 分數規劃', '參數化邊權', 'Kruskal']
judgment: '目標是兩個加總量的比值，固定答案 x 後可改寫成最小化 c+x*t 的生成樹。'
hints:
  - '二分候選利潤率 x。'
  - '把每條邊改權為 c+x*t，求最小生成樹權重。'
  - '若最小權重不超過 f，代表存在利潤率至少 x 的方案。'
solution_outline: '在 [0,f] 二分 80 次；每次按 c+x*t 排序做 Kruskal，依 MST 新權和是否 <=f 決定下界。'
proof_or_invariant: '對任一樹 S，利潤率至少 x 等價於 Σ(c_i+x t_i)<=f；所有樹中右側最小值正是新權 MST。可行性對 x 單調，二分收斂至最優率。'
common_errors: ['新權誤寫成 c-x*t', '未把負答案截為 0', '整數乘法在轉 double 前溢位']
complexity: { time: 'O(80m log m)', space: 'O(n+m)' }
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;

  int main() { ios::sync_with_stdio(false); cin.tie(nullptr); /* TODO：依三階段提示完成。 */ return 0; }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;

  struct Edge { int u, v; long long cost, duration; double weight; };
  struct Dsu { vector<int> p; explicit Dsu(int n):p(static_cast<size_t>(n+1)){iota(p.begin(),p.end(),0);} int find(int x){return p[static_cast<size_t>(x)]==x?x:p[static_cast<size_t>(x)]=find(p[static_cast<size_t>(x)]);} bool unite(int x,int y){x=find(x);y=find(y);if(x==y)return false;p[static_cast<size_t>(x)]=y;return true;}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;long long reward;if(!(cin>>n>>m>>reward))return 0;vector<Edge> edges(static_cast<size_t>(m));for(auto& e:edges)cin>>e.u>>e.v>>e.cost>>e.duration;auto feasible=[&](double ratio){for(auto& e:edges)e.weight=static_cast<double>(e.cost)+ratio*static_cast<double>(e.duration);sort(edges.begin(),edges.end(),[](const Edge&a,const Edge&b){return a.weight<b.weight;});Dsu dsu(n);double sum=0;int used=0;for(const auto&e:edges)if(dsu.unite(e.u,e.v)){sum+=e.weight;if(++used==n-1)break;}return sum<=static_cast<double>(reward);};double low=0,high=static_cast<double>(reward);for(int it=0;it<80;++it){double mid=(low+high)/2;if(feasible(mid))low=mid;else high=mid;}cout<<fixed<<setprecision(4)<<low+1e-10<<'\n';}
external_url: https://www.luogu.com.cn/problem/P4951
external_platform: 洛谷
external_problem_id: 'P4951'
external_title: '[USACO01OPEN] Earthquake'
external_relation: original
source_book_pages: [662]
source_pdf_pages: [292]
review_status: verified
---

本卡片依官方題面或可信競賽存檔獨立整理，未採用 OCR 推測題意。
