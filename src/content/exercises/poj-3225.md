---
id: poj-3225
volume: upper
source_file: upper-volume
title: POJ 3225 Help with Intervals
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - coordinate-doubling
  - lazy-assignment
  - lazy-xor
prerequisites:
  - segment-tree
statement: 從空集合開始，依序執行區間聯集、交集、差集、補差與對稱差，輸出終態。
constraints:
  - 端點整數 0..65535
  - 輸入至 EOF
input_format: 命令 U/I/D/C/S 加一個開閉區間。
output_format: 輸出不相交區間序列或 empty set。
samples:
  - input: |
      U [1,5]
      D (2,3)
    output: |
      [1,2] [3,5]
    explanation: 先加入閉區間，再刪除開區間 (2,3)。
core_knowledge: *id001
judgment: 倍增座標上的二元集合線段樹。
hints:
  - 座標乘二：偶數代表端點，奇數代表端點間。
  - U/D 是區間設 1/0；S 是翻轉。
  - I 清區間外；C 清外部並翻轉內部。
solution_outline: 倍增座標上的二元集合線段樹。
proof_or_invariant: 倍增後每種開閉區間精確對應整數索引閉區間；五種命令逐點布林語意與懶操作一致。
common_errors:
  - 端點、開閉區間或 0/1 起始索引處理錯誤
  - 合併摘要時遺漏跨左右區間的候選
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(K log C+C)
  space: O(C)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;constexpr int LIM=131071;struct N{int set=0;bool flip=false;};static vector<N>tr(static_cast<size_t>(4*LIM+8));static void apply_set(int p,int v){tr[static_cast<size_t>(p)].set=v;tr[static_cast<size_t>(p)].flip=false;}static void apply_flip(int p){if(tr[static_cast<size_t>(p)].set!=-1)tr[static_cast<size_t>(p)].set^=1;else tr[static_cast<size_t>(p)].flip=!tr[static_cast<size_t>(p)].flip;}static void push(int p){if(tr[static_cast<size_t>(p)].set!=-1){apply_set(p*2,tr[static_cast<size_t>(p)].set);apply_set(p*2+1,tr[static_cast<size_t>(p)].set);tr[static_cast<size_t>(p)].set=-1;}if(tr[static_cast<size_t>(p)].flip){apply_flip(p*2);apply_flip(p*2+1);tr[static_cast<size_t>(p)].flip=false;}}static void assign_r(int p,int l,int r,int ql,int qr,int v){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply_set(p,v);return;}push(p);int m=(l+r)/2;assign_r(p*2,l,m,ql,qr,v);assign_r(p*2+1,m+1,r,ql,qr,v);int a=tr[static_cast<size_t>(p*2)].set,b=tr[static_cast<size_t>(p*2+1)].set;tr[static_cast<size_t>(p)].set=a==b?a:-1;}static void flip_r(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply_flip(p);return;}push(p);int m=(l+r)/2;flip_r(p*2,l,m,ql,qr);flip_r(p*2+1,m+1,r,ql,qr);int a=tr[static_cast<size_t>(p*2)].set,b=tr[static_cast<size_t>(p*2+1)].set;tr[static_cast<size_t>(p)].set=a==b?a:-1;}static void collect(int p,int l,int r,vector<char>&v){if(tr[static_cast<size_t>(p)].set==0)return;if(tr[static_cast<size_t>(p)].set==1){fill(v.begin()+l,v.begin()+r+1,1);return;}push(p);int m=(l+r)/2;collect(p*2,l,m,v);collect(p*2+1,m+1,r,v);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);string line;while(getline(cin,line)){char op,lc,rc;int a,b;if(sscanf(line.c_str(),"%c %c%d,%d%c",&op,&lc,&a,&b,&rc)!=5)continue;int l=2*a+(lc=='('),r=2*b-(rc==')');if(op=='I'||op=='C'){if(l>0)assign_r(1,0,LIM,0,l-1,0);if(r<LIM)assign_r(1,0,LIM,r+1,LIM,0);}if(l<=r){if(op=='U')assign_r(1,0,LIM,l,r,1);else if(op=='D')assign_r(1,0,LIM,l,r,0);else if(op=='C'||op=='S')flip_r(1,0,LIM,l,r);}else if(op=='I'||op=='C')assign_r(1,0,LIM,0,LIM,0);}vector<char>v(static_cast<size_t>(LIM+1));collect(1,0,LIM,v);bool any=false;for(int i=0;i<=LIM;++i)if(v[static_cast<size_t>(i)]!=0){int j=i;while(j<LIM&&v[static_cast<size_t>(j+1)]!=0)++j;if(any)cout<<' ';cout<<(i&1?'(':'[')<<i/2<<','<<(j&1?(j+1)/2:j/2)<<(j&1?')':']');any=true;i=j;}if(!any)cout<<"empty set";cout<<'\n';}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;constexpr int LIM=131071;struct N{int set=0;bool flip=false;};static vector<N>tr(static_cast<size_t>(4*LIM+8));static void apply_set(int p,int v){tr[static_cast<size_t>(p)].set=v;tr[static_cast<size_t>(p)].flip=false;}static void apply_flip(int p){if(tr[static_cast<size_t>(p)].set!=-1)tr[static_cast<size_t>(p)].set^=1;else tr[static_cast<size_t>(p)].flip=!tr[static_cast<size_t>(p)].flip;}static void push(int p){if(tr[static_cast<size_t>(p)].set!=-1){apply_set(p*2,tr[static_cast<size_t>(p)].set);apply_set(p*2+1,tr[static_cast<size_t>(p)].set);tr[static_cast<size_t>(p)].set=-1;}if(tr[static_cast<size_t>(p)].flip){apply_flip(p*2);apply_flip(p*2+1);tr[static_cast<size_t>(p)].flip=false;}}static void assign_r(int p,int l,int r,int ql,int qr,int v){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply_set(p,v);return;}push(p);int m=(l+r)/2;assign_r(p*2,l,m,ql,qr,v);assign_r(p*2+1,m+1,r,ql,qr,v);int a=tr[static_cast<size_t>(p*2)].set,b=tr[static_cast<size_t>(p*2+1)].set;tr[static_cast<size_t>(p)].set=a==b?a:-1;}static void flip_r(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply_flip(p);return;}push(p);int m=(l+r)/2;flip_r(p*2,l,m,ql,qr);flip_r(p*2+1,m+1,r,ql,qr);int a=tr[static_cast<size_t>(p*2)].set,b=tr[static_cast<size_t>(p*2+1)].set;tr[static_cast<size_t>(p)].set=a==b?a:-1;}static void collect(int p,int l,int r,vector<char>&v){if(tr[static_cast<size_t>(p)].set==0)return;if(tr[static_cast<size_t>(p)].set==1){fill(v.begin()+l,v.begin()+r+1,1);return;}push(p);int m=(l+r)/2;collect(p*2,l,m,v);collect(p*2+1,m+1,r,v);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);string line;while(getline(cin,line)){char op,lc,rc;int a,b;if(sscanf(line.c_str(),"%c %c%d,%d%c",&op,&lc,&a,&b,&rc)!=5)continue;int l=2*a+(lc=='('),r=2*b-(rc==')');if(op=='I'||op=='C'){if(l>0)assign_r(1,0,LIM,0,l-1,0);if(r<LIM)assign_r(1,0,LIM,r+1,LIM,0);}if(l<=r){if(op=='U')assign_r(1,0,LIM,l,r,1);else if(op=='D')assign_r(1,0,LIM,l,r,0);else if(op=='C'||op=='S')flip_r(1,0,LIM,l,r);}else if(op=='I'||op=='C')assign_r(1,0,LIM,0,LIM,0);}vector<char>v(static_cast<size_t>(LIM+1));collect(1,0,LIM,v);bool any=false;for(int i=0;i<=LIM;++i)if(v[static_cast<size_t>(i)]!=0){int j=i;while(j<LIM&&v[static_cast<size_t>(j+1)]!=0)++j;if(any)cout<<' ';cout<<(i&1?'(':'[')<<i/2<<','<<(j&1?(j+1)/2:j/2)<<(j&1?')':']');any=true;i=j;}if(!any)cout<<"empty set";cout<<'\n';}
external_url: http://poj.org/problem?id=3225
external_platform: POJ
external_problem_id: '3225'
external_title: POJ 3225 Help with Intervals
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
