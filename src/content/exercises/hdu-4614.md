---
id: hdu-4614
volume: upper
source_file: upper-volume
title: HDU 4614 Vases and Flowers
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - lazy-segment-tree
  - order-statistics
  - range-assignment
prerequisites:
  - segment-tree
statement: N 個 0 起編號花瓶；從 A 起把 F 朵花放入空瓶，或清空 [A,B]。
constraints:
  - N,M < 50001
  - 每瓶至多一朵花
input_format: T 組；1 A F 放花，2 A B 清理。
output_format: 放花輸出首末瓶或固定失敗字串；清理輸出丟棄花數；組間空行。
samples:
  - input: |
      1
      5 3
      1 1 3
      2 2 3
      1 0 3
    output: |+
      1 3
      2
      0 3

    explanation: 先占 1..3，清空 2..3，再從零起放入三個空瓶至 3。
core_knowledge: *id001
judgment: 區間賦值線段樹加第 k 個零定位。
hints:
  - 節點維護已占瓶數並支援整段設 0/1。
  - 先算 [A,N-1] 空瓶數；首末位置是全域第 k 個空瓶。
  - 找第 k 個空瓶後，把首末間整段設 1；中間原占用不受語意影響。
solution_outline: 區間賦值線段樹加第 k 個零定位。
proof_or_invariant: 第 k 個零定位按左右空瓶數選支；首末選中間的每個空瓶恰是本次要放花的位置。
common_errors:
  - 懶標記合成順序錯誤
  - 閉區間端點或 0/1 起始索引混淆
  - 合併節點摘要時漏掉跨左右子樹的候選
complexity:
  time: O(M log N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示完成資料結構。
  #include <bits/stdc++.h>
  using namespace std;struct Node{int ones=0,lazy=-1;};static vector<Node>tr;static void apply(int p,int l,int r,int v){tr[static_cast<size_t>(p)].ones=v*(r-l+1);tr[static_cast<size_t>(p)].lazy=v;}static void push(int p,int l,int r){if(tr[static_cast<size_t>(p)].lazy<0||l==r)return;int m=(l+r)/2;apply(p*2,l,m,tr[static_cast<size_t>(p)].lazy);apply(p*2+1,m+1,r,tr[static_cast<size_t>(p)].lazy);tr[static_cast<size_t>(p)].lazy=-1;}static void assign_range(int p,int l,int r,int ql,int qr,int v){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,l,r,v);return;}push(p,l,r);int m=(l+r)/2;assign_range(p*2,l,m,ql,qr,v);assign_range(p*2+1,m+1,r,ql,qr,v);tr[static_cast<size_t>(p)].ones=tr[static_cast<size_t>(p*2)].ones+tr[static_cast<size_t>(p*2+1)].ones;}static int query(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].ones;push(p,l,r);int m=(l+r)/2;return query(p*2,l,m,ql,qr)+query(p*2+1,m+1,r,ql,qr);}static int kth_zero(int p,int l,int r,int k){if(l==r)return l;push(p,l,r);int m=(l+r)/2;int left_zero=m-l+1-tr[static_cast<size_t>(p*2)].ones;return k<=left_zero?kth_zero(p*2,l,m,k):kth_zero(p*2+1,m+1,r,k-left_zero);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;tr.assign(static_cast<size_t>(4*n+4),Node{});while(m--){int op,a,b;cin>>op>>a>>b;if(op==1){int free=n-a-query(1,0,n-1,a,n-1);if(free==0){cout<<"Can not put any one.\n";continue;}int before=a-query(1,0,n-1,0,a-1);int take=min(free,b);int first=kth_zero(1,0,n-1,before+1),last=kth_zero(1,0,n-1,before+take);cout<<first<<' '<<last<<'\n';assign_range(1,0,n-1,first,last,1);}else{int removed=query(1,0,n-1,a,b);cout<<removed<<'\n';assign_range(1,0,n-1,a,b,0);}}cout<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct Node{int ones=0,lazy=-1;};static vector<Node>tr;static void apply(int p,int l,int r,int v){tr[static_cast<size_t>(p)].ones=v*(r-l+1);tr[static_cast<size_t>(p)].lazy=v;}static void push(int p,int l,int r){if(tr[static_cast<size_t>(p)].lazy<0||l==r)return;int m=(l+r)/2;apply(p*2,l,m,tr[static_cast<size_t>(p)].lazy);apply(p*2+1,m+1,r,tr[static_cast<size_t>(p)].lazy);tr[static_cast<size_t>(p)].lazy=-1;}static void assign_range(int p,int l,int r,int ql,int qr,int v){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,l,r,v);return;}push(p,l,r);int m=(l+r)/2;assign_range(p*2,l,m,ql,qr,v);assign_range(p*2+1,m+1,r,ql,qr,v);tr[static_cast<size_t>(p)].ones=tr[static_cast<size_t>(p*2)].ones+tr[static_cast<size_t>(p*2+1)].ones;}static int query(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].ones;push(p,l,r);int m=(l+r)/2;return query(p*2,l,m,ql,qr)+query(p*2+1,m+1,r,ql,qr);}static int kth_zero(int p,int l,int r,int k){if(l==r)return l;push(p,l,r);int m=(l+r)/2;int left_zero=m-l+1-tr[static_cast<size_t>(p*2)].ones;return k<=left_zero?kth_zero(p*2,l,m,k):kth_zero(p*2+1,m+1,r,k-left_zero);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;tr.assign(static_cast<size_t>(4*n+4),Node{});while(m--){int op,a,b;cin>>op>>a>>b;if(op==1){int free=n-a-query(1,0,n-1,a,n-1);if(free==0){cout<<"Can not put any one.\n";continue;}int before=a-query(1,0,n-1,0,a-1);int take=min(free,b);int first=kth_zero(1,0,n-1,before+1),last=kth_zero(1,0,n-1,before+take);cout<<first<<' '<<last<<'\n';assign_range(1,0,n-1,first,last,1);}else{int removed=query(1,0,n-1,a,b);cout<<removed<<'\n';assign_range(1,0,n-1,a,b,0);}}cout<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=4614
external_platform: HDU
external_problem_id: '4614'
external_title: HDU 4614 Vases and Flowers
external_relation: original
source_book_pages:
  - 182
  - 192
source_pdf_pages:
  - 200
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
