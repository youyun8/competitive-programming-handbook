---
id: poj-1722
volume: lower
source_file: lower-volume
source_book_pages:
  - 417
source_pdf_pages:
  - 47
title: OpenJ_Bailian 1722 SUBTRACT
chapter: 6
section: '6.7'
kind: external-oj
difficulty: 4
topics:
  - 高斯消元
  - 動態規劃
prerequisites:
  - 模運算與狀態還原
statement: 反覆將相鄰 a_i,a_{i+1} 合併成 a_i-a_{i+1}，輸出 n-1 個操作位置，使最後結果為目標 T。
constraints:
  - 1<=n<=100
  - 1<=a_i<=100，|T|<=10000；保證有解
input_format: 依題面讀入一組或多組矩陣、序列資料。
output_format: 依題面固定格式輸出數值、判定或操作方案。
samples:
  - input: |
      5 4
      12
      10
      4
      3
      5
    output: |
      2
      2
      1
      1
    explanation: 依題意模擬或代入方程可驗證此輸出。
core_knowledge:
  - 有限域消元
  - 方案構造
judgment: 展開括號後第一項為正、第二項為負，其餘項可選正負；DP 找符號方案。先依序消除正號項，再以位置 1 消除負號項。
hints:
  - 先確認操作可否轉成線性方程或符號選擇。
  - 利用模域消元、第一列枚舉或可達和 DP。
  - 除答案外還要保存最早行號、前驅或固定輸出格式。
solution_outline: 展開括號後第一項為正、第二項為負，其餘項可選正負；DP 找符號方案。先依序消除正號項，再以位置 1 消除負號項。
proof_or_invariant: DP 枚舉所有可達帶符號和；指定輸出順序可構造對應括號結構，逐步合併後展開恰為所選符號式。
complexity:
  time: O(n·20001)
  space: O(n·20001)
common_errors:
  - 星期首尾天數少加一
  - 自由變數與矛盾列判斷錯誤
  - 方案還原後操作下標未隨刪除調整
cpp_skeleton: |
  // TODO：理解證明後重寫核心消元或 DP。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,T;cin>>n>>T;vector<int>a(n+1),sg(n+1);for(int i=1;i<=n;++i)cin>>a[i];const int O=10000,W=20001;vector<vector<signed char>>pre(n+1,vector<signed char>(W));pre[1][a[1]+O]=1;if(n>=2)pre[2][a[1]-a[2]+O]=-1;for(int i=3;i<=n;++i)for(int v=0;v<W;++v)if(pre[i-1][v]){if(v+a[i]<W)pre[i][v+a[i]]=1;if(v-a[i]>=0)pre[i][v-a[i]]=-1;}int v=T+O;for(int i=n;i>=2;--i){sg[i]=pre[i][v];if(sg[i]==1)v-=a[i];else v+=a[i];}int removed=0;for(int i=2;i<=n;++i)if(sg[i]==1)cout<<i-1-removed++<<"\n";for(int i=2;i<=n;++i)if(sg[i]!=1)cout<<1<<"\n";}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,T;cin>>n>>T;vector<int>a(n+1),sg(n+1);for(int i=1;i<=n;++i)cin>>a[i];const int O=10000,W=20001;vector<vector<signed char>>pre(n+1,vector<signed char>(W));pre[1][a[1]+O]=1;if(n>=2)pre[2][a[1]-a[2]+O]=-1;for(int i=3;i<=n;++i)for(int v=0;v<W;++v)if(pre[i-1][v]){if(v+a[i]<W)pre[i][v+a[i]]=1;if(v-a[i]>=0)pre[i][v-a[i]]=-1;}int v=T+O;for(int i=n;i>=2;--i){sg[i]=pre[i][v];if(sg[i]==1)v-=a[i];else v+=a[i];}int removed=0;for(int i=2;i<=n;++i)if(sg[i]==1)cout<<i-1-removed++<<"\n";for(int i=2;i<=n;++i)if(sg[i]!=1)cout<<1<<"\n";}
external_url: http://bailian.openjudge.cn/practice/1722/
external_platform: OpenJ_Bailian
external_problem_id: '1722'
external_title: SUBTRACT
external_relation: original
review_status: verified
---

本題需同時證明判定與輸出構造正確。
