---
id: hdu-5306
volume: upper
source_file: upper-volume
title: HDU 5306 Gorgeous Sequence
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 5
topics: &id001
  - segment-tree-beats
  - range-chmin
  - potential-analysis
prerequisites:
  - segment-tree
statement: 序列支援區間 a_i=min(a_i,x)、查區間最大值、查區間和。
constraints:
  - T 組
  - n,m <= 100000
  - 索引從 0 起
input_format: 0 l r x 取 min；1 l r 查最大；2 l r 查和。
output_format: 依序輸出查詢。
samples:
  - input: |
      1
      5 4
      5 1 4 3 2
      1 0 4
      0 0 2 3
      2 0 4
      1 0 2
    output: |
      5
      12
      3
    explanation: 前綴壓至 3 後序列為 3,1,3,3,2。
core_knowledge: *id001
judgment: Segment Tree Beats 批次壓低最大值群。
hints:
  - 節點記最大、嚴格次大、最大值個數與總和。
  - 若 second_max < x < max，只會改最大值群，可 O(1) 套用。
  - x<=second_max 才下遞迴；每次困難更新都降低節點值結構勢能。
solution_outline: Segment Tree Beats 批次壓低最大值群。
proof_or_invariant: 次大值不變量保證快速情況只影響最大群；其餘遞迴後 pull 恢復摘要，勢能分析給攤銷界。
common_errors:
  - 懶標記合成順序錯誤
  - 閉區間端點或 0/1 起始索引混淆
  - 合併節點摘要時漏掉跨左右子樹的候選
complexity:
  time: O((n+m) log² n) 攤銷
  space: O(n)
cpp_skeleton: |
  // TODO：依三階段提示完成資料結構。
  #include <bits/stdc++.h>
  using namespace std;struct Node{long long sum=0,mx=LLONG_MIN,second=LLONG_MIN;int count=0;};static vector<Node>tr;static vector<long long>a;static void pull(int p){const Node&x=tr[static_cast<size_t>(p*2)];const Node&y=tr[static_cast<size_t>(p*2+1)];Node&z=tr[static_cast<size_t>(p)];z.sum=x.sum+y.sum;if(x.mx==y.mx){z.mx=x.mx;z.count=x.count+y.count;z.second=max(x.second,y.second);}else if(x.mx>y.mx){z.mx=x.mx;z.count=x.count;z.second=max(x.second,y.mx);}else{z.mx=y.mx;z.count=y.count;z.second=max(x.mx,y.second);}}static void apply_min(int p,long long v){Node&x=tr[static_cast<size_t>(p)];x.sum+=(v-x.mx)*x.count;x.mx=v;}static void push(int p){long long v=tr[static_cast<size_t>(p)].mx;if(tr[static_cast<size_t>(p*2)].mx>v)apply_min(p*2,v);if(tr[static_cast<size_t>(p*2+1)].mx>v)apply_min(p*2+1,v);}static void build(int p,int l,int r){if(l==r){tr[static_cast<size_t>(p)]={a[static_cast<size_t>(l)],a[static_cast<size_t>(l)],LLONG_MIN,1};return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);pull(p);}static void chmin_range(int p,int l,int r,int ql,int qr,long long v){if(qr<l||r<ql||tr[static_cast<size_t>(p)].mx<=v)return;if(ql<=l&&r<=qr&&tr[static_cast<size_t>(p)].second<v){apply_min(p,v);return;}push(p);int m=(l+r)/2;chmin_range(p*2,l,m,ql,qr,v);chmin_range(p*2+1,m+1,r,ql,qr,v);pull(p);}static long long query_sum(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].sum;push(p);int m=(l+r)/2;return query_sum(p*2,l,m,ql,qr)+query_sum(p*2+1,m+1,r,ql,qr);}static long long query_max(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return LLONG_MIN;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].mx;push(p);int m=(l+r)/2;return max(query_max(p*2,l,m,ql,qr),query_max(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;a.assign(static_cast<size_t>(n),0);for(auto&x:a)cin>>x;tr.assign(static_cast<size_t>(4*n+4),Node{});build(1,0,n-1);while(m--){int op,l,r;cin>>op>>l>>r;if(op==0){long long x;cin>>x;chmin_range(1,0,n-1,l,r,x);}else if(op==1)cout<<query_max(1,0,n-1,l,r)<<'\n';else cout<<query_sum(1,0,n-1,l,r)<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct Node{long long sum=0,mx=LLONG_MIN,second=LLONG_MIN;int count=0;};static vector<Node>tr;static vector<long long>a;static void pull(int p){const Node&x=tr[static_cast<size_t>(p*2)];const Node&y=tr[static_cast<size_t>(p*2+1)];Node&z=tr[static_cast<size_t>(p)];z.sum=x.sum+y.sum;if(x.mx==y.mx){z.mx=x.mx;z.count=x.count+y.count;z.second=max(x.second,y.second);}else if(x.mx>y.mx){z.mx=x.mx;z.count=x.count;z.second=max(x.second,y.mx);}else{z.mx=y.mx;z.count=y.count;z.second=max(x.mx,y.second);}}static void apply_min(int p,long long v){Node&x=tr[static_cast<size_t>(p)];x.sum+=(v-x.mx)*x.count;x.mx=v;}static void push(int p){long long v=tr[static_cast<size_t>(p)].mx;if(tr[static_cast<size_t>(p*2)].mx>v)apply_min(p*2,v);if(tr[static_cast<size_t>(p*2+1)].mx>v)apply_min(p*2+1,v);}static void build(int p,int l,int r){if(l==r){tr[static_cast<size_t>(p)]={a[static_cast<size_t>(l)],a[static_cast<size_t>(l)],LLONG_MIN,1};return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);pull(p);}static void chmin_range(int p,int l,int r,int ql,int qr,long long v){if(qr<l||r<ql||tr[static_cast<size_t>(p)].mx<=v)return;if(ql<=l&&r<=qr&&tr[static_cast<size_t>(p)].second<v){apply_min(p,v);return;}push(p);int m=(l+r)/2;chmin_range(p*2,l,m,ql,qr,v);chmin_range(p*2+1,m+1,r,ql,qr,v);pull(p);}static long long query_sum(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return 0;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].sum;push(p);int m=(l+r)/2;return query_sum(p*2,l,m,ql,qr)+query_sum(p*2+1,m+1,r,ql,qr);}static long long query_max(int p,int l,int r,int ql,int qr){if(qr<l||r<ql)return LLONG_MIN;if(ql<=l&&r<=qr)return tr[static_cast<size_t>(p)].mx;push(p);int m=(l+r)/2;return max(query_max(p*2,l,m,ql,qr),query_max(p*2+1,m+1,r,ql,qr));}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;while(tests--){int n,m;cin>>n>>m;a.assign(static_cast<size_t>(n),0);for(auto&x:a)cin>>x;tr.assign(static_cast<size_t>(4*n+4),Node{});build(1,0,n-1);while(m--){int op,l,r;cin>>op>>l>>r;if(op==0){long long x;cin>>x;chmin_range(1,0,n-1,l,r,x);}else if(op==1)cout<<query_max(1,0,n-1,l,r)<<'\n';else cout<<query_sum(1,0,n-1,l,r)<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5306
external_platform: HDU
external_problem_id: '5306'
external_title: HDU 5306 Gorgeous Sequence
external_relation: original
source_book_pages:
  - 182
  - 192
source_pdf_pages:
  - 200
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
