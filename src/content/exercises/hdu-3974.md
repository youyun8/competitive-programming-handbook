---
id: hdu-3974
volume: upper
source_file: upper-volume
title: HDU 3974 Assign the task
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - euler-tour
  - range-assignment
  - point-query
prerequisites:
  - segment-tree
statement: 指派任務給員工時會覆蓋本人與所有下屬的現行任務；查詢單一員工任務。
constraints:
  - T <= 10
  - N,M <= 50000
  - 初始任務 -1
input_format: 每組主管關係；T x y 指派，C x 查詢。
output_format: '先輸出 Case #k:，再輸出查詢結果。'
samples:
  - input: |
      1
      3
      2 1
      3 1
      4
      C 2
      T 1 7
      C 3
      C 1
    output: |
      Case #1:
      -1
      7
      7
    explanation: 根收到任務後全部員工改做任務七。
core_knowledge: *id001
judgment: Euler 展平後用懶線段樹區間覆蓋。
hints:
  - 子樹在 DFS 序中是連續區間。
  - 任務指派是 Euler 區間賦值。
  - 查詢員工是 tin 位置單點查詢。
solution_outline: Euler 展平後用懶線段樹區間覆蓋。
proof_or_invariant: 子樹連續性把樹上操作等價成陣列區間；最新賦值標記下推保證單點取得最後任務。
common_errors:
  - 端點、開閉區間或 0/1 起始索引處理錯誤
  - 合併摘要時遺漏跨左右區間的候選
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O((N+M) log N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;static vector<int>tag;static void set_range(int p,int l,int r,int ql,int qr,int v){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){tag[static_cast<size_t>(p)]=v;return;}if(tag[static_cast<size_t>(p)]!=-2){tag[static_cast<size_t>(p*2)]=tag[static_cast<size_t>(p*2+1)]=tag[static_cast<size_t>(p)];tag[static_cast<size_t>(p)]=-2;}int m=(l+r)/2;set_range(p*2,l,m,ql,qr,v);set_range(p*2+1,m+1,r,ql,qr,v);}static int get(int p,int l,int r,int x){if(tag[static_cast<size_t>(p)]!=-2||l==r)return tag[static_cast<size_t>(p)];int m=(l+r)/2;return x<=m?get(p*2,l,m,x):get(p*2+1,m+1,r,x);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;for(int cs=1;cs<=T;++cs){int n;cin>>n;vector<vector<int>>ch(static_cast<size_t>(n+1));vector<char>child(static_cast<size_t>(n+1));for(int i=1;i<n;++i){int u,v;cin>>u>>v;ch[static_cast<size_t>(v)].push_back(u);child[static_cast<size_t>(u)]=1;}int root=1;while(child[static_cast<size_t>(root)]!=0)++root;vector<int>tin(static_cast<size_t>(n+1)),tout(static_cast<size_t>(n+1)),it(static_cast<size_t>(n+1)),st{root};int timer=0;while(!st.empty()){int x=st.back();if(it[static_cast<size_t>(x)]==0)tin[static_cast<size_t>(x)]=++timer;if(it[static_cast<size_t>(x)]<static_cast<int>(ch[static_cast<size_t>(x)].size()))st.push_back(ch[static_cast<size_t>(x)][static_cast<size_t>(it[static_cast<size_t>(x)]++)]);else{tout[static_cast<size_t>(x)]=timer;st.pop_back();}}tag.assign(static_cast<size_t>(4*n+4),-2);tag[1]=-1;int q;cin>>q;cout<<"Case #"<<cs<<":\n";while(q--){char op;int x;cin>>op>>x;if(op=='T'){int y;cin>>y;set_range(1,1,n,tin[static_cast<size_t>(x)],tout[static_cast<size_t>(x)],y);}else cout<<get(1,1,n,tin[static_cast<size_t>(x)])<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;static vector<int>tag;static void set_range(int p,int l,int r,int ql,int qr,int v){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){tag[static_cast<size_t>(p)]=v;return;}if(tag[static_cast<size_t>(p)]!=-2){tag[static_cast<size_t>(p*2)]=tag[static_cast<size_t>(p*2+1)]=tag[static_cast<size_t>(p)];tag[static_cast<size_t>(p)]=-2;}int m=(l+r)/2;set_range(p*2,l,m,ql,qr,v);set_range(p*2+1,m+1,r,ql,qr,v);}static int get(int p,int l,int r,int x){if(tag[static_cast<size_t>(p)]!=-2||l==r)return tag[static_cast<size_t>(p)];int m=(l+r)/2;return x<=m?get(p*2,l,m,x):get(p*2+1,m+1,r,x);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;for(int cs=1;cs<=T;++cs){int n;cin>>n;vector<vector<int>>ch(static_cast<size_t>(n+1));vector<char>child(static_cast<size_t>(n+1));for(int i=1;i<n;++i){int u,v;cin>>u>>v;ch[static_cast<size_t>(v)].push_back(u);child[static_cast<size_t>(u)]=1;}int root=1;while(child[static_cast<size_t>(root)]!=0)++root;vector<int>tin(static_cast<size_t>(n+1)),tout(static_cast<size_t>(n+1)),it(static_cast<size_t>(n+1)),st{root};int timer=0;while(!st.empty()){int x=st.back();if(it[static_cast<size_t>(x)]==0)tin[static_cast<size_t>(x)]=++timer;if(it[static_cast<size_t>(x)]<static_cast<int>(ch[static_cast<size_t>(x)].size()))st.push_back(ch[static_cast<size_t>(x)][static_cast<size_t>(it[static_cast<size_t>(x)]++)]);else{tout[static_cast<size_t>(x)]=timer;st.pop_back();}}tag.assign(static_cast<size_t>(4*n+4),-2);tag[1]=-1;int q;cin>>q;cout<<"Case #"<<cs<<":\n";while(q--){char op;int x;cin>>op>>x;if(op=='T'){int y;cin>>y;set_range(1,1,n,tin[static_cast<size_t>(x)],tout[static_cast<size_t>(x)],y);}else cout<<get(1,1,n,tin[static_cast<size_t>(x)])<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3974
external_platform: HDU
external_problem_id: '3974'
external_title: HDU 3974 Assign the task
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
