---
id: hdu-5443
volume: upper
source_file: upper-volume
title: HDU 5443 The Water Problem
chapter: 2
section: '2.5'
kind: external-oj
difficulty: 2
topics: ['Sparse Table', '區間最大值']
prerequisites: ['RMQ', '倍增']
statement: 給定水源大小序列，多次詢問閉區間 [l,r] 中最大的水源。
constraints: ['T ≤ 10', '0 ≤ n,q ≤ 1000', '1 ≤ a_i ≤ 10^6', '1 ≤ l ≤ r ≤ n']
input_format: 第一行測試組數；每組依序為 n、n 個水源值、q、q 行詢問。
output_format: 每個詢問輸出區間最大值。
samples:
  - input: |
      1
      5
      1 2 3 4 5
      3
      1 2
      2 4
      3 5
    output: |
      2
      4
      5
    explanation: 三段的最大水源依序是 2、4、5。
core_knowledge: ['冪長區間最大值預處理', '兩重疊區塊查詢']
judgment: max 為冪等運算，詢問可由兩個長度 2^k、允許重疊的區間合併。
hints:
  - 'st[k][i] 表示從 i 起長 2^k 的最大值。'
  - '第 k 層由兩個相鄰的第 k-1 層合併。'
  - '查詢長 L 取 k=floor(log2L)，比較左端塊與貼右端塊。'
solution_outline: 建整數 log 與最大值 ST 表；每次以兩塊 O(1) 回答。
proof_or_invariant: 建表轉移精確涵蓋目標冪長區間。查詢兩塊聯集覆蓋完整範圍，重複元素不影響最大值，故答案正確。
common_errors: ['右區塊起點少一', '浮點 log2 邊界誤差', '層數不足', '輸入測試組格式讀錯']
complexity: { time: 'O(T(n log n+q))', space: 'O(n log n)' }
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){int tests;cin>>tests;while(tests--){int n;cin>>n;for(int i=0,x;i<n;++i)cin>>x;int q;cin>>q;while(q--){int l,r;cin>>l>>r;cout<<0<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){
      ios::sync_with_stdio(false);cin.tie(nullptr);
      int tests;cin>>tests;
      while(tests--){int n;cin>>n;vector<int> logs(static_cast<size_t>(n)+1U);for(int i=2;i<=n;++i)logs[static_cast<size_t>(i)]=logs[static_cast<size_t>(i/2)]+1;int levels=n==0?1:logs[static_cast<size_t>(n)]+1;vector<vector<int>> st(static_cast<size_t>(levels),vector<int>(static_cast<size_t>(n)));for(int i=0;i<n;++i)cin>>st[0][static_cast<size_t>(i)];for(int k=1;k<levels;++k)for(int i=0;i+(1<<k)<=n;++i)st[static_cast<size_t>(k)][static_cast<size_t>(i)]=max(st[static_cast<size_t>(k-1)][static_cast<size_t>(i)],st[static_cast<size_t>(k-1)][static_cast<size_t>(i+(1<<(k-1)))]);
          int q;cin>>q;while(q--){int l,r;cin>>l>>r;--l;--r;int k=logs[static_cast<size_t>(r-l+1)];cout<<max(st[static_cast<size_t>(k)][static_cast<size_t>(l)],st[static_cast<size_t>(k)][static_cast<size_t>(r-(1<<k)+1)])<<'\n';}}
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5443
external_platform: HDU
external_problem_id: '5443'
external_title: The Water Problem
external_relation: original
source_book_pages: [65]
source_pdf_pages: [83]
review_status: verified
---

靜態區間最大值可先預處理所有冪長區間，再以兩塊回答。
