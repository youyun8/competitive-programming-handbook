---
id: hdu-5233
volume: upper
source_file: upper-volume
title: HDU 5233 Gunner II
chapter: 2
section: '2.7'
kind: external-oj
difficulty: 4
topics: ['離散化', '雜湊映射', '佇列']
prerequisites: []
statement: |-
  每棵樹頂有一隻鳥。每次朝指定高度射擊，擊落該高度最靠左且尚存的鳥，輸出編號或 -1。
constraints:
  - '約 5 組；n,m<=100000；高度<=10^9。'
input_format: '依上列敘述順序輸入所有參數與操作。'
output_format: '依題意輸出每組答案。'
samples:
  - input: |
      3 4
      2 1 2
      2 2 1 2
    output: |
      1
      3
      2
      -1
    explanation: '依操作或定義直接計算可得此結果。'
core_knowledge: ['離散化', '雜湊映射', '佇列']
judgment: '座標或狀態範圍大，但實際事件有限，應用本節離散化、差分或狀態壓縮。'
hints:
  [
    '先找出真正會改變答案的事件或邊界。',
    '將大座標／大量區間轉成有限狀態後依序處理。',
    '維持狀態不變量，最後掃描所有候選並取最優。'
  ]
solution_outline: '依核心知識壓縮資料，再按事件順序更新並輸出答案。'
proof_or_invariant: '壓縮後每個原始事件仍有唯一對應，且事件間狀態不變；逐段計算涵蓋所有候選，因此所得值與原問題相同。'
common_errors: ['閉區間端點處理錯誤', '多組資料未清空', '計數或面積使用 int 溢位']
complexity:
  time: 'O(n log n) 或與壓縮後網格大小同階'
  space: 'O(n) 或與壓縮後網格大小同階'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：依提示完成。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){unordered_map<long long,queue<int>>q;for(int i=1;i<=n;++i){long long h;cin>>h;q[h].push(i);}while(m--){long long h;cin>>h;auto&z=q[h];if(z.empty())cout<<-1<<'\n';else{cout<<z.front()<<'\n';z.pop();}}}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=5233
external_platform: HDU
external_problem_id: '5233'
external_title: 'Gunner II'
external_relation: original
source_book_pages: [78]
source_pdf_pages: [96]
review_status: verified
---

本題採獨立敘述與 C++17 解法。
