---
id: hdu-2795
volume: upper
source_file: upper-volume
title: HDU 2795 Billboard
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 3
topics: &id001
  - segment-tree
  - first-fit
  - range-maximum
prerequisites:
  - segment-tree
statement: 高 h、寬 w 看板逐張放入寬 x 海報；每張放到最上方仍有足夠剩餘寬的列。
constraints:
  - h,w,n <= 200000
  - 每張高度為 1
input_format: 每組 h w n 與 n 個海報寬。
output_format: 每張輸出列號，放不下輸出 -1。
samples:
  - input: |
      3 5 4
      4
      3
      3
      3
    output: |
      1
      2
      3
      -1
    explanation: 前三張分別占三列，第四張無任何列剩三格。
core_knowledge: *id001
judgment: 最大值線段樹做 first-fit 定位與單點扣減。
hints:
  - 最多只會用 min(h,n) 列。
  - 葉節點存該列剩餘寬，內節點存最大剩餘寬。
  - 若左子最大值足夠就往左，否則往右，得到最上列。
solution_outline: 最大值線段樹做 first-fit 定位與單點扣減。
proof_or_invariant: 節點最大值判定子樹是否存在可行列；左優先搜尋因此返回最小列號。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O(n log min(h,n))
  space: O(min(h,n))
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;static vector<int>tr;static void update(int p,int l,int r,int pos,int value){if(l==r){tr[static_cast<size_t>(p)]=value;return;}int m=(l+r)/2;if(pos<=m)update(p*2,l,m,pos,value);else update(p*2+1,m+1,r,pos,value);tr[static_cast<size_t>(p)]=max(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static int locate(int p,int l,int r,int need){if(l==r)return l;int m=(l+r)/2;if(tr[static_cast<size_t>(p*2)]>=need)return locate(p*2,l,m,need);return locate(p*2+1,m+1,r,need);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int h,w,n;while(cin>>h>>w>>n){int rows=min(h,n);tr.assign(static_cast<size_t>(4*rows+4),0);tr[1]=w;if(rows>1){function<void(int,int,int)>build=[&](int p,int l,int r){if(l==r){tr[static_cast<size_t>(p)]=w;return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);tr[static_cast<size_t>(p)]=w;};build(1,1,rows);}for(int i=0;i<n;++i){int x;cin>>x;if(tr[1]<x){cout<<-1<<'\n';continue;}int row=locate(1,1,rows,x);int remain=0;function<int(int,int,int)>get=[&](int p,int l,int r){if(l==r)return tr[static_cast<size_t>(p)];int m=(l+r)/2;return row<=m?get(p*2,l,m):get(p*2+1,m+1,r);};remain=get(1,1,rows)-x;update(1,1,rows,row,remain);cout<<row<<'\n';}}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;static vector<int>tr;static void update(int p,int l,int r,int pos,int value){if(l==r){tr[static_cast<size_t>(p)]=value;return;}int m=(l+r)/2;if(pos<=m)update(p*2,l,m,pos,value);else update(p*2+1,m+1,r,pos,value);tr[static_cast<size_t>(p)]=max(tr[static_cast<size_t>(p*2)],tr[static_cast<size_t>(p*2+1)]);}static int locate(int p,int l,int r,int need){if(l==r)return l;int m=(l+r)/2;if(tr[static_cast<size_t>(p*2)]>=need)return locate(p*2,l,m,need);return locate(p*2+1,m+1,r,need);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int h,w,n;while(cin>>h>>w>>n){int rows=min(h,n);tr.assign(static_cast<size_t>(4*rows+4),0);tr[1]=w;if(rows>1){function<void(int,int,int)>build=[&](int p,int l,int r){if(l==r){tr[static_cast<size_t>(p)]=w;return;}int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);tr[static_cast<size_t>(p)]=w;};build(1,1,rows);}for(int i=0;i<n;++i){int x;cin>>x;if(tr[1]<x){cout<<-1<<'\n';continue;}int row=locate(1,1,rows,x);int remain=0;function<int(int,int,int)>get=[&](int p,int l,int r){if(l==r)return tr[static_cast<size_t>(p)];int m=(l+r)/2;return row<=m?get(p*2,l,m):get(p*2+1,m+1,r);};remain=get(1,1,rows)-x;update(1,1,rows,row,remain);cout<<row<<'\n';}}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2795
external_platform: HDU
external_problem_id: '2795'
external_title: HDU 2795 Billboard
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
