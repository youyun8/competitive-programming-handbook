---
id: hdu-1255
volume: upper
source_file: upper-volume
title: HDU 1255 覆蓋的面積
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 4
topics: &id001
  - scanline
  - coverage-at-least-two
prerequisites:
  - segment-tree
statement: 求平面上被至少兩個矩形覆蓋的總面積。
constraints:
  - T <= 100
  - N <= 1000
  - 座標 0..100000，為浮點數
input_format: 每組 N 與 N 個矩形座標。
output_format: 每組輸出面積至小數點後兩位。
samples:
  - input: |
      1
      2
      0 0 2 2
      1 1 3 3
    output: |
      1.00
    explanation: 兩個正方形重疊的單位正方形面積為一。
core_knowledge: *id001
judgment: 掃描線線段樹維護 one、two 長度。
hints:
  - 沿 y 掃描並離散 x。
  - 節點同時維護至少一次與至少兩次覆蓋長。
  - 節點 cover=1 時，兩次覆蓋長等於子節點至少一次覆蓋長。
solution_outline: 掃描線線段樹維護 one、two 長度。
proof_or_invariant: 依 cover 為 0、1、至少2 分類的 push-up 精確平移子樹覆蓋層級；帶狀面積相加即答案。
common_errors:
  - 端點、開閉區間或 0/1 起始索引處理錯誤
  - 合併摘要時遺漏跨左右區間的候選
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(N log N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示自行完成核心。
  #include <bits/stdc++.h>
  using namespace std;struct E{double y,x1,x2;int d;};struct N{int c=0;double one=0,two=0;};static vector<double>xs;static vector<N>tr;static void pull(int p,int l,int r){double all=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)],a1=0,a2=0;if(l!=r){a1=tr[static_cast<size_t>(p*2)].one+tr[static_cast<size_t>(p*2+1)].one;a2=tr[static_cast<size_t>(p*2)].two+tr[static_cast<size_t>(p*2+1)].two;}tr[static_cast<size_t>(p)].one=tr[static_cast<size_t>(p)].c>=1?all:a1;tr[static_cast<size_t>(p)].two=tr[static_cast<size_t>(p)].c>=2?all:(tr[static_cast<size_t>(p)].c==1?a1:a2);}static void upd(int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].c+=d;else{int m=(l+r)/2;upd(p*2,l,m,ql,qr,d);upd(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;cout<<fixed<<setprecision(2);while(T--){int n;cin>>n;vector<E>e;xs.clear();for(int i=0;i<n;++i){double x1,y1,x2,y2;cin>>x1>>y1>>x2>>y2;e.push_back({y1,x1,x2,1});e.push_back({y2,x1,x2,-1});xs.push_back(x1);xs.push_back(x2);}sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(e.begin(),e.end(),[](const E&a,const E&b){return a.y<b.y;});tr.assign(xs.size()*4+4,N{});double ans=0;for(size_t i=0;i<e.size();++i){int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x1)-xs.begin()),r=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x2)-xs.begin())-1;upd(1,0,static_cast<int>(xs.size())-2,l,r,e[i].d);if(i+1<e.size())ans+=tr[1].two*(e[i+1].y-e[i].y);}cout<<ans<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct E{double y,x1,x2;int d;};struct N{int c=0;double one=0,two=0;};static vector<double>xs;static vector<N>tr;static void pull(int p,int l,int r){double all=xs[static_cast<size_t>(r+1)]-xs[static_cast<size_t>(l)],a1=0,a2=0;if(l!=r){a1=tr[static_cast<size_t>(p*2)].one+tr[static_cast<size_t>(p*2+1)].one;a2=tr[static_cast<size_t>(p*2)].two+tr[static_cast<size_t>(p*2+1)].two;}tr[static_cast<size_t>(p)].one=tr[static_cast<size_t>(p)].c>=1?all:a1;tr[static_cast<size_t>(p)].two=tr[static_cast<size_t>(p)].c>=2?all:(tr[static_cast<size_t>(p)].c==1?a1:a2);}static void upd(int p,int l,int r,int ql,int qr,int d){if(qr<l||r<ql)return;if(ql<=l&&r<=qr)tr[static_cast<size_t>(p)].c+=d;else{int m=(l+r)/2;upd(p*2,l,m,ql,qr,d);upd(p*2+1,m+1,r,ql,qr,d);}pull(p,l,r);}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;cout<<fixed<<setprecision(2);while(T--){int n;cin>>n;vector<E>e;xs.clear();for(int i=0;i<n;++i){double x1,y1,x2,y2;cin>>x1>>y1>>x2>>y2;e.push_back({y1,x1,x2,1});e.push_back({y2,x1,x2,-1});xs.push_back(x1);xs.push_back(x2);}sort(xs.begin(),xs.end());xs.erase(unique(xs.begin(),xs.end()),xs.end());sort(e.begin(),e.end(),[](const E&a,const E&b){return a.y<b.y;});tr.assign(xs.size()*4+4,N{});double ans=0;for(size_t i=0;i<e.size();++i){int l=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x1)-xs.begin()),r=static_cast<int>(lower_bound(xs.begin(),xs.end(),e[i].x2)-xs.begin())-1;upd(1,0,static_cast<int>(xs.size())-2,l,r,e[i].d);if(i+1<e.size())ans+=tr[1].two*(e[i+1].y-e[i].y);}cout<<ans<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1255
external_platform: HDU
external_problem_id: '1255'
external_title: HDU 1255 覆蓋的面積
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
