---
id: hdu-3639
volume: lower
source_file: lower-volume
original_label: 'HDU 3639'
title: 'HDU 3639 Hawk-and-Chicken'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 4
topics: [strongly-connected-components, reachability]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  A 支持 B 且支持可傳遞；每人不計自己的支持，求最大支持數及所有獲得該數的人。
constraints: [2 <= n <= 5000, 0 <= m <= 30000, T <= 50]
input_format: >-
  第一行 T；每組 n m，接著 m 行 0-based 的 A B。
output_format: >-
  每組先輸出 Case k: 最大支持數，下一行遞增輸出所有贏家。
samples:
  - input: |
      1
      3 2
      0 1
      1 2
    output: |
      Case 1: 2
      2
    explanation: >-
      0 支持 1 並傳遞支持 2，1 也支持 2。
core_knowledge: [SCC 縮點, 反圖可達統計]
judgment: >-
  同 SCC 每個人得票相同；候選只需考慮原縮點 DAG 的出度零分量。
hints:
  - 把邊反向後搜尋支持來源。
  - SCC 內其他 size−1 人都支持候選。
  - 以 visited 防止多路徑重複計票。
solution_outline: >-
  Tarjan 縮點，反向建立 DAG；對原 DAG 匯分量 DFS，累加所有可達分量大小再減一。
proof_or_invariant: >-
  候選得票者正是原圖可達候選的所有其他點；反圖 DFS 精確枚舉此集合。非匯分量可到下一分量，後者支持集合嚴格包含前者，故最大者必在匯分量。
complexity: { time: 'O(n + m + C*(C+m))', space: 'O(n + m)' }
common_errors: [同一來源經多路徑重複計票, 忘記扣自己, 只輸出分量代表]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Scc{int node_count,t=0,c=0;vector<vector<int>>g;vector<int>d,l,st,id;vector<char>on;explicit Scc(int count):node_count(count),g(static_cast<size_t>(count)),d(static_cast<size_t>(count)),l(static_cast<size_t>(count)),id(static_cast<size_t>(count)),on(static_cast<size_t>(count)){}void dfs(int u){d[u]=l[u]=++t;st.push_back(u);on[u]=1;for(int v:g[u])if(!d[v])dfs(v),l[u]=min(l[u],l[v]);else if(on[v])l[u]=min(l[u],d[v]);if(d[u]==l[u]){while(true){int v=st.back();st.pop_back();on[v]=0;id[v]=c;if(v==u)break;}++c;}}void run(){for(int i=0;i<node_count;++i)if(!d[i])dfs(i);}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n,m;cin>>n>>m;Scc s(n);vector<pair<int,int>>e;while(m--){int u,v;cin>>u>>v;s.g[u].push_back(v);e.push_back({u,v});}s.run();vector<int>sz(s.c),out(s.c);vector<vector<int>>rev(s.c);for(int x:s.id)++sz[x];for(auto [u,v]:e)if(s.id[u]!=s.id[v])rev[s.id[v]].push_back(s.id[u]),out[s.id[u]]=1;vector<int>score(s.c,-1);int best=-1;for(int c=0;c<s.c;++c)if(!out[c]){vector<char>seen(s.c);int sum=0;auto dfs=[&](auto&&self,int u)->void{if(seen[u])return;seen[u]=1;sum+=sz[u];for(int v:rev[u])self(self,v);};dfs(dfs,c);score[c]=sum-1;best=max(best,score[c]);}cout<<"Case "<<cs<<": "<<best<<'\n';bool first=true;for(int i=0;i<n;++i)if(score[s.id[i]]==best){if(!first)cout<<' ';cout<<i;first=false;}cout<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=3639
external_platform: HDU
external_problem_id: '3639'
external_title: 'Hawk-and-Chicken'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
