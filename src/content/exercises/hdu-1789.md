---
id: hdu-1789
volume: upper
source_file: upper-volume
title: HDU 1789 Doing Homework again
chapter: 2
section: '2.10'
kind: external-oj
difficulty: 4
topics: ['截止期限排程', '貪心']
prerequisites: []
statement: |-
  每份作業耗時一天，逾期會扣指定分數；安排順序使總扣分最少。
constraints:
  - 'T 組；n<=1000。'
input_format: '依題意輸入測資數、規模與各項資料。'
output_format: '依題意輸出每組最優值。'
samples:
  - input: |
      1
      3
      1 3 1
      6 2 3
    output: |
      3
    explanation: '按所述限制比較所有必要決策後，可得此最優值。'
core_knowledge: ['截止期限排程', '貪心']
judgment: '目標具有可交換的局部決策；以兩項交換論證導出排序規則，再線性累積。'
hints:
  [
    '先假設只有兩個候選，分別計算兩種先後順序。',
    '把較優順序化成可排序的比較式，避免浮點除法。',
    '排序後維護前綴量、剩餘量或樹形子問題答案。'
  ]
solution_outline: '依交換論證得到貪心次序，排序後逐項更新累積狀態與答案。'
proof_or_invariant: '若相鄰兩項違反比較式，交換它們不會使目標更差；反覆消除逆序可得到排序後方案，因此該方案全域最優。'
common_errors: ['比較器不滿足嚴格弱序', '乘積或總和溢位', '相等與邊界狀況處理錯誤']
complexity:
  time: 'O(n log n)'
  space: 'O(n)'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：完成貪心排序與答案計算。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;struct J{int d,p;};int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int n;cin>>n;vector<J>a(n);for(auto&x:a)cin>>x.d;for(auto&x:a)cin>>x.p;sort(a.begin(),a.end(),[](J x,J y){return x.p>y.p;});vector<char>used(n+1);int loss=0;for(auto x:a){int d=min(n,x.d);while(d>0&&used[d])--d;if(d)used[d]=1;else loss+=x.p;}cout<<loss<<'\n';}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1789
external_platform: HDU
external_problem_id: '1789'
external_title: 'Doing Homework again'
external_relation: original
source_book_pages: [99]
source_pdf_pages: [117]
review_status: verified
---

本題採獨立繁體中文敘述與 C++17 解法。
