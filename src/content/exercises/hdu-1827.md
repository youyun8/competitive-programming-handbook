---
id: hdu-1827
volume: lower
source_file: lower-volume
original_label: 'HDU 1827'
title: 'HDU 1827 Summer Holiday'
chapter: 10
section: '10.5'
kind: external-oj
difficulty: 3
topics: [strongly-connected-components, greedy]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  每人可沿有向聯絡關係通知他人。直接通知第 i 人有指定費用，求通知所有人的最少直接聯絡人數與在此前提下的最低費用。
constraints: [1 <= n <= 1000, 1 <= m <= 2000, 多組資料直到 EOF]
input_format: >-
  每組 n m、n 個費用、m 行 x y。
output_format: >-
  每組輸出最少人數與最低總費用。
samples:
  - input: |
      3 2
      5 2 4
      1 2
      2 3
    output: |
      1 5
    explanation: >-
      只需直接通知無外來來源的分量 1。
core_knowledge: [SCC 縮點, 源分量最小權]
judgment: >-
  每個入度零 SCC 必須且只需選一人，選分量內費用最小者。
hints:
  - 互相可達者只需通知一人。
  - 縮點 DAG 的源無法被其他分量通知。
  - 每個源分量獨立取最小費用。
solution_outline: >-
  Tarjan 後統計跨分量入度與各分量最低費用，累加所有入度零分量。
proof_or_invariant: >-
  任一源分量沒有外來路徑故至少直接通知一人；通知其中任一人可覆蓋整個分量並沿 DAG 向後傳播，所有源合起來覆蓋全圖。
complexity: { time: 'O(n + m)', space: 'O(n + m)' }
common_errors: [統計出度零, 每個 SCC 都選人, 源分量未取最低費]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  struct Scc{int node_count,t=0,c=0;vector<vector<int>>g;vector<int>d,l,st,id;vector<char>on;explicit Scc(int count):node_count(count),g(static_cast<size_t>(count)),d(static_cast<size_t>(count)),l(static_cast<size_t>(count)),id(static_cast<size_t>(count)),on(static_cast<size_t>(count)){}void dfs(int u){d[u]=l[u]=++t;st.push_back(u);on[u]=1;for(int v:g[u])if(!d[v])dfs(v),l[u]=min(l[u],l[v]);else if(on[v])l[u]=min(l[u],d[v]);if(d[u]==l[u]){while(true){int v=st.back();st.pop_back();on[v]=0;id[v]=c;if(v==u)break;}++c;}}void run(){for(int i=0;i<node_count;++i)if(!d[i])dfs(i);}};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){vector<int>w(n);for(int&x:w)cin>>x;Scc s(n);vector<pair<int,int>>e;while(m--){int u,v;cin>>u>>v;--u;--v;s.g[u].push_back(v);e.push_back({u,v});}s.run();vector<int>in(s.c),mn(s.c,INT_MAX);for(int i=0;i<n;++i)mn[s.id[i]]=min(mn[s.id[i]],w[i]);for(auto [u,v]:e)if(s.id[u]!=s.id[v])in[s.id[v]]=1;int cnt=0,sum=0;for(int i=0;i<s.c;++i)if(!in[i])++cnt,sum+=mn[i];cout<<cnt<<' '<<sum<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1827
external_platform: HDU
external_problem_id: '1827'
external_title: 'Summer Holiday'
external_relation: original
source_book_pages: [627]
source_pdf_pages: [257]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
