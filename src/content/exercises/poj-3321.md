---
id: poj-3321
volume: upper
source_file: upper-volume
title: POJ 3321 Apple Tree：子樹蘋果動態計數
chapter: 4
section: '4.2'
kind: external-oj
difficulty: 4
topics: &id001
  - euler-tour
  - fenwick-tree
  - subtree-query
prerequisites:
  - fenwick-tree
statement: 根為 1 的樹每節點起初有一顆蘋果；C x 切換 x 是否有蘋果，Q x 查 x 子樹蘋果數。
constraints:
  - N <= 100000
  - 操作數 <= 100000
input_format: 輸入 N、N-1 條邊、M，再給 C/Q 指令。
output_format: 每個 Q 輸出子樹蘋果數。
samples:
  - input: |
      3
      1 2
      1 3
      4
      Q 1
      C 2
      Q 1
      Q 2
    output: |
      3
      2
      0
    explanation: 切換節點 2 後根子樹剩兩顆，節點 2 子樹為零。
core_knowledge: *id001
judgment: Euler 序加 BIT 處理切換與子樹和。
hints:
  - DFS 進入時間可把每棵子樹攤平成連續區間。
  - 切換節點等同 Euler 位置做 ±1 單點更新。
  - 子樹查詢就是 [tin,tout] 區間和。
solution_outline: Euler 序加 BIT 處理切換與子樹和。
proof_or_invariant: DFS 的括號性質保證子樹節點恰佔連續時間區間，BIT 的單點狀態和即為蘋果數。
common_errors:
  - 索引基準或閉區間端點處理錯誤
  - 更新資料結構後忘記同步原始狀態
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O((N+M) log N)
  space: O(N)
cpp_skeleton: |
  // TODO：先依三階段提示自行完成核心；以下框架可嚴格編譯。
  #include <bits/stdc++.h>
  using namespace std;
  class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}long long sum(int x)const{long long s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<long long>t;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;cin>>n;vector<vector<int>>g(static_cast<size_t>(n+1));for(int i=1;i<n;++i){int a,b;cin>>a>>b;g[static_cast<size_t>(a)].push_back(b);g[static_cast<size_t>(b)].push_back(a);}vector<int>tin(static_cast<size_t>(n+1)),tout(static_cast<size_t>(n+1)),parent(static_cast<size_t>(n+1)),it(static_cast<size_t>(n+1));int timer=0;vector<int>st{1};while(!st.empty()){int x=st.back();if(it[static_cast<size_t>(x)]==0)tin[static_cast<size_t>(x)]=++timer;if(it[static_cast<size_t>(x)]<static_cast<int>(g[static_cast<size_t>(x)].size())){int y=g[static_cast<size_t>(x)][static_cast<size_t>(it[static_cast<size_t>(x)]++)];if(y!=parent[static_cast<size_t>(x)]){parent[static_cast<size_t>(y)]=x;st.push_back(y);}}else{tout[static_cast<size_t>(x)]=timer;st.pop_back();}}Fenwick bit(n);vector<char>has(static_cast<size_t>(n+1),1);for(int i=1;i<=n;++i)bit.add(i,1);int q;cin>>q;while(q--){char op;int x;cin>>op>>x;if(op=='C'){int delta=has[static_cast<size_t>(x)]!=0?-1:1;has[static_cast<size_t>(x)]^=1;bit.add(tin[static_cast<size_t>(x)],delta);}else cout<<bit.sum(tout[static_cast<size_t>(x)])-bit.sum(tin[static_cast<size_t>(x)]-1)<<'\n';}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  class Fenwick{public:explicit Fenwick(int n):t(static_cast<size_t>(n+1)){}void add(int x,int v){for(int i=x;i<static_cast<int>(t.size());i+=i&-i)t[static_cast<size_t>(i)]+=v;}long long sum(int x)const{long long s=0;for(int i=x;i>0;i-=i&-i)s+=t[static_cast<size_t>(i)];return s;}private:vector<long long>t;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;cin>>n;vector<vector<int>>g(static_cast<size_t>(n+1));for(int i=1;i<n;++i){int a,b;cin>>a>>b;g[static_cast<size_t>(a)].push_back(b);g[static_cast<size_t>(b)].push_back(a);}vector<int>tin(static_cast<size_t>(n+1)),tout(static_cast<size_t>(n+1)),parent(static_cast<size_t>(n+1)),it(static_cast<size_t>(n+1));int timer=0;vector<int>st{1};while(!st.empty()){int x=st.back();if(it[static_cast<size_t>(x)]==0)tin[static_cast<size_t>(x)]=++timer;if(it[static_cast<size_t>(x)]<static_cast<int>(g[static_cast<size_t>(x)].size())){int y=g[static_cast<size_t>(x)][static_cast<size_t>(it[static_cast<size_t>(x)]++)];if(y!=parent[static_cast<size_t>(x)]){parent[static_cast<size_t>(y)]=x;st.push_back(y);}}else{tout[static_cast<size_t>(x)]=timer;st.pop_back();}}Fenwick bit(n);vector<char>has(static_cast<size_t>(n+1),1);for(int i=1;i<=n;++i)bit.add(i,1);int q;cin>>q;while(q--){char op;int x;cin>>op>>x;if(op=='C'){int delta=has[static_cast<size_t>(x)]!=0?-1:1;has[static_cast<size_t>(x)]^=1;bit.add(tin[static_cast<size_t>(x)],delta);}else cout<<bit.sum(tout[static_cast<size_t>(x)])-bit.sum(tin[static_cast<size_t>(x)]-1)<<'\n';}}
external_url: http://poj.org/problem?id=3321
external_platform: POJ
external_problem_id: '3321'
external_title: POJ 3321 Apple Tree
external_relation: original
source_book_pages:
  - 151
  - 170
source_pdf_pages:
  - 169
  - 188
review_status: verified
---

本卡片依外部題面與限制獨立整理。
