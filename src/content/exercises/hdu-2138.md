---
id: hdu-2138
volume: lower
source_file: lower-volume
title: HDU 2138 How many prime numbers：質數計數
chapter: 6
section: '6.10'
kind: external-oj
difficulty: 2
topics:
  - 質數判定
  - 試除法
prerequisites:
  - prime-numbers
statement: >-
  讀入多組整數序列，對每組輸出其中質數的個數。
constraints:
  - 每個待判定整數滿足 2 <= x <= 2^31 - 1
  - 輸入包含多組資料，直到 EOF
input_format: >-
  每組先給整數 N，接著給 N 個整數；資料讀到 EOF。
output_format: >-
  每組輸出一行，為序列中的質數數量。
samples:
  - input: |
      3
      2 3 4
    output: |
      2
    explanation: >-
      2、3 是質數，4 是合數，因此答案為 2；此為可信 HDU 題面存檔所列官方樣例。
hints:
  - >-
    合數 x 一定有不大於 sqrt(x) 的因數。
  - >-
    先特判 2，再排除偶數，只枚舉奇數因數。
  - >-
    以 d <= x / d 取代 d*d <= x，可避免 32 位乘法溢位。
core_knowledge:
  - 質數判定
  - 平方根試除
judgment: >-
  數值僅到 32 位元，逐數試除至平方根即可，不需要建到 2^31 的篩表。
solution_outline: >-
  對每個 x 執行確定性的試除判定；小於 2 非質數，2 是質數，其他偶數不是質數，再測試 3,5,... 到 sqrt(x)。
proof_or_invariant: >-
  若 x 為合數，可寫成 ab=x，至少一個因數不大於 sqrt(x)，試除必會找到；若沒有任何此範圍內的因數，x 不可能為合數。
common_errors:
  - 把 1 判為質數
  - 迴圈使用 d*d 而在 32 位整數溢位
  - 只處理一組資料而忽略 EOF
complexity:
  time: O(N sqrt(max x))
  space: O(1)
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;

  int main() {
      ios::sync_with_stdio(false);
      cin.tie(nullptr);
      // TODO：依照三個提示完成演算法；先保留可編譯的輸入輸出骨架。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  static bool is_prime(int value) {
      if (value == 2) return true;
      if (value < 2 || value % 2 == 0) return false;
      for (int divisor = 3; divisor <= value / divisor; divisor += 2)
          if (value % divisor == 0) return false;
      return true;
  }
  int main() {
      ios::sync_with_stdio(false); cin.tie(nullptr);
      int count;
      while (cin >> count) {
          int answer = 0;
          for (int i = 0, value = 0; i < count; ++i) {
              cin >> value;
              if (is_prime(value)) ++answer;
          }
          cout << answer << '\n';
      }
      return 0;
  }
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2138
external_platform: HDU
external_problem_id: '2138'
external_title: 'How many prime numbers'
external_relation: original
original_label: 'HDU 2138'
source_book_pages: [424, 430]
source_pdf_pages: [54, 60]
review_status: verified
---

這題用來確認質數判定的邊界是否寫穩。

原始題單中本題位於第 6.10 節、例題 第 1 題；競賽來源記為「HDU 2007-11 Programming Contest WarmUp」。可用小範圍直接枚舉作為對拍程式，逐一比較最佳化版本。
