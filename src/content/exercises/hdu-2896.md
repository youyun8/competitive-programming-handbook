---
id: hdu-2896
volume: lower
source_file: lower-volume
title: HDU 2896 病毒侵襲
chapter: 9
section: '9.5'
kind: external-oj
difficulty: 4
topics: [aho-corasick, trie, failure-link]
prerequisites: [kmp, trie]
statement: '以可見 ASCII 病毒碼掃描多個網站；對每個含毒網站輸出其包含的病毒編號，最後輸出含毒網站總數。'
constraints:
  - '1 <= n <= 500'
  - '病毒碼長度 20 到 200'
  - '1 <= m <= 1000'
  - '每網站含病毒種類不超過 3'
input_format: '先 n 與 n 個病毒碼，再 m 與 m 行網站源碼。'
output_format: '只輸出含毒網站，格式 web i: ids；最後輸出 total: count。'
samples:
  - input: "3\naaa\nbbb\nccc\n2\naaabbbccc\nbbaacc\n"
    output: "web 1: 1 2 3\ntotal: 1"
    explanation: '第一網站含三種病毒，第二網站沒有完整病毒碼。'
core_knowledge:
  - 多模式 Trie
  - fail 後綴跳轉
  - 線性文本掃描
judgment: '只輸出含毒網站，格式 web i: ids；最後輸出 total: count。'
hints:
  - '字元集是可見 ASCII，不能只建 26 條邊。'
  - '為每節點保存終止編號及最近終止 fail 跳轉。'
  - '每網站收集命中編號後排序去重，再依固定格式輸出。'
solution_outline: 'ASCII AC 自動機掃每個網站，沿 output-link 列出終止模式，排序去重。'
proof_or_invariant: 'AC 狀態與 fail 鏈涵蓋目前位置結束的所有模式；output-link 只跳終止節點而不漏模式。每網站集合去重，故輸出恰為包含的病毒種類。'
common_errors:
  - '陣列只開 26'
  - '同一病毒多次出現重複輸出'
  - 'web 與 total 格式或空格錯誤'
complexity:
  time: O(模式總長 + 文本長 + 輸出量)
  space: O(模式總長乘字元集)
cpp_skeleton: |
  #include <iostream>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：建立 AC 自動機。*/return 0;}
cpp_solution: |
  #include <algorithm>
  #include <array>
  #include <iostream>
  #include <queue>
  #include <string>
  #include <vector>
  using namespace std;
  struct Node{array<int,128> next{};int fail=0;int output_link=0;vector<int> ids;};
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n=0;while(cin>>n){vector<Node>a(1);string s;for(int id=1;id<=n;++id){cin>>s;int u=0;for(unsigned char c:s){if(a[static_cast<size_t>(u)].next[c]==0){a[static_cast<size_t>(u)].next[c]=static_cast<int>(a.size());a.push_back({});}u=a[static_cast<size_t>(u)].next[c];}a[static_cast<size_t>(u)].ids.push_back(id);}queue<int>q;for(int c=0;c<128;++c){int v=a[0].next[static_cast<size_t>(c)];if(v!=0)q.push(v);}while(!q.empty()){int u=q.front();q.pop();int f=a[static_cast<size_t>(u)].fail;a[static_cast<size_t>(u)].output_link=!a[static_cast<size_t>(f)].ids.empty()?f:a[static_cast<size_t>(f)].output_link;for(int c=0;c<128;++c){int&v=a[static_cast<size_t>(u)].next[static_cast<size_t>(c)];if(v!=0){a[static_cast<size_t>(v)].fail=a[static_cast<size_t>(f)].next[static_cast<size_t>(c)];q.push(v);}else v=a[static_cast<size_t>(f)].next[static_cast<size_t>(c)];}}int m=0,total=0;cin>>m;for(int site=1;site<=m;++site){cin>>s;vector<int>found;int u=0;for(unsigned char c:s){u=a[static_cast<size_t>(u)].next[c];for(int v=u;v!=0;v=a[static_cast<size_t>(v)].output_link)found.insert(found.end(),a[static_cast<size_t>(v)].ids.begin(),a[static_cast<size_t>(v)].ids.end());}sort(found.begin(),found.end());found.erase(unique(found.begin(),found.end()),found.end());if(!found.empty()){++total;cout<<"web "<<site<<":";for(int id:found)cout<<' '<<id;cout<<'\n';}}cout<<"total: "<<total<<'\n';}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=2896
external_platform: HDU
external_problem_id: '2896'
external_title: '病毒侵襲'
external_relation: original
source_book_pages: [575, 595]
source_pdf_pages: [205, 225]
review_status: verified
---

AC 自動機把多個模式的共同前綴與失配後綴集中到同一個狀態圖。
