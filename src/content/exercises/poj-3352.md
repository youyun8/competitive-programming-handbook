---
id: poj-3352
volume: lower
source_file: lower-volume
original_label: 'OpenJudge 百練 3091 / POJ 3352'
title: 'POJ 3352 Road Construction'
chapter: 10
section: '10.4'
kind: external-oj
difficulty: 3
topics: [bridge, edge-biconnected-components, tree]
prerequisites: [depth-first-search, graph-connectivity]
statement: >-
  給定一張連通無向圖，求至少新增多少條邊，才能使任意一條邊失效後全圖仍連通。
constraints: [1 <= n <= 5000, n-1 <= m <= 10000, 原圖連通]
input_format: >-
  第一行 n m，後接 m 行無向邊。
output_format: >-
  輸出最少新增邊數。
samples:
  - input: |
      4 3
      1 2
      2 3
      3 4
    output: |
      1
    explanation: >-
      連接 1 與 4 後所有原邊都位於環上。
core_knowledge: [橋, 邊雙連通縮點, 葉節點配對]
judgment: >-
  縮去非橋邊後得到橋樹，答案為度數一分量數量加一除二。
hints:
  - 不在任何環上的邊就是橋。
  - 縮掉每個邊雙連通分量。
  - 一條新增邊可同時消除兩個葉端的需求。
solution_outline: >-
  Tarjan 標記橋，以非橋邊染分量，統計橋樹葉數 L 並輸出 ceil(L/2)。
proof_or_invariant: >-
  每個葉分量只有一條橋連外，至少需一個新增端點，故需 ceil(L/2) 條；將葉按 DFS 次序跨子樹配對可讓每條橋落入某個新增環，達到下界。
complexity: { time: 'O(n + m)', space: 'O(n + m)' }
common_errors: [把割點數當答案, 平行邊以父頂點略過, 葉數為奇數時未上取整]
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);/* TODO：依提示完成演算法與輸出。*/return 0;}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;cin>>n>>m;vector<vector<pair<int,int>>>g(n);vector<pair<int,int>>edges(m);for(int i=0;i<m;++i){int u,v;cin>>u>>v;--u;--v;edges[i]={u,v};g[u].push_back({v,i});g[v].push_back({u,i});}vector<int>d(n),low(n);vector<char>bridge(m);int timer=0;auto dfs=[&](auto&&self,int u,int pe)->void{d[u]=low[u]=++timer;for(auto [v,id]:g[u])if(id!=pe){if(!d[v]){self(self,v,id);low[u]=min(low[u],low[v]);if(low[v]>d[u])bridge[id]=1;}else low[u]=min(low[u],d[v]);}};dfs(dfs,0,-1);vector<int>comp(n,-1);int cc=0;for(int i=0;i<n;++i)if(comp[i]<0){queue<int>q;q.push(i);comp[i]=cc;while(!q.empty()){int u=q.front();q.pop();for(auto [v,id]:g[u])if(!bridge[id]&&comp[v]<0)comp[v]=cc,q.push(v);}++cc;}if(cc==1){cout<<"0\n";return 0;}vector<int>degree(cc);for(int i=0;i<m;++i)if(bridge[i]){auto [u,v]=edges[i];++degree[comp[u]];++degree[comp[v]];}int leaves=static_cast<int>(count(degree.begin(),degree.end(),1));cout<<(leaves+1)/2<<'\n';}
external_url: http://poj.org/problem?id=3352
external_platform: POJ
external_problem_id: '3352'
external_title: 'Road Construction'
external_relation: original
source_book_pages: [619, 620]
source_pdf_pages: [249, 250]
review_status: verified
---

本卡依官方題面與可信存檔重新整理；敘述、證明與程式均為本站獨立撰寫。卡片範例已由所附 C++17 解答實際執行核對。
