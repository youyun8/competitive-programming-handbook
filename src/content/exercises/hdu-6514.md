---
id: hdu-6514
volume: upper
source_file: upper-volume
title: HDU 6514 Monitor
chapter: 2
section: '2.6'
kind: external-oj
difficulty: 4
topics: ['二維差分', '二維前綴和']
prerequisites: []
statement: |-
  給定 n×m 農地與監視矩形；每次詢問一個矩形是否每格都被至少一台監視器覆蓋。
constraints:
  - 'n*m<=10^7；監視器與詢問數各<=10^6。'
input_format: '依上列敘述順序輸入所有參數與操作。'
output_format: '依題意輸出每組答案。'
samples:
  - input: |
      2 2
      1
      1 1 1 2
      2
      1 1 1 2
      1 1 2 2
    output: |
      YES
      NO
    explanation: '依操作或定義直接計算可得此結果。'
core_knowledge: ['二維差分', '二維前綴和']
judgment: '座標或狀態範圍大，但實際事件有限，應用本節離散化、差分或狀態壓縮。'
hints:
  [
    '先找出真正會改變答案的事件或邊界。',
    '將大座標／大量區間轉成有限狀態後依序處理。',
    '維持狀態不變量，最後掃描所有候選並取最優。'
  ]
solution_outline: '依核心知識壓縮資料，再按事件順序更新並輸出答案。'
proof_or_invariant: '壓縮後每個原始事件仍有唯一對應，且事件間狀態不變；逐段計算涵蓋所有候選，因此所得值與原問題相同。'
common_errors: ['閉區間端點處理錯誤', '多組資料未清空', '計數或面積使用 int 溢位']
complexity:
  time: 'O(n log n) 或與壓縮後網格大小同階'
  space: 'O(n) 或與壓縮後網格大小同階'
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;
  int main() {
      // TODO：依提示完成。
      return 0;
  }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m;while(cin>>n>>m){int w=m+2;vector<int>a(static_cast<size_t>(n+2)*w);auto at=[&](int x,int y)->int&{return a[static_cast<size_t>(x)*w+y];};int p;cin>>p;while(p--){int x,y,u,v;cin>>x>>y>>u>>v;++at(x,y);--at(u+1,y);--at(x,v+1);++at(u+1,v+1);}for(int i=1;i<=n;++i)for(int j=1;j<=m;++j)at(i,j)+=at(i-1,j)+at(i,j-1)-at(i-1,j-1);for(int i=1;i<=n;++i)for(int j=1;j<=m;++j)at(i,j)=at(i,j)>0?1:0;for(int i=1;i<=n;++i)for(int j=1;j<=m;++j)at(i,j)+=at(i-1,j)+at(i,j-1)-at(i-1,j-1);int q;cin>>q;while(q--){int x,y,u,v;cin>>x>>y>>u>>v;int sum=at(u,v)-at(x-1,v)-at(u,y-1)+at(x-1,y-1);cout<<(sum==(u-x+1)*(v-y+1)?"YES":"NO")<<'\n';}}return 0;}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=6514
external_platform: HDU
external_problem_id: '6514'
external_title: 'Monitor'
external_relation: original
source_book_pages: [75]
source_pdf_pages: [93]
review_status: verified
---

本題採獨立敘述與 C++17 解法。
