---
id: hdu-1698
volume: upper
source_file: upper-volume
title: HDU 1698 Just a Hook
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 3
topics: &id001
  - lazy-segment-tree
  - range-assignment
prerequisites:
  - segment-tree
statement: 長鉤初始每段價值 1；多次把閉區間所有段賦成 1、2 或 3，求最終總值。
constraints:
  - T <= 10
  - N <= 100000
  - Q <= 100000
input_format: 每組 N Q 與 Q 個 x y z。
output_format: 依固定 Case 字串輸出總值。
samples:
  - input: |
      1
      5 2
      1 3 2
      2 4 3
    output: |
      Case 1: The total value of the hook is 12.
    explanation: 最終值 2,3,3,3,1，總和十二。
core_knowledge: *id001
judgment: 區間賦值懶線段樹維護總和。
hints:
  - 更新是賦值而非加值。
  - 完整覆蓋節點可直接設 sum=value*length 並留標記。
  - 下探前把父賦值標記覆蓋到兩子。
solution_outline: 區間賦值懶線段樹維護總和。
proof_or_invariant: 標記代表整段當前同值，完整更新精確；部分更新下推後遞迴並重算，保持 sum 不變量。
common_errors:
  - 掃描線同座標事件或半開區間處理錯誤
  - 線段樹合併時遺漏跨界候選
  - 輸出固定字串或案例格式不符題面
complexity:
  time: O(Q log N)
  space: O(N)
cpp_skeleton: |
  // TODO：依三階段提示完成核心。
  #include <bits/stdc++.h>
  using namespace std;static vector<int>sum,lazy_tag;static void apply(int p,int l,int r,int value){sum[static_cast<size_t>(p)]=value*(r-l+1);lazy_tag[static_cast<size_t>(p)]=value;}static void push(int p,int l,int r){if(lazy_tag[static_cast<size_t>(p)]==0||l==r)return;int m=(l+r)/2;apply(p*2,l,m,lazy_tag[static_cast<size_t>(p)]);apply(p*2+1,m+1,r,lazy_tag[static_cast<size_t>(p)]);lazy_tag[static_cast<size_t>(p)]=0;}static void update(int p,int l,int r,int ql,int qr,int value){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,l,r,value);return;}push(p,l,r);int m=(l+r)/2;update(p*2,l,m,ql,qr,value);update(p*2+1,m+1,r,ql,qr,value);sum[static_cast<size_t>(p)]=sum[static_cast<size_t>(p*2)]+sum[static_cast<size_t>(p*2+1)];}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n,q;cin>>n>>q;sum.assign(static_cast<size_t>(4*n+4),0);lazy_tag=sum;apply(1,1,n,1);while(q--){int l,r,v;cin>>l>>r>>v;update(1,1,n,l,r,v);}cout<<"Case "<<cs<<": The total value of the hook is "<<sum[1]<<".\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;static vector<int>sum,lazy_tag;static void apply(int p,int l,int r,int value){sum[static_cast<size_t>(p)]=value*(r-l+1);lazy_tag[static_cast<size_t>(p)]=value;}static void push(int p,int l,int r){if(lazy_tag[static_cast<size_t>(p)]==0||l==r)return;int m=(l+r)/2;apply(p*2,l,m,lazy_tag[static_cast<size_t>(p)]);apply(p*2+1,m+1,r,lazy_tag[static_cast<size_t>(p)]);lazy_tag[static_cast<size_t>(p)]=0;}static void update(int p,int l,int r,int ql,int qr,int value){if(qr<l||r<ql)return;if(ql<=l&&r<=qr){apply(p,l,r,value);return;}push(p,l,r);int m=(l+r)/2;update(p*2,l,m,ql,qr,value);update(p*2+1,m+1,r,ql,qr,value);sum[static_cast<size_t>(p)]=sum[static_cast<size_t>(p*2)]+sum[static_cast<size_t>(p*2+1)];}int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int tests;cin>>tests;for(int cs=1;cs<=tests;++cs){int n,q;cin>>n>>q;sum.assign(static_cast<size_t>(4*n+4),0);lazy_tag=sum;apply(1,1,n,1);while(q--){int l,r,v;cin>>l>>r>>v;update(1,1,n,l,r,v);}cout<<"Case "<<cs<<": The total value of the hook is "<<sum[1]<<".\n";}}
external_url: https://acm.hdu.edu.cn/showproblem.php?pid=1698
external_platform: HDU
external_problem_id: '1698'
external_title: HDU 1698 Just a Hook
external_relation: original
source_book_pages:
  - 192
source_pdf_pages:
  - 210
review_status: verified
---

本卡片依外部題面與限制獨立整理。
