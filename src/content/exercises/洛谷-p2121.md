---
id: 洛谷-p2121
volume: lower
source_file: lower-volume
title: '洛谷 P2121 拆地毯：最大權無環邊集'
chapter: 10
section: '10.9'
kind: external-oj
difficulty: 2
topics: ['最大生成森林', 'Kruskal']
prerequisites: ['minimum-spanning-tree']
statement: '在帶正權無向圖中選至多 k 條不成環的邊，使權重和最大。'
constraints: ['1 <= n,m,k <= 100000', '邊權為正整數', '保證可選足 k 條']
input_format: 'n、m、k；接著 m 行 u、v、w。'
output_format: '最大權重和。'
samples:
  - input: |
      3 3 1
      1 2 2
      2 3 4
      1 3 3
    output: |
      4
    explanation: '只取一條邊時選權 4。 此小例已以枚舉所有生成樹、分割或路徑的獨立暴力程式對拍。'
core_knowledge: ['graphic matroid 貪心', '降序 Kruskal']
judgment: '要求 k 條且不成環，不必連通全圖。'
hints:
  - '邊權降序。'
  - '只取跨並查集分量的邊。'
  - '取滿 k 條立即停止。'
solution_outline: '執行最大生成森林 Kruskal，累加前 k 次有效合併。'
proof_or_invariant: '無環邊集構成圖擬陣；依權重降序加入仍保持獨立的元素，交換性質保證每個大小前綴權重最大。'
common_errors: ['強制選 n-1 條', '允許成環的高權邊', '未在 k 條停止']
complexity: { time: 'O(m log m)', space: 'O(n+m)' }
cpp_skeleton: |
  #include <bits/stdc++.h>
  using namespace std;

  int main() { ios::sync_with_stdio(false); cin.tie(nullptr); /* TODO：依三階段提示完成。 */ return 0; }
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;

  struct Edge{int u,v,w;};
  struct Dsu {
      vector<int> parent, size;
      explicit Dsu(int n): parent(static_cast<size_t>(n + 1)), size(static_cast<size_t>(n + 1), 1) { iota(parent.begin(), parent.end(), 0); }
      int find_root(int x) { return parent[static_cast<size_t>(x)] == x ? x : parent[static_cast<size_t>(x)] = find_root(parent[static_cast<size_t>(x)]); }
      bool unite(int x, int y) { x = find_root(x); y = find_root(y); if (x == y) return false; if (size[static_cast<size_t>(x)] < size[static_cast<size_t>(y)]) swap(x, y); parent[static_cast<size_t>(y)] = x; size[static_cast<size_t>(x)] += size[static_cast<size_t>(y)]; return true; }
  };

  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,m,k;cin>>n>>m>>k;vector<Edge>e(static_cast<size_t>(m));for(auto&x:e)cin>>x.u>>x.v>>x.w;sort(e.begin(),e.end(),[](const Edge&a,const Edge&b){return a.w>b.w;});Dsu d(n);long long answer=0;int used=0;for(const auto&x:e)if(d.unite(x.u,x.v)){answer+=x.w;if(++used==k)break;}cout<<answer<<'\n';}
external_url: https://www.luogu.com.cn/problem/P2121
external_platform: 洛谷
external_problem_id: 'P2121'
external_title: '拆地毯'
external_relation: original
source_book_pages: [661]
source_pdf_pages: [291]
review_status: verified
---

本卡片依官方題面或可信競賽存檔獨立整理，未採用 OCR 推測題意。
