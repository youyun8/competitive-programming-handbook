---
id: poj-1681
volume: lower
source_file: lower-volume
source_book_pages:
  - 401
  - 402
source_pdf_pages:
  - 31
  - 32
title: OpenJ_Bailian 1681 Painter's Problem
chapter: 6
section: '6.4'
kind: external-oj
difficulty: 3
topics:
  - 高斯消元
  - 動態規劃
prerequisites:
  - 模運算與狀態還原
statement: n×n 牆面由黃、白磚構成。粉刷一磚會翻轉自己及上下左右，求全部變黃所需最少次數，無解輸出 inf。
constraints:
  - 1<=T<=20
  - 1<=n<=15
input_format: 依題面讀入一組或多組矩陣、序列資料。
output_format: 依題面固定格式輸出數值、判定或操作方案。
samples:
  - input: |
      2
      3
      yyy
      yyy
      yyy
      5
      wwwww
      wwwww
      wwwww
      wwwww
      wwwww
    output: |
      0
      15
    explanation: 依題意模擬或代入方程可驗證此輸出。
core_knowledge:
  - 有限域消元
  - 方案構造
judgment: 枚舉第一列粉刷集合；為修正上一列，每個後續列的操作被唯一決定，最後檢查末列並取最小操作數。
hints:
  - 先確認操作可否轉成線性方程或符號選擇。
  - 利用模域消元、第一列枚舉或可達和 DP。
  - 除答案外還要保存最早行號、前驅或固定輸出格式。
solution_outline: 枚舉第一列粉刷集合；為修正上一列，每個後續列的操作被唯一決定，最後檢查末列並取最小操作數。
proof_or_invariant: 固定第一列後，第 r 列只有第 r+1 列操作尚能影響，故其選擇唯一；逐列不變量保證離開的列全黃，枚舉涵蓋全部方案。
complexity:
  time: O(T·2^n·n^2)
  space: O(n)
common_errors:
  - 星期首尾天數少加一
  - 自由變數與矛盾列判斷錯誤
  - 方案還原後操作下標未隨刪除調整
cpp_skeleton: |
  // TODO：理解證明後重寫核心消元或 DP。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int n;cin>>n;vector<string>s(n);for(auto&x:s)cin>>x;int best=1000000000;for(int mask=0;mask<(1<<n);++mask){vector<int>p(n);p[0]=mask;for(int r=1;r<n;++r){int prev=p[r-1],above=r>=2?p[r-2]:0,white=0;for(int c=0;c<n;++c)if(s[r-1][c]=='w')white|=1<<c;p[r]=white^prev^(prev<<1)^(prev>>1)^above;p[r]&=(1<<n)-1;}int last=0,r=n-1;for(int c=0;c<n;++c)if(s[r][c]=='w')last|=1<<c;int effect=last^p[r]^(p[r]<<1)^(p[r]>>1)^(n>=2?p[r-1]:0);if((effect&((1<<n)-1))==0){int cnt=0;for(int x:p)cnt+=__builtin_popcount((unsigned)x);best=min(best,cnt);}}if(best==1000000000)cout<<"inf\n";else cout<<best<<"\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int T;cin>>T;while(T--){int n;cin>>n;vector<string>s(n);for(auto&x:s)cin>>x;int best=1000000000;for(int mask=0;mask<(1<<n);++mask){vector<int>p(n);p[0]=mask;for(int r=1;r<n;++r){int prev=p[r-1],above=r>=2?p[r-2]:0,white=0;for(int c=0;c<n;++c)if(s[r-1][c]=='w')white|=1<<c;p[r]=white^prev^(prev<<1)^(prev>>1)^above;p[r]&=(1<<n)-1;}int last=0,r=n-1;for(int c=0;c<n;++c)if(s[r][c]=='w')last|=1<<c;int effect=last^p[r]^(p[r]<<1)^(p[r]>>1)^(n>=2?p[r-1]:0);if((effect&((1<<n)-1))==0){int cnt=0;for(int x:p)cnt+=__builtin_popcount((unsigned)x);best=min(best,cnt);}}if(best==1000000000)cout<<"inf\n";else cout<<best<<"\n";}}
external_url: http://bailian.openjudge.cn/practice/1681/
external_platform: OpenJ_Bailian
external_problem_id: '1681'
external_title: Painter's Problem
external_relation: original
review_status: verified
---

本題需同時證明判定與輸出構造正確。
