---
id: poj-2828
volume: upper
source_file: upper-volume
title: POJ 2828 Buy Tickets
chapter: 4
section: '4.3'
kind: external-oj
difficulty: 3
topics: &id001
  - reverse-processing
  - order-statistic-segment-tree
prerequisites:
  - segment-tree
statement: 人依序插入隊伍指定位置，已有人向後移；求最終隊列。
constraints:
  - N <= 200000
  - 多組讀到 EOF
input_format: 每人輸入插入位置 pos（0 起）與值 val。
output_format: 每組輸出最終值序列。
samples:
  - input: |
      4
      0 10
      1 20
      1 30
      0 40
    output: |
      40 10 30 20
    explanation: 逆序處理時，第 i 人佔目前第 pos+1 個空位。
core_knowledge: *id001
judgment: 倒序取第 pos+1 個空位。
hints:
  - 正序插入會移動大量元素，改從最後一人倒推。
  - 最後插入者的位置已固定；更早的人只能佔剩餘空位。
  - 線段樹維護每段空位數並找第 k 空位。
solution_outline: 倒序取第 pos+1 個空位。
proof_or_invariant: 倒序時尚空位置恰是當時隊列可插入的位置，選第 pos+1 空位與原插入語意等價。
common_errors:
  - 端點、開閉區間或 0/1 起始索引處理錯誤
  - 合併摘要時遺漏跨左右區間的候選
  - 使用 32 位元儲存可能溢位的計數或總和
complexity:
  time: O(N log N)
  space: O(N)
cpp_skeleton:
  "// TODO：依三階段提示自行完成核心。\n#include <bits/stdc++.h>\nusing namespace std;static vector<int>tr;static void build(int\
  \ p,int l,int r){tr[static_cast<size_t>(p)]=r-l+1;if(l==r)return;int m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);}static\
  \ int take(int p,int l,int r,int k){--tr[static_cast<size_t>(p)];if(l==r)return l;int m=(l+r)/2;return k<=tr[static_cast<size_t>(p*2)]?take(p*2,l,m,k):take(p*2+1,m+1,r,k-tr[static_cast<size_t>(p*2)]);}int\
  \ main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){vector<int>pos(static_cast<size_t>(n)),value(static_cast<size_t>(n)),answer(static_cast<size_t>(n+1));for(int\
  \ i=0;i<n;++i)cin>>pos[static_cast<size_t>(i)]>>value[static_cast<size_t>(i)];tr.assign(static_cast<size_t>(4*n+4),0);build(1,1,n);for(int\
  \ i=n-1;i>=0;--i)answer[static_cast<size_t>(take(1,1,n,pos[static_cast<size_t>(i)]+1))]=value[static_cast<size_t>(i)];for(int\
  \ i=1;i<=n;++i){if(i>1)cout<<' ';cout<<answer[static_cast<size_t>(i)];}cout<<'\\n';}}\n"
cpp_solution:
  "#include <bits/stdc++.h>\nusing namespace std;static vector<int>tr;static void build(int p,int l,int r){tr[static_cast<size_t>(p)]=r-l+1;if(l==r)return;int\
  \ m=(l+r)/2;build(p*2,l,m);build(p*2+1,m+1,r);}static int take(int p,int l,int r,int k){--tr[static_cast<size_t>(p)];if(l==r)return\
  \ l;int m=(l+r)/2;return k<=tr[static_cast<size_t>(p*2)]?take(p*2,l,m,k):take(p*2+1,m+1,r,k-tr[static_cast<size_t>(p*2)]);}int\
  \ main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n;while(cin>>n){vector<int>pos(static_cast<size_t>(n)),value(static_cast<size_t>(n)),answer(static_cast<size_t>(n+1));for(int\
  \ i=0;i<n;++i)cin>>pos[static_cast<size_t>(i)]>>value[static_cast<size_t>(i)];tr.assign(static_cast<size_t>(4*n+4),0);build(1,1,n);for(int\
  \ i=n-1;i>=0;--i)answer[static_cast<size_t>(take(1,1,n,pos[static_cast<size_t>(i)]+1))]=value[static_cast<size_t>(i)];for(int\
  \ i=1;i<=n;++i){if(i>1)cout<<' ';cout<<answer[static_cast<size_t>(i)];}cout<<'\\n';}}\n"
external_url: http://poj.org/problem?id=2828
external_platform: POJ
external_problem_id: '2828'
external_title: POJ 2828 Buy Tickets
external_relation: original
source_book_pages:
  - 202
source_pdf_pages:
  - 220
review_status: verified
---

本卡片依外部題面與限制獨立整理。
