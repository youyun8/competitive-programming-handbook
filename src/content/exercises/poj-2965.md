---
id: poj-2965
volume: lower
source_file: lower-volume
source_book_pages:
  - 387
  - 461
source_pdf_pages:
  - 17
  - 91
title: POJ 2965 The Pilots Brothers' refrigerator
chapter: 6
section: '6.4'
kind: external-oj
difficulty: 2
topics:
  - 線性代數
  - 數論
prerequisites:
  - 高斯消元與圖論
statement: 4x4 冰箱把手中，加號為關閉、減號為開啟；操作一格會翻轉同列同欄，求全部開啟的最少操作及方案。
constraints:
  - 固定 4x4 狀態
input_format: 依題面讀入維度、矩陣、圖或測試資料。
output_format: 依指定精度與固定字串輸出答案。
samples:
  - input: |
      -+--
      ----
      ----
      -+--
    output: |
      6
      1 1
      1 3
      1 4
      4 1
      4 3
      4 4
    explanation: 依操作定義或方程直接驗算，可得到所示結果。
core_knowledge:
  - 不變量與代數建模
  - 消元或狀態搜尋
judgment: 以 16 位元表示狀態，預算每格翻轉遮罩，從初態 BFS 至全零並記錄前驅。
hints:
  - 先將幾何、操作或連通條件寫成方程或有限狀態。
  - 選擇符合代數結構的消元、矩陣樹或 BFS。
  - 最後處理唯一性、模數、精度與輸出方案。
solution_outline: 以 16 位元表示狀態，預算每格翻轉遮罩，從初態 BFS 至全零並記錄前驅。
proof_or_invariant: 每條 BFS 邊恰代表一次合法操作；無權圖 BFS 首次到達全零的距離最短，前驅鏈給出合法方案。
complexity:
  time: O(2^16·16)
  space: O(2^16)
common_errors:
  - 主元或狀態編號錯誤
  - 非質數模數誤用逆元
  - 輸出精度或固定字串不符
cpp_skeleton: |
  // TODO：依證明自行重寫核心步驟。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){int st=0;for(int i=0;i<16;++i){char c;cin>>c;if(c=='+')st|=1<<i;}vector<int>d(1<<16,-1),pre(1<<16),op(1<<16);queue<int>q;q.push(st);d[st]=0;while(d[0]<0){int x=q.front();q.pop();for(int p=0;p<16;++p){int r=p/4,c=p%4,mask=0;for(int j=0;j<4;++j)mask^=1<<(r*4+j),mask^=1<<(j*4+c);mask^=1<<p;int y=x^mask;if(d[y]<0)d[y]=d[x]+1,pre[y]=x,op[y]=p,q.push(y);}}vector<int>a;for(int x=0;x!=st;x=pre[x])a.push_back(op[x]);reverse(a.begin(),a.end());cout<<a.size()<<"\n";for(int p:a)cout<<p/4+1<<' '<<p%4+1<<"\n";}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){int st=0;for(int i=0;i<16;++i){char c;cin>>c;if(c=='+')st|=1<<i;}vector<int>d(1<<16,-1),pre(1<<16),op(1<<16);queue<int>q;q.push(st);d[st]=0;while(d[0]<0){int x=q.front();q.pop();for(int p=0;p<16;++p){int r=p/4,c=p%4,mask=0;for(int j=0;j<4;++j)mask^=1<<(r*4+j),mask^=1<<(j*4+c);mask^=1<<p;int y=x^mask;if(d[y]<0)d[y]=d[x]+1,pre[y]=x,op[y]=p,q.push(y);}}vector<int>a;for(int x=0;x!=st;x=pre[x])a.push_back(op[x]);reverse(a.begin(),a.end());cout<<a.size()<<"\n";for(int p:a)cout<<p/4+1<<' '<<p%4+1<<"\n";}
external_url: http://poj.org/problem?id=2965
external_platform: POJ
external_problem_id: '2965'
external_title: The Pilots Brothers' refrigerator
external_relation: original
review_status: verified
---

將題意轉成可驗證的代數或圖模型。
