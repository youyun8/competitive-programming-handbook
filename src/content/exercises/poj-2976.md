---
id: poj-2976
volume: lower
source_file: lower-volume
source_book_pages:
  - 410
source_pdf_pages:
  - 40
title: POJ 2976 Dropping tests
chapter: 6
section: '6.6'
kind: external-oj
difficulty: 3
topics:
  - 線性基
  - 分數規劃
prerequisites:
  - GF(2) 與二分答案
statement: n 次測驗得分 a_i/b_i，可刪除恰 k 次，求剩餘總得分比率乘 100 的最大值並四捨五入。
constraints:
  - 1<=n<=1000，0<=k<n
  - 0<=a_i<=b_i<=10^9；0 0 結束
input_format: 依題意讀入測試組、陣列或圖資料。
output_format: 逐組輸出指定答案。
samples:
  - input: |
      3 1
      5 0 2
      5 1 6
      0 0
    output: |
      83
    explanation: 直接列舉小型案例或依判定式計算可得。
core_knowledge:
  - 等價判定
  - 線性獨立或負環
judgment: 二分比率 x，將每項轉成 100a_i-xb_i，保留最大的 n-k 項；其和非負即 x 可行。
hints:
  - 先把目標改寫成線性獨立或「答案至少為 x」的判定。
  - 找出判定中需要最大化的加總量。
  - 使用線性基、背包或負環檢測完成判定，並處理精度。
solution_outline: 二分比率 x，將每項轉成 100a_i-xb_i，保留最大的 n-k 項；其和非負即 x 可行。
proof_or_invariant: 比率至少 x 等價於所選轉換值總和非負；固定 x 時選最大 n-k 項顯然最大化總和，故判定具單調性。
complexity:
  time: O(n log n log precision)
  space: O(n)
common_errors:
  - 線性基主元方向錯誤
  - 二分可行方向顛倒
  - 64 位元或浮點精度不足
cpp_skeleton: |
  // TODO：依提示重寫核心資料結構。
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,k;while(cin>>n>>k&&(n||k)){vector<double>a(n),b(n),v(n);for(auto&x:a)cin>>x;for(auto&x:b)cin>>x;double l=0,r=100;for(int z=0;z<70;++z){double x=(l+r)/2;for(int i=0;i<n;++i)v[i]=100*a[i]-x*b[i];nth_element(v.begin(),v.begin()+k,v.end());double s=accumulate(v.begin()+k,v.end(),0.0);if(s>=0)l=x;else r=x;}cout<<(long long)floor(l+0.5)<<"\n";}}
cpp_solution: |
  #include <bits/stdc++.h>
  using namespace std;
  int main(){ios::sync_with_stdio(false);cin.tie(nullptr);int n,k;while(cin>>n>>k&&(n||k)){vector<double>a(n),b(n),v(n);for(auto&x:a)cin>>x;for(auto&x:b)cin>>x;double l=0,r=100;for(int z=0;z<70;++z){double x=(l+r)/2;for(int i=0;i<n;++i)v[i]=100*a[i]-x*b[i];nth_element(v.begin(),v.begin()+k,v.end());double s=accumulate(v.begin()+k,v.end(),0.0);if(s>=0)l=x;else r=x;}cout<<(long long)floor(l+0.5)<<"\n";}}
external_url: http://poj.org/problem?id=2976
external_platform: POJ
external_problem_id: '2976'
external_title: Dropping tests
external_relation: original
review_status: verified
---

以代數結構或單調判定取代直接枚舉。
